# W17-1 Review (judge-m3): CI Windows-only Matrix, CLI Zero-Warning Baseline, Tech-Debt挂账

- 范围：NortHing `1c9ac2f..77b69df`（含 4bc3fb1）
- 审查者：judge-m3（= minimax-m3，本仓 20+ 场实证）
- 方法：全部结论出自 `git diff` / 源码 / `git log` / 本机亲跑 cargo + node 脚本；不采信台账/报告自述
- 结论：**APPROVE** — Critical 0 / Important 0 / Minor 2 / Cannot verify 0

## 0. 独立复跑清单（HEAD = 4bc3fb1，本机亲跑）

| 命令 | 结果 |
|---|---|
| `<USERPROFILE>/.cargo/bin/rustup.exe run stable-x86_64-pc-windows-msvc cargo check -p northhing-cli` | exit 0；16 条 warning 全部归属 `northhing-core`（含同一行汇总 `warning: `northhing-core` (lib) generated 16 warnings`），`northhing-cli` 包本身 0 warning（与报告一致） |
| `node scripts/verify-rot-budget.mjs` | passed；5 grep + 3 dir + 6 god-file 全部数字逐位与报告一致（unwrap=483/502, expect=940/1089, let_underscore=370/388, unix_epoch=69/69, allow_dead_code=104/109；scripts=44/48, docs/design=1/1, .superpowers/sdd=59/400） |
| `node scripts/check-repo-hygiene.mjs` | passed；本机报 `3 content files scanned, 3832 filenames checked`（报告报 `2 / 3831`——见 Minor-1） |
| `git diff 1c9ac2f..77b69df --name-only` | `.github/workflows/ci.yml`、`docs/status/tech-debt-ledger.md`（2 文件，与 brief 允许集 + ci.yml 一致） |
| `git diff 1c9ac2f..4bc3fb1 --name-only` | `src/apps/cli/src/ui/question/mod.rs`（1 文件，与 brief 允许集 mod.rs 一致） |
| `git diff 1c9ac2f..77b69df -- src/apps/cli/src/ui/question/{types,question,render}.rs` | 零输出（types.rs/question.rs/render.rs 三文件均未触碰） |

## 1. ci.yml 改动合规性

### 1.1 矩阵收窄（brief 改动一第 1 条）

- `git diff 1c9ac2f..77b69df -- .github/workflows/ci.yml` 第一 hunk（`@@ -29,9 +29,8 @@ jobs:`）确认：
  - 删除 `- ubuntu-latest`（旧 30 行）、`- macos-15`（旧 31 行）共 2 行
  - 新增 1 行注释 `# Windows-only per user decision 2026-09-05; non-Windows builds currently broken (terminal-core E0624), see tech-debt-ledger`
  - 注释置于 `os:` 之前（YAML 注释合法位置）
- 注释三要素校验：
  - 拍板日期：`2026-09-05` ✓
  - 伤情出处：`terminal-core E0624` + `see tech-debt-ledger`（E0624 + 跳转指向两个事实点齐备）✓
  - 措辞：与 brief 建议原文逐字一致 ✓

### 1.2 Linux 依赖步骤删除（brief 改动一第 2 条）

- `git diff` 第二 hunk（`@@ -41,37 +40,6 @@ jobs:`）确认 `- name: Install Linux system dependencies (Tauri)` 整块 31 行删除（含 `if: runner.os == 'Linux'` 条件、所有 `apt-get install` 包、`tesseract-ocr*` 等）
- 删除后 `runner.os == 'Linux'` 分支再无触发点，留之确为死配置——改动合理

### 1.3 OpenSSL 步骤保留（brief 改动一第 3 条）

- 新文件 `ci.yml:38-41` 仍含 `- name: Setup OpenSSL (Windows, prebuilt)` + `if: runner.os == 'Windows'` + `run: ./scripts/ci/setup-openssl-windows.ps1`，逐字与 BASE 一致 ✓
- 条件保留（diff 未触碰），符合 brief「保留或去掉均可，以保持 diff 最小为准」

### 1.4 其余 job 零改动（brief 改动一第 4 条）

- `git diff` 仅产出 2 个 hunk，均位于 rust-build-check job 内部（行 29-37 matrix + 行 41-77 死步骤）
- `ci.yml` 其余 6 个 job（rust-tests-serial / kernel-api-clean / core-boundaries / rot-budget / repo-hygiene / i18n-contract）零行改动 ✓
- 总变更：8 行新增 + 33 行删除 = 净 -25 行（与 `git diff --stat` 一致：`2 files changed, 8 insertions(+), 33 deletions(-)`）

### 1.5 YAML 结构

- 全文 189 行，缩进统一为 2 空格，无 tab（自检：`grep -P "\t"` 零命中）
- `jobs:` 块包含 7 个完整子节点（rust-build-check / rust-tests-serial / kernel-api-clean / core-boundaries / rot-budget / repo-hygiene / i18n-contract），与 BASE 数量一致
- 顶层 `on` / `concurrency` / `permissions` / `jobs` 块结构完整，未被本改动波及

## 2. mod.rs 改动合规性

### 2.1 单行双删除（brief 改动二）

- `git diff 1c9ac2f..4bc3fb1 -- src/apps/cli/src/ui/question/mod.rs` 唯一 hunk：`@@ -12,4 +12,4 @@` 确认：
  - 旧：`pub use types::{QuestionAction, QuestionData, QuestionOption, QuestionPrompt};`
  - 新：`pub use types::{QuestionAction, QuestionPrompt};`
  - 仅删除 `QuestionData` 与 `QuestionOption` 两个名字 ✓（与 brief 第 26 行逐字一致）

### 2.2 types.rs 零触碰（brief 改动二附加）

- `git diff 1c9ac2f..77b69df -- src/apps/cli/src/ui/question/types.rs` 零输出 ✓

### 2.3 唯一消费方路径核查（brief 第 27 行）

- `src/apps/cli/src/ui/question/question.rs` 全文使用 `use super::types::{QuestionAction, QuestionData, QuestionPrompt};`（line 3）+ `super::types::QuestionOption { label, description }`（line 29），全数走 `super::types::` 直接路径，不经 mod.rs 重导出
- 全仓 `grep -rn 'QuestionData\|QuestionOption' src/apps/cli/src/` 仅命中 4 处，全部位于 types.rs（声明点）与 question.rs（消费者），无第三方使用重导出路径——重导出确为孤儿，可安全删除 ✓

### 2.4 验证基线（brief 第 28 行）

- 本机亲跑 `cargo check -p northhing-cli` 末行 `warning: `northhing-core` (lib) generated 16 warnings`——所有 warning 归属上游 lib
- `northhing-cli` 包本身 0 warning（与报告「0 warnings generated by `northhing-cli`」一致）
- 16 条 warning 全数位于 `src\crates\assembly\core\src\` 子路径下，与 cli's own warnings=0 互不冲突
- 旧基线 1 条 warning（question/mod.rs:15 unused imports）已消除 ✓

## 3. tech-debt-ledger 挂账（brief 改动三）

### 3.1 格式合规

- 新条目 `### P2-23: 非 Windows 平台构建失败（terminal-core E0624 private deadline method）`（line 246）位于 P2-22 与 `## Change Protocol` 之间，编号接续（无跳号）
- 字段齐全：`- **Symptom**:` / `- **Evidence**:` / `- **Proposed fix**:` / `- **Status**:`（与既有 P2-17 ~ P2-22 逐字段对照一致）
- 中文措辞 + ASCII 状态值 `deferred`（与既有 `resolved` / `active` 风格一致）

### 3.2 三要素校验（brief 第 33-35 行）

| 要素 | ledger 内容 | 来源 |
|---|---|---|
| E0624 | `error[E0624]: method deadline is private` ×2 | Symptom + Evidence 两处命中 ✓ |
| Run 号 | `CI run 33964321637` | Symptom + Evidence 两处命中 ✓ |
| 拍板日期 | `2026-09-05` | Symptom + Evidence + Proposed fix + Status 四处命中 ✓ |

### 3.3 状态字

- `Status: deferred — 按用户 2026-09-05「Windows 限定」拍板挂起；若未来恢复跨平台支持需先修此项；关联：ci.yml 矩阵同日收窄为 windows-only（W17-1）`
- 关联项指向本波 ci.yml 改动，形成闭环 ✓

### 3.4 既有条目零改动

- `git diff 1c9ac2f..77b69df -- docs/status/tech-debt-ledger.md` 仅 7 行新增（line 246-252），旧 P2-17 ~ P2-22 全部保留 ✓

## 4. 提交纪律（brief 第 48-52 行 + 项目硬规则）

- 两 commit 分域正确：
  - `77b69df ci: windows-only build matrix per user decision 2026-09-05 (W17-1)` → 2 文件（ci.yml + tech-debt-ledger.md）✓
  - `4bc3fb1 fix(cli): drop unused question re-exports, zero-warning baseline (W17-1)` → 1 文件（mod.rs）✓
- 无裹挟：mod.rs 不在 77b69df；ci.yml / tech-debt-ledger.md 不在 4bc3fb1 ✓
- 未触发 `git add -A`：每个 commit 仅含本任务明确允许的文件
- 未触碰 workflow-policy.json（metaRatchetPaths SSOT 本单不动，仅消费其 `metaRatchetPaths` 触发双 judge 车道的事实）✓

## 5. rot/hygiene 计数对照

| 指标 | 报告 | 本机 | 一致 |
|---|---|---|---|
| unwrap_production | 483/502 | 483/502 | ✓ |
| expect_production | 940/1089 | 940/1089 | ✓ |
| let_underscore | 370/388 | 370/388 | ✓ |
| unix_epoch_inline | 69/69 | 69/69 | ✓ |
| allow_dead_code | 104/109 | 104/109 | ✓ |
| dir_entries:scripts | 44/48 | 44/48 | ✓ |
| dir_entries:docs/design | 1/1 | 1/1 | ✓ |
| dir_entries:.superpowers/sdd | 59/400 | 59/400 | ✓ |
| repo-hygiene content files | 2 | 3 | ✗（见 Minor-1） |
| repo-hygiene filenames | 3831 | 3832 | ✗（见 Minor-1） |

所有 grep/dir-entry 计数与报告逐位一致；差异仅 repo-hygiene 一项（见下）。

## 6. Findings

### Critical (0)

无。

### Important (0)

无。

### Minor (2)

#### Minor-1: repo-hygiene 计数快照与本机重跑有 1 文件偏差

- 报告 `§ Verification Evidence §3`：`Repository hygiene check passed (2 content files scanned, 3831 filenames checked)`
- 本机亲跑（HEAD = 4bc3fb1，工作树 3 untracked：`packages/w16-final-review.md` 1:21:37 + `reports/w17-1-report.md` 1:43:59 + `w17-1-brief.md` 1:39:09）：`Repository hygiene check passed (3 content files scanned, 3832 filenames checked)`
- 差异成因：`check-repo-hygiene.mjs` 在 `localChangedFiles` 非空时优先扫描本地变更 + untracked（line 45-48）；本机有 3 untracked，故扫 3 个 content 文件 + 1 个额外 filename
- 时序一致性存疑：`w16-final-review.md`（1:21:37）早于 `w17-1-report.md`（1:43:59）已写入；报告若在 1:43 末跑 hygiene，理应见 3 untracked。可能解释：实现者在写完 brief（1:39）→ 跑 hygiene 拿到 2 文件（当时 `w16-final-review.md` 或未 untracked、或尚在 staging）→ 完成两 commit → 写报告（1:43）→ 报告直接复用早先 hygiene 输出，**未在 commit 后重跑**
- 影响：两边都 PASS，无实质性回归；纯文档/观测层偏差。不影响 spec 合规。
- 建议（非阻断）：报告 hygiene 行应注明 `at <timestamp>` 与当时 untracked 文件列表，避免后续 judge 复读时对不齐；CI hygiene 重跑应在 commit 后、报告生成前完成

#### Minor-2: 报告 file:line 引用与实际 @@ hunk 略有偏差

- 报告 `§ Changes Summary §1`：`File: .github/workflows/ci.yml:32-34, 43-74`
- 实际 diff hunk：
  - Hunk 1 `@@ -29,9 +29,8 @@` → 旧 29-37（matrix 段）+ 新 29-36（comment + os + windows-latest）
  - Hunk 2 `@@ -41,37 +40,6 @@` → 旧 41-77（37 行，含 3 行 OpenSSL context + 31 行 Linux step）+ 新 40-45（只剩 OpenSSL）
- 实际删除行：旧 32-33（ubuntu-latest、macos-15）+ 旧 44-74（Install Linux 步骤，31 行）；实际新增行：新 32（comment）+ 新 34（windows-latest 上移）
- 报告 `32-34` 大致覆盖了 matrix 新位置 ✓；`43-74` 与实际删除范围（44-74）相差 1 行，且未含 hunk 1 的位置
- 影响：仅 reader 寻行定位偏差；与代码事实无冲突（实指行号仍属正确文件正确区域）
- 建议（非阻断）：报告应改用 `@@ hunk 1: 旧 29-37 / 新 29-36; hunk 2: 旧 41-77 / 新 40-45` 表达，或直接写「matrix: ci.yml 旧 L30-33 / 新 L32-34; Linux step: 旧 L44-74 删除」

### Cannot verify (0)

无。

## 7. 双判决小结

- spec 合规（brief 逐条对照）：通过——3 项改动均按 brief 字面要求落地，无越界文件、无裹挟 commit
- 代码质量：ci.yml 改动机械且对称；mod.rs 删除为 root-cause fix（移除孤儿重导出，types.rs/question.rs 经独立 `grep` 确证不依赖该路径）；ledger 新条目格式与既有 P2-N 完全对齐
- 结论：**APPROVE** — 实现合格，报告可信（Minor 项为观测层偏差，不阻断 spec 合规与代码正确性）

## 8. 跨任务接口影响

- 触发 `scripts/workflow-policy.json` `metaRatchetPaths` 中 `.github/workflows/` 命中 → 双 judge 车道（已走：本 judge-m3 为 judge 2/2）+ 用户拍板（2026-09-05「Windows 限定」明文在 commit message + ci.yml 注释 + ledger P2-23 四处落点），合规
- ledger P2-23 关联项 `（W17-1）` 与本 commit `77b69df` 形成可追溯闭环
- 未来若恢复跨平台：ledger P2-23 是首要解锁项，terminal-core `deadline` private 方法需先公开或改 pub(crate) caller
