# Handoff — 后端安全修复分支收官（2026-08-01）

接手者：人或 agent。本文是 `northing` 后端审计修复一轮工作的交接文档，目标让你无需读完整会话即可继续。
事实源指针：进度账本 `.superpowers/sdd/progress.md`；终审 `.superpowers/sdd/final-review.md`；follow-up `.superpowers/sdd/tech-debt-followups.md`；模型台账 `.opencode/model-capability-notes.md`；各任务 `task-0N-{brief,report,review}.md`。

## 1. 一句话结论

审计报告 `northing-full-bug-audit-2026-07-31.md`（+ 补充报告）后端方向 9 个任务全部完成、双判决通过、整分支终审双 PASS，已 `merge --no-ff` 进 main（merge commit `8872f59`），合并后 core 全量 1134/1134 通过。本会话**未引入回归**。

## 2. 交付清单

分支 `fix/backend-debug-0731`（基线 c6096cb）13 commits，已并入 main：

| 范围 | commit | 内容 |
|---|---|---|
| T1 | 1c41dc0..e3d0e53 | Relay validated path types + 磁盘 containment 防线 (C-1/C-2 磁盘层, M-8) |
| T2 | 5971c54 | 协议认证 + create_room 原子三态 + 资源限制 (C-1/C-2 协议层, H-1/H-2) |
| T3 | ab6a91a | e2e web asset 测试 + V-1 GET traversal 动态定性封闭 |
| T4 | 88c719a | vault fail-closed + 原子写 (H-5) |
| T5 | a53711e | bot persistence 单写者事务 + fail-closed + 原子写 (H-6) |
| T6 | 64c64dc | MCP 项目配置 + MiniApp sync fail-closed (H-7/H-8) |
| T7 | 9be74ec | desktop settings 统一写入口 + 原子落盘 (H-9) |
| T8 | 1a65fc1 | LSP plugin ID 校验 + staging 原子安装 (M-9); schedule_repo_release async (M-2) |
| T9 | 6574b01 | 6 个 pre-existing 测试失败修复：GlobalConfigManager initialize 并发安全（产品 bug）+ 测试 hermetic 化 |
| SDLC | de7a72c, e3b2bef | 能力台账回填 + 台账清理 + follow-ups 入库 |
| merge | 8872f59 | main 上的 --no-ff 合并 |

## 3. 仓库当前状态

- **main**：HEAD `8872f59`。工作区有一个**未跟踪**目录 `docs/design/2026-07-22-frontend-redesign/prototypes/_review/`——前端视觉迭代产物，与本会话无关，**不要**在后续 commit 中误纳入。
- **worktree** `E:\agent-project\.worktrees\northing-backend-debug`：分支 `fix/backend-debug-0731`，HEAD `e3b2bef`，已合并、干净。合并已完成后此 worktree 与分支可清理（`git worktree remove` + `git branch -d`），但 openchamber 不能删 worktree，需手工。
- **可 prune 的旧 worktree**：`E:\agent-project\northing\.loop-worktrees\b2-god-split-v2-20260715`（1b147c3，标 prunable），与本会话无关，按需 `git worktree prune`。

## 4. 验证基线（合并后已在 main 复验 core）

| Crate | 命令 | 结果 |
|---|---|---|
| northhing-core | `cargo test -p northhing-core --features product-full --lib` | 1134/1134（含 T9 修复的 6） |
| relay-core + relay-server | `cargo test -p northhing-relay-core -p northhing-relay-server` | 49/49 |
| services-integrations | `cargo test -p northhing-services-integrations --features product-full` | 172/172 |
| desktop | `cargo test -p northhing --lib` | 98/98 |

**未覆盖**：`cargo check --workspace` 被上游 `embed-resource 3.0.11`（webdriver→tauri 链）阻断，非代码问题；CI 应跑全量。各 crate 用 focused `-p` 验证。

## 5. 后续 follow-up（5 项高优先级，已结构化）

见 `.superpowers/sdd/tech-debt-followups.md`，每项含 file:line + 根因 + 修复方向 + 验证命令，可直接派单：

1. **FU-1** [security] `save_user_config` fail-open（与已修 H-7 同漏洞类，用户级 MCP 配置）
2. **FU-2** [functional] `LspManager::uninstall_plugin` 把 plugin_id 当 language key 传给 stop_server → 卸载后 LSP 进程未停（pre-existing 功能 bug + 孤儿进程）
3. **FU-3** [concurrency] dedup 迁移挂在只读 `load_app_settings` 未持锁（窄窗口竞态）——与 FU-4 同文件，建议合并派单
4. **FU-4** [hygiene] `save_app_settings` dead wrapper（trivial 删除，消 CI 噪声）
5. **FU-5** [concurrency] `AIClientFactory::initialize_global` 同款 TOCTOU（client_factory.rs:224-263，套用 T9 的 global.rs 模式）

终审另列 18 项 tech-debt / 16 项 accept，完整清单在 `final-review.md` §5；上述 5 项是其中最高优先级子集。

## 6. 关键决策与理由（接手者需知）

- **merge 方式**：`--no-ff` 保留分支拓扑；与 main 的前端 docs 迭代**零文件重叠**，无冲突。
- **T9 根因修正**：brief 曾把 cancel 测试失败归因于 initialize TOCTOU；implementer 独立验证（main 基线单独跑仍失败 + 插桩）推翻之——真因是本机有真实 LLM 凭证使 `execution_task` 网络往返 ~0.84s > 测试 50ms cancel 窗口；修复为测试不再初始化 `AIClientFactory`，恢复测试注释原声明的 "LLM error path" 假设。initialize TOCTOU 是**独立的真产品 bug**，已一并修（double-checked locking + fallible-work-first）。
- **global facts 优先语义保留**：`MemoryDb::get_facts` 返回 global+workspace facts 是产品 feature（测试 `select_facts_respects_scope_global_first`），T9 只修测试隔离（thread-local DB 路径 seam），未改此语义。
- **relay 是 frozen-experimental surface**；GlobalConfig 单一事实源（desktop app.json 与 core 同文件，core 侧写协调记 follow-up，未在本轮做）。

## 7. 已知陷阱 / 工具坑（下一 agent 必读，省时间）

- **实施者模型额度**：`opencode/deepseek-v4-flash-free` 免费额度曾耗尽（T8 时改 ark/glm-5.2）。派发前确认额度，无备选时勿依赖 dv4f。
- **openchamber `session.send` 模型漂移**：会把模型漂回默认（如 agnes-2.5-flash）。派发一律用 `session.create` + 显式 model。`session.messages wait` 不可靠，用 `session.list` 的 tokens/status 判活。
- **裸 `cargo fmt` / `cargo fmt -p <大crate>` 会卷无关文件**，已两次污染需 revert。brief 已禁；格式手工对齐。
- **`git status` 大量 M 多为 stat 噪声**：先 `git update-index --refresh` 再 `git diff --stat` 判真改动。
- **implementer 越权 commit 风险**：T9 implementer 多 commit 了一个 SDLC 收口（de7a72c，仅台账未碰代码，无害）；brief 写"不 commit"仍可能被违反，收口时核对 `git log` 而非信任记忆。
- **审查者模型**：judge 用 `minimax-cn-coding-plan/MiniMax-M3`（任务级稳定）；终审/架构用 `ark/glm-5.2` 提供独立视角。勿用 m27 系做 judge。

## 8. 前端并行线

main 已含前端原型视觉迭代（`docs/design/2026-07-22-frontend-redesign/`，bdc885c/4b3275c/d4a93fc 等 docs commit）。**本会话未介入前端**，未派发其需求。继续前端工作请从 main 的该目录与未跟踪的 `_review/` 入手，与后端无耦合。

## 9. 接手建议

- 若继续后端：从 `tech-debt-followups.md` 的 FU-1（安全，最高优先级）或 FU-3+FU-4（同文件合并单）开新分支，沿用本会话纪律（brief→implementer→judge 双判决；不裸 fmt；日志 English-only；并发改动带测试；focused `-p` 验证）。
- 收尾清理：确认 main 已含全部 SDLC 证据后，可删后端 worktree 与分支；prune 旧 loop worktree。
