# Handoff — consult-room T7 执行收口 / E·F 开局（2026-08-23）

> 恢复：本文件 + `progress.md` 台账。前篇链：`handoff-20260822b.md`（预检+CDP 纪律，已执行完毕）→ `handoff-20260822.md`（W1/W2 战报与候裁定清单原文）。
> 接续点：**页面窗 E/F（archive / space）**——前置已定案：chrome 轻标题条+✕ / 入口 room-head 状态行（handoff-20260822 §4.1）。

## 1. 坐标与硬事实

- **worktree**：`E:\agent-project\northing\.worktrees\consult-room-build`（分支 `feat/consult-room-slint`）。
- **HEAD**：docs/chore commit（见 git log；code = `3c9fb2d` 模块窗系统 13 文件 +1258/−848，含 registry.rs 入库）。
- **flags 态**：`DIOXUS_SHELL = false`、`dioxus_shell_default_false` guard 在位（rg 实证）。flip 从未入历史，「同 commit 还原」纪律以最强形式了结。
- **分叉**：merge-base `8e43dc4`；main 领先 173 commits；分支自有 47+N commits（N=本次三连）。合并路径 = ③-e：模块窗 code commit 作单个 cherry-pick 批。
- **T7 状态：执行收口完成**。双判决证据链：代码侧 review-w2polish / review-w1racefix 双 PASS（在库）+ 视觉侧 task-t7-visual-report.md **VISUAL PASS 0C/0I/0M/2F，CAN MERGE**（m3，用户点名）。
- **合并决定**仍归用户拍板（T7 计划定义：终审双判决 PASS 才可合并，决定权在用户）；main 侧 `scripts/i18n-audit.mjs:481` 修复挂 T2-3 解冻时单独做（② 裁定在案）。

## 2. 本 session 完成（七项必办核销）

1. **CDP 取证补完 ✅**：`t7-cdp2.ps1` 修两处上轮卡点——target 差分法（首个 page=room / `#trig-mind` 两新窗 / `#trig-work` 一新窗）+ retry-classify 等 mount（上限 15s）。八张 `t7v-*` 全真渲染，编排者逐张目验。
2. **m3 视觉审查 ✅**：VISUAL PASS；戒律 10/10、双光学 6/6、三窗规格全过；2 FYI（F1 终端深底豁免 / F2 skill 卡底截切=内滚语义）。
3. **① flags 还原 + 门禁 ✅**：check exit 0（bin33 持平）/ test 226/0 / i18n:audit exit 0（1 grandfathered）。
4. **③ 系落记 ✅**：③-c 接受+注释（app.rs:508）；③-b 接受+注释（windows.rs:202）；③-a/③-d 记录关闭（见台账）。
5. **commit 三连 ✅**：code `3c9fb2d`（除 flags.rs，含 registry.rs 显式 add）→ docs（台账/handoff/m3 证据）→ chore（build-shots t1-main-{dark,light}.png `git rm --cached` 剥离，文件留盘）。
6. **② 裁定入台账 ✅**（执行点在 merge 时）。
7. **台账+handoff 收口 ✅（本篇）**；flags=false 实证 → E/F 入口开放。

## 3. E/F 开局要点

- **定义**：archive / space 两页窗化（模块窗体系延伸）。前置定案：chrome 用模块窗式轻标题条+✕（非 room 重 chrome）；入口省 room-head 状态行。
- **入口语义**：room-head 状态行承载 archive/space 唤起；archive 背景切 abyss 冷雾（visual-iter-compass §4.5）；space 多会话卡片网格（§4.4）。
- **可复用构件**：registry 插件注册（WindowPlugin 描述符）、`spawn_module_window` 通道、W2 卡五件套（w2-pin/w2-scroll/w2-foot）、theme 三窗同步链、CDP 取证脚本。
- **派发纪律不变**：implementer=coder-lc / judge=独立中档以上；GUI 取证 CDP 优先、禁光标劫持（下 §4）。
- 顺带项候排：③-b 备选改名（data-window inner→self/facility 语义化）可在 E/F 顺带做（OVERLAY_CSS 同步改）。

## 4. CDP 取证纪律（2026-08-23 定版，取代 0822b §4 补充）

- **CDP 优先，禁光标劫持**（白天用户在机）。启动：`Stop-Process -Name northhing -Force` → env `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9333` → `Start-Process ... -WindowStyle Hidden -WorkingDirectory <worktree>`（Hidden 窗也能抓帧）。
- **找窗用差分，不靠 classify**：记录启动后首个 page target = room；点 gem 后新出现的 page target 即新窗。classify（`body[data-window]` + `.w2c-sediment`/`.w2c-runtime`）只作 mount 确认，须 retry 上限 15s（mount 前全窗同形）。
- **DOM 点击**：`#trig-mind`（沉积+设施）/ `#trig-work`（身外之物）/ `#theme-toggle`（三窗同步翻转，已实证）。
- **真伪判据勘误**：room 真图 ≥30KB；模块小窗 8–12KB 即真图（稀疏+PNG 压缩）。「11KB=空白」只证上轮误截 pre-mount 帧，不是阈值。真假以**目验**为终裁。
- 脚本 `C:\WINDOWS\TEMP\opencode\t7-cdp2.ps1` + trace `t7-cdp2-trace.log`（TEMP 不持久，丢了按本节重写）。
- 仪器 FYI：work target classify-probe 曾 15s 超时但抓帧正常；work 窗 target 出现于点击后 ~16s（另两窗瞬时）——若 E/F 复跑见此两现象，已知非阻塞。

## 5. 资产指针

- 截图：`C:\WINDOWS\TEMP\opencode\t7-shots\t7v-*.png`（八张有效）；上轮 c1/d1-d4 对照保留，c2/c6/l* 作废件可忽略。
- 文档链：task-t7-visual-brief.md / task-t7-visual-report.md（视觉）；review-w2polish-20260822-report.md / review-w1racefix-20260822-report.md（代码双判决）；台账 T7 两行（开局/收口）。
- 门禁日志：`C:\WINDOWS\TEMP\opencode\t7-gate-{check,test,audit}.log`。
- E/F 设计真值：`docs/design/2026-07-22-frontend-redesign/consult-room/`（archive/space v2 HTML + PAGES-BRIEF）；戒律罗盘 visual-iter-compass_20260802.md。
- 队列（承 0822 §5 无变化）：T2-9 / T2-10 / PCS-1→PCS-2 可启动；T2-3 frozen（含 audit:481）；骨架入树与 .gitattributes 仍候用户裁定。
