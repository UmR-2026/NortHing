# Handoff — consult-room 建构期（2026-08-14b，R4-R9 视觉收口完成 / 用户裁定主窗口满意 / 改动未 commit 候裁定）

> 新 session 从本文件 + handoff-20260814（R3' 功能层/Bug B 终案）+ progress.md + task-r4-visual-report.md（R4-R9 全记录）恢复。
> 接续点：**commit 裁定（候用户）→ R3' 终审关账 → 后续页面裁定（TBD-6）**。

## 1. 坐标与硬事实

- **worktree**：`E:\agent-project\northing\.worktrees\consult-room-build`（分支 `feat/consult-room-slint`）
- **HEAD**：`5bcc285`（F1-F5 验收批：overlay 宽度链收口 / room 100vh deck-pinned / gem-mid 221px+20px 命中 / head-fold 初版）。
- **未提交改动**（git status M）：`ui_dioxus/{app,css,entry,i18n,windows}.rs` 5 件 + `progress.md` + `.opencode/model-capability-notes.md`。**flags.rs 已 restore=false（收口裁定后执行）**。
- **用户裁定（08-14 夜）：「主窗口满意」**——R4-R9 六轮视觉迭代收口。改动全部未 commit，入库方式候用户裁定（§5）。
- 本 session 全程**编排者亲跑**（派发通道 cancelled，用户指示「你自己来做」）。

## 2. R4-R9 大事记（细节全在 task-r4-visual-report.md）

1. **R4 frameless**：decorations=false + undecorated_shadow + with_menu(None)（去 dioxus 默认菜单）；─□✕ 真功能（min/max/quit=整进程退出，防孤儿窗）；room-head 拖动。**最大根因=真值 css UTF-8 BOM** → `:root` 永不匹配 → 全部 :root 变量失效；`css::truth_css()` strip BOM（TRUTH_CSS 常量保持逐字节，守卫测试不受影响）。
2. **R5 去嵌套**（用户判决）：room 铺满、containment/membrane-frame 移除、侧栏 padding:0、outer 四独立卡。
3. **R6**：竖签/见证说明元素级移除（i18n key 保留 + allow(dead_code)，audit 基线不动）；挂载→⊕ SVG 图标；主题钮→内联 SVG 日月；宝石**反对角线轴对称**（左结锚头像中轴 --gem-mid:85px 逻辑，右结 bottom 联动）。
4. **R7**：收纳钮=骑 room-head 下缘边缘条（折叠态常亮橙、随缝上移可展开，stop_propagation 防拖动）；宝石 B 方案=实心细柱（::before 贴边）+box-shadow 光晕（偏离真值「漏光非漆条」注释，用户判决，披露在案）；**完全贴边根因=UA body margin 8px**（真值无 reset，被浏览器 demo containment 掩盖）→ `body{margin:0}` 三窗真铺满。
5. **R8**：**scrim 退役**（色差根因=scrim 22% 常开压暗；偏离 block-contract §2 降级形态，用户判决，披露在案）；存在感标定（柱 3→4px、is-open .55→.72、常态 .9、hover 5px；缝条 68×4 --muted hover 84px）；主题同步实测两次翻转三窗跟随（用户 image-4 为瞬态非 bug）。
6. **R9**：窗控对齐（根因=.room-status 无带高 → padding:10px 18px 带高 34 逻辑 + .room-controls top:3px 带内居中不压虚线）；state-dot 呼吸点删除，**8s 呼吸钟移植头像渐变**（breath-avatar-fill ::before 叠层 / dark breath-avatar-glow box-shadow / light breath-avatar-ring 边框色灰↔橙；@media prefers-reduced-motion: no-preference 门控；保留真值 breath-avatar 缩放，同钟律动）。
7. **功能实测全过**：宝石切换 inner/outer ✅（用户手动确认）、□ 最大化/还原 ✅、─ 最小化 ✅、✕ 整进程干净退出 ✅、主题三窗同步 ✅、呼吸动画 ✅（r9a/r9b 两帧间隔 4s=谷/峰，头像区 102 采样点差异）、稳态 CPU 0% ✅。
8. **门禁全守**：build exit 0；warnings 基线 lib 19/bin 40 不增；`cargo test -p northhing ui_dioxus` ok（TRUTH_CSS 字节守卫过）；`pnpm run i18n:audit` 154 持平。

## 3. 关键度量与工具事实（下次 GUI 迭代必读）

- **截图物理 px ≠ CSS 逻辑 px，K≈1.44**（描边法标定：头像中轴物理 123 → 逻辑 85px，与真值回落 84 一致）。勿把截图像素当 CSS px。
- **tao hit_test 吃窗缘点击**：x<10 物理进 OS 缩放区，脚本点击丢失（曾致窗被误改宽 205px）；宝石命中区已扩 24px 补偿。**冷启动首点击可能仅激活窗口不投递，第二次生效**。
- **GetWindowRect 含 ~6px DWM 阴影带**，缘区像素有背景终端透出伪影，缘区采样须内缩。
- **构建闭环**：`Stop-Process -Name northhing -Force` → `rustup run stable-msvc cargo build -p northhing` → `Start-Process target\debug\northhing.exe -WorkingDirectory C:\WINDOWS\TEMP\dioxus-verify` → `C:\WINDOWS\TEMP\opencode\capture-all-windows.ps1 -TargetPid <pid> -Prefix <p> -OutDir C:\WINDOWS\TEMP\dioxus-verify\r4-review`（**必须传 TargetPid**；TEMP 会丢，必要时按此重建）→ 点击 `click-room.ps1 -X -Y`（room 窗相对物理 px，先截图读坐标再点）。
- dioxus 0.8 alpha rsx 支持内联 SVG（svg/circle/line/path，snake_case 属性 view_box/stroke_width）。
- 遗留未解但不影响：≤940 媒体查询是否激活的推断悖论（K≈1.44 时逻辑宽 ~776 应激活 72vh 但 room 实测满高）——经验标定绕过，未深究。

## 4. R3' 验收尾款状态更新（handoff-20260814 §5 结转）

1. **Private-sum 复测**：仍未跑。
2. **截图归档**：final-dark/light + R4-R9 全部截图（`C:\WINDOWS\TEMP\dioxus-verify\r4-review\`）均在 TEMP 未归档，**TEMP 会丢，需尽快**拷入 `docs/design/2026-07-22-frontend-redesign/consult-room/build-shots/`（不 commit）。
3. jewel 显隐/竖签/scrim 端到端：**已关闭/转化**——宝石显隐实测 ✅（用户手动确认）；竖签 R6 用户判决移除；scrim R8 用户判决退役。
4. **终审未做**：范围现为 `8428d22..HEAD` + 工作树未提交改动（R4-R9 五文件）。

## 5. 候用户裁定清单

- **commit 方式**（建议）：① 源码 5 件一 commit（fix(desktop): ui_dioxus R4-R9 视觉收口…）；② 台账/报告/handoff 等 SDD 文档另 commit 或保持 untracked（FYI 前科：SDD 文档误捎源码 commit，合并前需剥离）。progress.md 与 model-capability-notes.md 是 tracked 修改，**须入库**（reset --hard 毁台账前科 → 台账及时入库是红线）。
- 候选视觉待办（已向用户列出，未排期）：见证者消息右缘孤线 / ➤ 发送钮偏淡 / 模块「隐蔽·拉开」交互入口（真值 data-drag 设计未定）。
- locale 树 154 基线腐蚀修复（zh-CN/zh-TW .ftl + installer JSON GBK 腐蚀）——候授权，同审计修复例。
- TBD-6（archive/space 侧栏形态）——R3' 关账后重提。

## 6. 通道与模型（08-14 夜实况）

- 派发通道 cancelled（用户侧现象同 handoff-20260814 §6 dsv4f 条目：显示 cancelled 实际在执行）；本 session 全部改动编排者 K3 亲跑，未派子代理。
- 视觉验收：编排者原生视觉 read 直读 PNG；像素级采样用 PS System.Drawing 探针脚本。
- 其余通道现状见 handoff-20260814 §6（dsv4f coder/fixer 可用；minimax-m3 稳定备用；google/volcengine/ark/stepfun 死）。
