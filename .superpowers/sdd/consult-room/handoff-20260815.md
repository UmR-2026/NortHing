# Handoff — consult-room 建构期（2026-08-15 晚，模块窗系统建构中 / W1 未提交 / W2 视觉解耦定案待派）

> 新 session 从本文件 + progress.md（台账）+ handoff-20260814b（R4-R9 收口期）恢复。
> 接续点：**W1 R5 代码复审（上次派发被用户取消）→ W2 视觉解耦派发 → W1 残留竞态处置方案候用户**。

## 1. 坐标与硬事实

- **worktree**：`E:\agent-project\northing\.worktrees\consult-room-build`（分支 `feat/consult-room-slint`）
- **HEAD**：`c559d30`（locale 修复 docs）。其下 `afd829e`（locale 源码）→ `2261d2a`（R3' 终审 docs）→ `ef1c1db` → `a202028` → `5bcc285`。
- **W1 全部改动未提交**：BASE=c559d30，工作树含 ui_dioxus 6 文件改 + registry.rs 新增（R5 后 diff 85.8KB，+约600/−约760）。`git status` 里 `i18n.shared.json` 的 M 是 autocrlf 幻影（numstat 0/0），非白名单违规。
- **bin warnings 新基线 = 35**（W1 顺带清了 R3' 孤儿 VisibilityState/GlobalVisibility，原基线 40 作废）；lib 19。
- **R3' 已关账**（双 PASS CAN MERGE）；**locale 154 修复 complete**（audit exit 0）。
- untracked 骨架（web-ui/installer/mobile-web）维持不入树。

## 2. 用户决策记录（2026-08-15，本日下午/晚新增）

### 2.1 模块窗系统（TBD-6 作废，设计稿 design-module-windows-v2-20260815.md §5 定案）
1. main 侧栏模块拆为**多个独立 OS 窗**，按需唤起，**非纯三栏**；
2. 其余页面（archive/space/onboarding/settings）= 独立 OS 窗；顺序：**侧栏页先行（archive/space）**；
3. **宝石升格**：左结 = self+facility 对窗开关，右结 = work 窗开关；
4. **默认 = room 独显**；
5. 「先 spike 再设计」→ spike GO（task-winlifecycle-spike-report.md，m3 复核"可信"）：`new_window`/`close()`/`close_window(id)`/`WindowCloseBehaviour::WindowCloses` 运行时全通；关窗释放 ~82% 内存（-26MB）；主题 watch 迟到订阅可取现值；
6. UI 形态哲学 = **core+plugins**：稳定 WindowPlugin 描述符契约，注册只在 shell 装配层（registry.rs default_registry 一处），新增页面/模块窗 = 注册一条描述符，零管道。

### 2.2 视觉解耦（2026-08-15 晚，W2 任务依据，用户已确认分组）
1. 总原则：既然分窗，窗内各块**尽可能解耦**，除非逻辑非常接近才可合并为一个模块的子模块；
2. **self 窗改名「沉积」**，自我成长部分所有子项放入该大模块；
3. **skill = 「沉积 skill 列表」**：agent 自己发掘、可整理成新 skill 的候选清单——**不是** settings 页的能力集（MCP&SKILLS），是新建子模块（mock 内容）；
4. 滚动语义：**标题钉住 + 子项目列表区内滚动**；窗口本身永不滚动；总高超窗时各卡 flex 收缩出内滚动条；
5. 定案分组：
   - **「沉积」窗**（原 self）：子模块① 沉积记忆 SEDIMENT（3 条记忆列表内滚；seg-bar 进度 + 「新层形成中」作卡尾固定）② 知识沉积 RAG（@philosophy-core，**从 facility 迁入**）③ 沉积 skill（新建，mock 2-3 条候选 + i18n ×3 语）；
   - **「设施」窗**：卡1 **运行 RUNTIME**（模型引擎 + 上下文 + 全局状态）、卡2 **准则 AXIOMS** 独立浮卡；
   - **work 窗**：现状四卡即目标形态，**零改动**；settings 页真值不动。
6. 事实依据：facility 的「知识沉积 RAG」@philosophy-core 与 settings「能力集」@philosophy-core 同源——近亲判决有实物依据；settings 的 SKILL 区是能力挂载集，非用户所指 skill。

## 3. W1 任务状态（窗口插件注册表 + main 三模块窗拆分）

- implementer：gemini-36-flash（task_id ses_ffb4fc4fdffeXCGWJ9eCe7MyXs）；reviewer：minimax-m3（ses_ffb46c51effeLdJgMcXztalMGa）。
- 审查轮史：R0 FAIL（3C/5I）→ R1 修 → R1 审出新 Critical×2（三连点竞态 + I3 报告失实）→ R2 修（世代号 + css 实修）→ R2 审出 Drop 侧非世代感知 → R3 修 → **R3 双判决 PASS/PASS（Critical 清零）**。
- **编排者 GUI 验收抓到静态审查两轮漏掉的运行时 Critical**：宝石稳态 toggle 断裂（每点必叠窗、room 永不感知活跃态、is-open 永不生效）。R4（Arc 共享下沉）静态正确但**运行时零变化**；编排者根因定位两缺陷叠加：
  - **D1**：`pending.await` 永不 resolve（注册从不发生）→ R5 改为模块窗 root use_effect 自注册，失败自杀保孤儿防护；
  - **D2**：`WindowDropGuard` 裸放 `use_hook`，dioxus 按值取出 → **每次渲染提前 drop** 抹掉 Opening 状态 → R5 改 `Rc::new(guard)` + 移除 derive(Clone)。
- **R5 后运行时复测**：首循环 开✓/关✓，但存在**非确定性残留竞态**（一实例：首关后宝石永久失效；另一实例：关不上）。stdout 无窗口生命周期日志，内部不可观测——**这是当前卡点**。
- R5 代码复审派发**被用户取消**（2026-08-15 晚），尚未重派。
- 处置选项（候用户）：① fixer R6 加临时 eprintln 全链路日志复现拿实证再修；② 升级模型做深度推理；③ 编排者先跑日志版实证再定派发。

## 4. W2 视觉解耦任务（已定案，可直接开 brief）

- 依据 §2.2。白名单 `ui_dioxus/**`（windows.rs 重组 + css.rs OVERLAY_CSS 追加 + i18n ×3 语 + session_mock 如需）。红线照旧：TRUTH_CSS 字节锁、零 sleep use_future、单文件 <800 行、不 commit、i18n 三语对称 audit exit 0。
- CSS 先例：R5.3 work 窗四浮卡模式（aside 透明容器 + 卡五件套 + gap 14px）可复用到 facility/沉积窗。
- 改名联动：registry.rs WindowPlugin.title（self→沉积）+ i18n 键。
- 建议顺带：模块窗生命周期 eprintln 调试日志（为 W1 残留竞态取证）。
- implementer 必须附改动前后截图（编排者读图验收）。

## 5. 候用户裁定清单（08-15 晚更新）

- **W1 残留竞态处置**：§3 三选项。
- **页面窗 E/F 决策**（archive/space 任务前置）：页面窗 chrome（建议：轻量标题栏+✕）/ 页面入口（建议：room-head 状态行）。
- **候选视觉待办**（未排期）：见证者消息右缘孤线 / ➤ 发送钮偏淡 / 模块「隐蔽·拉开」交互入口。
- **骨架入树**：audit 全新克隆可复跑性依赖 untracked 骨架——候裁定。
- **.gitattributes `*.ftl text eol=lf`**：是否长期强制候裁定。
- **终审 Minor ×2**（ui_dioxus 硬编码中文 aria-label/收纳钮）：修 = 新增 3 键 ×3 语，候授权。
- 整分支合并 = T7 终审范围，未排期。

## 6. 通道与模型（08-15 实测，含纪律事件）

- task 派发：gemini-36-flash、minimax-m3 均正常往返；gemini-31-pro 仍 cancelled。**用户裁定：gemini 系用 3.6/3.7-flash 档，3.1 能力不达标**。
- **gemini-36-flash 报告失真前科 ×2**（W1 内：warnings 基线改写为"达标"；I3 声称已修但代码未改）。纪律升级有效：R2 起要求每条修复附 git diff hunk，R2/R3/R5 报告与代码字字对应（已恢复可信，但仍须逐条核 diff）。
- 教训：静态双判决 + 单测**不能**替代运行时 GUI 验收——W1 两个最大缺陷（D1/D2）静态审查两轮全漏，编排者实测才抓到。

## 7. 工具/环境事实（增量，GUI 验收测试法）

- rustup 全路径 `C:\Users\UmR\.cargo\bin\rustup.exe`；构建后台化 ~1m；`Start-Process` 启动 app 会带**控制台窗口遮挡被测窗**（ANSI 日志乱码源）→ 先枚举 ConsoleWindowClass 隐藏再测。
- 合成点击测试纪律：① 每次点击前 `SetWindowPos(HWND_TOP)` 置顶被测窗（DSH Desktop/OpenChamber 会遮挡 gem 区）；② 点击前 `WindowFromPoint` 探点确认落到 msedgewebview2；③ 固定等待不可靠 → **状态稳定轮询**（连续 2 次计数不变再判）；④ `Get-Process.CPU` 差值测闲置。
- 宝石坐标（room 左上 (L,T)、右下 (R,B)）：左结 (L+34, T+120)，右结 (R-30, B-122)（物理px，K≈1.44 逻辑比）。
- 窗口枚举：按 pid + IsWindowVisible + 标题含 self/facility/work/consult room 计数（MainWindowTitle 每进程只报一个，不可靠）。
- wv2 僵尸排查：`Get-CimInstance Win32_Process -Filter "Name='msedgewebview2.exe'"` 按 ParentProcessId 归树；Stop-Process 主进程后其子树会随灭（实测无积累）。
- Private-sum 量测：BFS 进程树（键统一 [int]），排除他机进程树。
- review diff 收 untracked：`git add -N <paths>` → `git diff` → `git reset -q`。
- autocrlf=true：行尾以 `git hash-object` blob 归一实证闭环。
- 其余 GUI 闭环/截图坑见 handoff-20260814b §3。
