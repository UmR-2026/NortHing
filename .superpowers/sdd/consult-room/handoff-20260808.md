# Handoff — consult-room Slint 建构期（2026-08-08 编排者端）

> 新 session 从本文件 + `.superpowers/sdd/consult-room/progress.md` + `plan.md` 恢复上下文。
> 关键事实以文件/git 为准，不以记忆为准。本文件是阶段 0 → 阶段 1 决策的接续点。

## 1. 坐标与硬事实

- **repo**：`E:\agent-project\northing`
- **worktree**：`E:\agent-project\northing\.worktrees\consult-room-build`（分支 `feat/consult-room-slint`）
- **当前 HEAD**：`628ec4b`（chore: 台账更新）
- **分支领先 main**：29 commits；合 main 需先 rebase，settings/io.rs 132 行冲突待处理
- **台账**：`<worktree>/.superpowers/sdd/consult-room/progress.md`
- **计划**：`<worktree>/.superpowers/sdd/consult-room/plan.md`
- **任务档案**（同目录）：`task-01..04-*` 已 commit 完；`task-02b/*` `task-02c-brief.md` `task-02c-report-A.md` `task-02c-B-brief.md` `task-02c-report-B.md` 阶段产物；**`task-02c-C-brief.md` `task-02c-report-C.md` 缺报告**
- **本 worktree 截图**：`docs/design/2026-07-22-frontend-redesign/consult-room/build-shots/`（≈30 张，全部不 commit）

## 2. 已落地 commits（feat/consult-room-slint 上）

| commit | 内容 |
|---|---|
| `9e3405d` | fix(desktop): 嵌入 Common-Controls v6 manifest 修复 0xC0000139 启动崩溃（`src/apps/desktop/northhing.exe.manifest` + `.rc` + `build.rs` windres 链接） |
| `229abd2` | feat(desktop): T2c 房间外壳落地（SpaceView/ChatPaneView/RoomHead/DoorbellGem；room-wrap 三层；膜线 1px；avatar-mid-y 暴露；strings.slint 补 5 键） |
| `a1fc9d9` | fix(desktop): T2c lane B deck/chrome/编码（B-1 stretch 修复 / B-2 send Path / B-3 字标去重 / B-4 mojibake） |
| `628ec4b` | chore(sdd): 台账更新 |

## 3. 工作树未 commit（重要 — 起点不干净）

```
 M src/apps/desktop/src/ui/components/RoomHead.slint     # Lane C 代码改动（待 commit）
 D docs/design/2026-07-22-frontend-redesign/consult-room/  # 25 个 v2/v3/v4 旧草稿（待清理或归档）
?? commit.txt                                            # 上一轮 commit 草稿遗留
?? task-02c-B-brief.md / task-02c-report-B.md            # 过程文件，不 commit
?? task-02c-C-brief.md                                   # 过程文件，不 commit
?? build-shots/t2c-*                                     # 截图，不 commit
```

**Lane C 改动**（来自 agent，代码正确但未提交）：
- `RoomHead.slint:21` 加 `height: root.folded ? 72px : 198px;`
- `RoomHead.slint:23` 改 `avatar-mid-y: 21/52px` → `36/67px`
- `RoomHead.slint:191` chronicle bar wrapper 加 `height: 4px;`

## 4. qwen 审查结论（2026-08-08 自我审视）

刚派 general（qwen 系）做了 5 阶段计划的批判性审查。**5 项关键事实**（已逐条文件核验）：

### 4.1 真值 HTML 缺 4 页套（严重）
当前 worktree 只有 `consult-room-main.html`。
- `consult-room-onboarding.html` / `-settings.html` / `-archive.html` / `-space.html` 不存在
- 被 `b3ea521 docs(design): 归档 consult-room 方向定稿原型` 归档删除
- 归档前的 v2 版本仍在 commit 历史里（`b3ea521` 的 parent tree），可恢复

**阶段 0 必须做**：`git checkout b3ea521 -- docs/design/2026-07-22-frontend-redesign/consult-room/` 然后**只取回 -onboarding/-settings/-archive/-space**（main.html 已在工作树）。然后立刻对这些 v2 文件做 v5 验证（与真值裁定的 4-step wizard / 760×580 五导航 / 12 地层 / 走廊门 一一对齐）。

### 4.2 视觉罗盘文件不存在（严重）
`visual-iter-compass_20260802.md` 在 worktree 里 grep 不到。
- 估计在 main 分支某 commit 中（待 `git log --all -- "**/visual-iter-compass*"` 找）
- 或在归档目录 `archive_drafts/`
- 或需要从 spike 重新生成

**阶段 0 必须做**：`git log --all --diff-filter=D --name-only -- "**/visual-iter-compass*"` 找最后一次存在的 commit，`git show <commit>:path > path` 恢复。

### 4.3 6 个死 settings 面板（不是 5 个）
```
src/apps/desktop/src/ui/views/AccessSettingsPanel.slint
src/apps/desktop/src/ui/views/GeneralSettingsPanel.slint
src/apps/desktop/src/ui/views/MCPSettingsPanel.slint
src/apps/desktop/src/ui/views/ProviderSettingsPanel.slint
src/apps/desktop/src/ui/views/SkillsSettingsPanel.slint
src/apps/desktop/src/ui/views/WorkspaceSettingsPanel.slint
```
全部 0 import。grep 验证：`grep -r "import.*SettingsPanel" src/apps/desktop/src/ui/` 应该 0 结果。

**删之前**：先 grep 确认无引用；删之后：cargo check 验零编译错误。

### 4.4 main 合冲突 29 commit（严重）
`HEAD..main` 有 settings/io.rs / settings/sync.rs / callbacks_settings/* 多文件变更（commit 253b337 等 settings v2 重做；ecbe76e settings.rs 拆模块）。**直接 merge 必有冲突**。

**阶段 0 必须做**：
```
git fetch origin main
git merge-tree $(git merge-base HEAD main) HEAD origin/main > /tmp/conflicts.txt
```
预演所有冲突点，列在阶段 1 决策包里。

### 4.5 P1 估时严重低估
`SettingsView.slint` 1483 行单窗组件 → 真 760×580 浮窗改造，3h 估时是幻想。

**真实估时**（基于 qwen 评估）：
- SettingsView 改造：8-12 h（导入重组 + 布局重写 + 状态绑定 + 回调路由）
- ArchiveView（12 地层）：12-16 h
- SpaceView（走廊门）：12-16 h
- 多窗口 Rust 端：6-8 h
- Scrim 模糊 spike：4-8 h（如果失败 12-16 h 走 Popup 兜底）
- **多窗口架构总估：50-80 h（不是 12h）**

## 5. 真正的 blocker — 多窗口架构未验证

settings 真值是「独立 Modal 浮层 + Scrim 模糊遮罩 + 透出主窗」。当前实现是单窗内 route 切换。

**Slint 1.17 能否在 Windows 开多 OS 浮窗，未验证**：
- Slint `Window` 组件存在但需 WindowHandle 持有
- Winit 后端多窗口 API（0.30+ 改过）
- backdrop-filter 高斯模糊：GPU 合成支持，RDP/VM 退化

**spike 必须先做**（不能 0.5h）：
- 写 hello-world 双窗（4-6 h）
- 验证失焦回调（2 h）
- 验证 scrim 模糊（2-4 h）
- **失败 fallback**：always-on-top 主窗内的 modal panel（不是真浮窗，但视觉接近）
- **fallback 2**：纯色 scrim（放弃模糊）

## 6. 工作树 cleanup 清单（合并前必做）

| 操作 | 对象 |
|---|---|
| `git restore` | 25 个 v2/v3/v4 draft HTML（已在 working tree 删除状态，等于已 rm 但未 commit）|
| `git rm` | 6 个死 settings 面板 |
| 删 | `commit.txt`、`task-02b-brief.md`、`task-02b-report.md`（如有）、`task-02c-*` 全部过程文件 |
| 删 | `docs/.../archive_drafts/` 内 v3/v4 HTML（已存在但从未在主分支 commit） |
| 删 | `handoff-20260805.md` / `handoff-20260806.md` / `settings-view-head.slint`（杂物） |
| 归档 | `progress.md` `plan.md` 内 T1-T6 状态行 — 留 commit history 不动 |

## 7. 决策包（**阶段 1 需要你拍板的 3 项**）

下次 session 开始时，先把这 3 个决策以问题形式问用户（用 `question` 工具），答案进入 `phase-1-decisions.md`。

### 决策 A：多窗口策略
- **A1 真 OS Window**：slint Window 组件 × N，每个独立 WindowHandle。代价：spike 4-8 h + Rust 重构 6-8 h + 测试 RDP fallback。
- **A2 Popup 模态**：单窗内 Popup 组件 + backdrop-filter 近似 scrim。代价：spike 1-2 h + Slint Popup 限制（不能跨窗 z-order、不能 OS 级置顶）。
- **A3 主窗内浮层**：不真弹窗，主窗 760×580 overlay panel。代价：0 h spike，但**违反真值**（真值要独立 OS 浮窗）。

**建议**：A2（折中）。真值说要独立浮窗，但 A1 风险太高、A3 直接违规真值。A2 用 Slint Popup + backdrop-filter 视觉 90% 接近，spike 成本可控。

### 决策 B：Settings 三方取一
- **B1 真值 760×580 五导航**：接入点（全局+subagent）/ 工作区 / MCP / 技能 / 安全。完整按你之前裁定。
- **B2 T4 三页浮层**：接入点 + MCP + 技能。少工作区 + 安全。已 commit 但功能不全。
- **B3 删 6 死面板 + 占位**：最简方案，settings 显示「即将推出」。

**建议**：B1。T4 是妥协方案（少工作区+安全 = 缺两个用户必须能改的功能）。

### 决策 C：冷暖底色
- **C1 真值 #101216（冷蓝灰）**：冷峻、未来感
- **C2 罗盘 #181612（暖深棕）**：温和、人文感
- **C3 暂不改**：保持现状，P2 推迟到 T7 前

**建议**：C1。用户在 handoff-20260806.md 终裁「真值优先」。

## 8. 阶段 0 执行脚本（self-do，不派 agent）

```powershell
# 1. Lane C 收口（10 min）
cd E:\agent-project\northing\.worktrees\consult-room-build
$env:PATH = "C:\msys64\mingw64\bin;C:\msys64\usr\bin;" + $env:PATH
git add src/apps/desktop/src/ui/components/RoomHead.slint
git status  # 确认仅 RoomHead.slint
git -c i18n.commitEncoding=utf-8 commit -F <(echo -e "fix(desktop): T2c lane C RoomHead — C-1 编年史条挂入/C-2-6 核验通过\n\nagent 已修改代码但未 commit：height 锁 72/198 + avatar-mid-y 36/67 + chronicle bar 4px。\nC-2 state pill / C-3 head-fold 常显 / C-4 亮态 opacity 0.7 / C-5 高度 / C-6 avatar-mid-y 已在 A 越界 (229abd2) 或本次 commit 内完成。\n\n已知偏离：因缺乏完整 settings.json 文件无法截 main 路由图，仅以 welcome 路由 + code review 验证。")
# 注意：commit -F 文件必须 UTF-8 带 BOM (EF BB BF) 否则 GBK 静默转码

# 2. 删 6 死 settings 面板（5 min）
git rm src/apps/desktop/src/ui/views/AccessSettingsPanel.slint \
       src/apps/desktop/src/ui/views/GeneralSettingsPanel.slint \
       src/apps/desktop/src/ui/views/MCPSettingsPanel.slint \
       src/apps/desktop/src/ui/views/ProviderSettingsPanel.slint \
       src/apps/desktop/src/ui/views/SkillsSettingsPanel.slint \
       src/apps/desktop/src/ui/views/WorkspaceSettingsPanel.slint
git commit -F <(echo -e "chore(desktop): 删 6 个死 settings 面板 (1752 行)\n\n0 import 验证。改写前先 grep 一次：\ngrep -r 'import.*SettingsPanel' src/apps/desktop/src/ui/")

# 3. 恢复 4 页真值 HTML（10 min）
git checkout b3ea521 -- docs/design/2026-07-22-frontend-redesign/consult-room/consult-room-onboarding.html \
                       docs/design/2026-07-22-frontend-redesign/consult-room/consult-room-settings.html \
                       docs/design/2026-07-22-frontend-redesign/consult-room/consult-room-archive.html \
                       docs/design/2026-07-22-frontend-redesign/consult-room/consult-room-space.html
git commit -F <(echo -e "docs(design): 恢复 consult-room 4 页 v2 真值（onboarding/settings/archive/space）\n\n来源 commit b3ea521 归档删除，恢复以重建 5 页套真值对照表。\n下一步：逐页 v5 验证（4-step wizard / 760×580 五导航 / 12 地层 / 走廊门）")

# 4. 找罗盘文件
git log --all --diff-filter=D --name-only -- "**/visual-iter-compass*" 2>&1 | head -20
git log --all --diff-filter=D --name-only -- "**/compass*"
# 找到后恢复：
# git show <commit>:path/to/visual-iter-compass.md > docs/design/2026-07-22-frontend-redesign/consult-room/visual-iter-compass_20260802.md
# git add ... && git commit ...

# 5. main 合冲突预演
git fetch origin main 2>&1 | head -5  # 注意：可能没设 remote，用 git remote -v 查
git merge-tree $(git merge-base HEAD main) HEAD main > C:\Users\UmR\AppData\Local\Temp\opencode\merge-conflicts.txt
# 把 merge-conflicts.txt 内容列在阶段 1 决策包里
```

**总阶段 0 估时：1.5-2 h**（包括 cargo check 验证 + 写决策包）。

## 9. 阶段 1 决策包格式

阶段 0 完成后，写 `.superpowers/sdd/consult-room/phase-1-decisions.md`，结构：

```markdown
# Phase 1 决策包 — 等待用户拍板

## 事实恢复
- ✅ 4 页真值 HTML 已恢复
- ✅ 罗盘文件已恢复（路径：xxx）
- ✅ 6 死 settings 面板已删
- ✅ main 合冲突清单：[粘贴 merge-tree 输出关键点]

## 待决策（3 项）
### A. 多窗口策略
### B. Settings 设计
### C. 冷暖底色

## 推荐顺序
A2 → B1 → C1（详见 handoff §7）
```

## 10. 已知坑（避免重蹈）

- **commit message GBK 陷阱**：PowerShell 默编码 GBK，写入 commit message 文件必须 UTF-8 **带 BOM** (`EF BB BF`)，且 `git -c i18n.commitEncoding=utf-8 commit -F <file>`
- **应用启动进入 welcome 路由**：要在 main 路由截图必须写 `~/.northhing/config/app.json`：
  ```json
  {"schema_version": 1, "providers": [...], "workspaces": [...], "default_model": {...}, "onboarding_completed": true}
  ```
  缺字段 → schema parse fail → banner 红条 → welcome 路由仍生效（因为 first-run 默认）
- **Cargo 编译前置**：`$env:PATH = "C:\msys64\mingw64\bin;C:\msys64\usr\bin;" + $env:PATH` 否则 gcc 0xC0000139
- **截图工具**：`powershell -NoProfile -File 'E:\agent-project\.opencode\tools\shot-window.ps1' -ProcessName northhing -OutFile <path>`（独立进程，禁同进程 Add-Type 冲突）
- **应用启动 4 秒延迟**：Rust first-run 路由切换需时，截屏前 Start-Sleep 4
- **sub-agent 失败模式**：
  - gemini-31-pro 在任务粒度过大（commit + report + 5 张截图）时常返回空 task_result
  - 失败时**保留 git status / 修改的文件**，不要重派浪费 token，直接 self-do
  - 失败报告归档：`./.superpowers/sdd/failures/<date>-<task>.md`

## 11. 关联引用

- `task-02c-c-brief.md` Lane A 详细规范（已 commit 报告：`task-02c-report-A.md`）
- `task-02c-brief.md` §3.2 Lane B 详细规范（已 commit 报告：`task-02c-report-B.md`）
- `task-02c-brief.md` §3.3 Lane C 详细规范（**待 commit 报告**：`task-02c-report-C.md`）
- `task-04-brief.md` §4 Global Constraints（所有阶段生效）
- `slint-feasibility-consult-room.md` Slint 翻译词汇
- `visual-iter-compass_20260802.md` 视觉戒律罗盘（**待恢复**）
- `plan.md` §4 任务分解
- `progress.md` 任务台账
- `AGENTS.md` / `AGENTS-CN.md` 仓库级事实
- `Cargo.toml:248` `[profile.dev] split-debuginfo = "packed"` msvc 不稳定

## 12. 下次 session 第一动作

读本 handoff + `progress.md` + `plan.md` → 看 `git log --oneline -5` 确认 HEAD → 进入**阶段 0**（5 步 self-do）。

阶段 0 完成 → 写 `phase-1-decisions.md` → 用 `question` 工具问用户 A/B/C 三个决策 → 等用户答 → 进入阶段 2-4 执行。