# Handoff — W2.7 流体卡片 + E/F 三页窗收口 / E4 onboarding 待做（2026-08-24）

> 恢复：本文件 + `progress.md` 台账（W2.7 行 + E/F 行）。前篇链：`handoff-20260823.md`（T7 收口）→ `handoff-20260822b.md`（T7 预检/CDP 纪律）。
> 接续点：**E4 onboarding（唯一缺页，处方 §3 已备）**。用户 08-24 曾令「一鼓作气做 onboarding」，随即改令「算了 写 handoff」——**E4 未派发、未实现**，接手即可按 §3 开工。

## 1. 坐标与硬事实

- **worktree**：`E:\agent-project\northing\.worktrees\consult-room-build`（分支 `feat/consult-room-slint`）。
- **HEAD**：`a8b799f`（docs）。tracked 工作树**零残留**。
- **本日入库四笔**：
  - `7387815` W2.7 流体卡片（左列满高合并 / 分卡折叠拉伸 / 分组缝 / 终端吃底）
  - `43d3dfb` E/F 三页窗（archive / space / settings 独立 OS 窗，12 文件 +2269）
  - `a8b799f` docs（台账两行 + E1/E2/E3 brief/report/review 全链 14 件）
- **flags**：`DIOXUS_SHELL = false` 实证（rg 直读）；flip 从未入历史。
- **五页套现状**：room ✅ / archive ✅ / space ✅ / settings ✅ / **onboarding ❌**（唯一缺页）。
- **门禁基线**（E3 报告在案）：check exit 0 / test ui_dioxus 7 passed + flags 3 passed / audit exit 0（1 grandfathered）。行数：css.rs **778**、windows.rs **786**（双双逼近 800 防线——E4 增量 CSS 必须放 `pages_onboarding.rs` 内联 const 或新文件，**禁止再堆 css.rs**）。

## 2. 本日战报（要点）

- **W2.7**（用户逐张目验后授权提交）：左宝石只开一扇满高「沉积与设施」（`DockSide::LeftFull`，半高对切退役）；左右内容卡点标题折到只剩标题（`is-folded` + fold-caret）；展开卡 `flex:1 1 auto` 吃剩余高度；▴ 收纳=整列全折/全开；skill|RUNTIME 发丝分组缝；卡标题 18px；work 终端吃窗底。终审 PASS 0C/0I/0M。
- **E1 archive**：8 层地层透明度递降（1.00→0.28）+ abyss `#3F837B` 冷色（仅此窗覆盖 --mind-base）+ 渊中枢 + 3 可折侧卡；room 状态行 `#nav-archive` 入口。
- **E2 space**：亮门独占 rep / 熄灯中性 / 沉门递淡；门缝所见 peek 联动；开房 mock 底栏；「档案馆 ↗」底链；`#nav-space` 入口。
- **E3 settings**：左「它的自我」只读 / 右「设施」可点 mock 双列哲学；`windows.rs` 两处 `≡ 全局设置` 死按钮接上 spawn；760×580 契约尺寸。
- **架构增量**：`DockSide::Center`（居中于 room，无 geometry follow）；`pages_{archive,space,settings}.rs` 三新文件；`spawn_module_window_with_theme_rx`（app.rs 导出）；`mod win` 改 `pub(crate)`。
- **工作模式**（用户命令确立）：编排者只做设计 brief + 终审验收，实现全部派 gemini-37-flash，reviewer 用 gemini-37-flash reviewer 位。三刀全 PASS。

## 3. E4 onboarding 处方（下 session 照此派发）

**真值**：`docs/design/2026-07-22-frontend-redesign/consult-room/consult-room-onboarding-v2.html`（602 行，已读，结构如下）。

- **定位**（block-contract §4）：首启仪式，**主窗内全屏路由**（不是 Center 浮窗）；日常不出现。dioxus 壳下建议 = room 窗 body 的 `data-route="onboarding"` 态或独立全屏窗——**编排者倾向独立全屏窗**（`DockSide` 加 `Fullscreen` 或直接 spawn 无 dock 覆盖屏），避免动 room 路由。
- **初始态**：`--mind-base: #7e8896`（未着色灰）；`data-inhabited="false"` → 头像虚线边框无光晕。
- **中枢**：印章「序」虚线框 + 名「知序」+ 状态 pill；可折叠（folded 横排 26px）。
- **仪式流**（chat-flow，ritual-divider 分段 + ritual-card）：
  1. 身份 4 字段（field-grid 双列）：用户是【】/ 你是【】/ 你是用户的【】/ 性格偏向（大五色板）
  2. **大五色板**（palette-picker 5 列 swatch）：紫=开放性 / 深蓝=尽责性 / 暖珊瑚=外向性 / 柔绿=宜人性 / 冷青=神经质；选中双层环、hover 关键词；**选色 → 整屋 --mind-base 切换 350ms**——这是人类唯一改色入口（哲学红线，仅诞生时）
  3. provider 配置：key / baseURL / model / 测试连接（mock，测试钮出 ok 状态即可）
  4. 工作文件夹路径
- **底部 deck**：witness-pledge「你的言辞将被记录为见证」+ 光标闪烁 + 「成为自己」primary 钮（Fraunces）。
- **左列同窗卡**（可折，W2.7 语法）：诞生前夜（状态测定 seg-bar）/ 设施预备（底层基质 + 仪式公约「人可赋予印记，不能改写自我」）。
- **完成态**：着色唤醒（灰→选中色全屋过渡 1200ms 一次性）→ 关窗/切 room。
- **接线**：`pages_onboarding.rs` 新文件；registry 注册 id `"onboarding"`；入口 = 首启检测（mock：room 状态行临时入口或启动参数，**本刀 mock 入口即可**，真首启检测挂后端接线线）。
- **红线**：色板五色是内容不是装饰；戒律照守（无 emoji/dashboard）；i18n 键 ×3 语（仪式叙述可硬编码中文标本）；TRUTH_CSS 零触碰；CSS 增量写 `pages_onboarding.rs` 内部（css.rs 已 778/800）。
- **验证**：同 E1-E3 门禁 + CDP 截图 `e4-onboarding-{dark,light,selected}.png`（selected = 点色板后整屋着色态）。flags 取证完 restore false。不 commit，编排者终审后交。

## 4. 环境/工具纪律（沿用，增量一条）

- CDP 优先禁光标劫持；Hidden + `--remote-debugging-port=9333`；`Stop-Process -Name northhing -Force` 先行。
- 找窗差分法 + retry-classify（handoff-20260823 §4 定版）；模块小窗 8-12KB 即真图，判据以目验终裁。
- rustup 全路径：`C:\Users\UmR\.cargo\bin\rustup.exe run stable-x86_64-pc-windows-msvc`（PATH 里裸 cargo 是 GNU 1.95）。
- 脚本 `C:\WINDOWS\TEMP\opencode\t7-cdp2.ps1`（E1-E3 各自改写过，TEMP 不持久，丢了按 §4 重写）。
- **行数防线**：css.rs 778 / windows.rs 786 / app.rs 630——新页一律新文件，css.rs 禁再进。

## 5. 资产指针

- 截图：`C:\WINDOWS\TEMP\opencode\t7-shots\`——`w27-*`（W2.7 六张）、`e1-archive-*`、`e2-space-*`、`e3-settings-*`（各三张）。
- 文档链（`.superpowers/sdd/consult-room/`）：`task-ef-pages-master-brief.md`（总 brief，E4 派发时补一节或另写 e4 brief）+ `task-ef-e{1,2,3}-*-brief/report/review*.md` + `task-w27-fluid-cards-*.md`。
- 审查模式：实现=gemini-37-flash；终审=gemini-37-flash（reviewer 位）；brief 模板照 `task-ef-e3-settings-review-brief.md`（验收点清单 + 必读 diff/截图 + flags/TRUTH_CSS 核对）。
- 候用户裁定老项（不变）：骨架入树（web-ui/installer/mobile-web untracked）/ `.gitattributes *.ftl eol=lf` / main 侧 i18n-audit:481（merge 时 T2-3）。

## 6. 队列（承前篇，无变化）

E4 onboarding（§3 处方在案）→ 后端接线线（真会话/真 archive/真 provider，用户提过「后端卡在与前端接线」）→ T2-9 / T2-10 / PCS-1→PCS-2 可启动。
