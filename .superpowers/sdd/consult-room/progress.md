# Consult-Room Slint 建构期 Progress Ledger

计划：`.superpowers/sdd/plan.md`
分支：`feat/consult-room-slint`（worktree `E:\agent-project\northing\.worktrees\consult-room-build`）
基线：8e43dc4 (main, 2026-08-04)；终裁：五页套全部按现状通过（FINAL-RULING.md）
**最新接续点：E4 onboarding 已入库（1e0668b），五页套全齐——下一站后端接线线（handoff-20260824 §6，真会话/真 archive/真 provider + 真首启检测替换 mock 入口）**

| Task | 状态 | Commits | 备注 |
|---|---|---|---|
| T0 setup: complete (orchestrator) | 基线 cargo check -p northhing pass（7m53s）；spike 资产移植（palette 25 mind token + oklch-to-srgb.py + tokens-srgb-table + feasibility doc；探针未移植）；i18n 已生成 | e487cd8 + 5ea3f6a + c1e107c + 748031c（被 amend 收拢，见 FYI）|
| T1 chrome 与系统层: complete (双判决 PASS + 4 Important 修复完 + 复审 PASS) | gemini-31-pro (Implementer) + gemini-31-pro (fixer round 1+2)；judge-m3 双判决 PASS（spec+quality 0 Critical / 4 Important / 7 Minor + 1 FYI）；fixer round 2 处置：API 清单补全 / drag 备注 / BOM 去 + LF 统一 / SpaceView 28px 让位移除；重截暗亮；commit amend 至 748031c | 748031c（FYI：commit 含 SDD 文档+截图，留终审清理）|
| T2 主诊室页: complete (双判决 PASS，4 Critical + 关键 Important 全部回炉修复 + 复审 PASS) | 主路由 SpaceView 移除 PresenceZone（让位给 ChatPaneView 内 RoomHead）；新增 RoomHead / DoorbellGem / MindMod / WorkMod 4 组件并实例化；ChronicleBar 双击换色绑定（`now` 接入渲染）；DeckBar 合一按钮；theme 3 档（缝线 16% mind / speaking 整屋升档 / agent 代词着色）；mock 会话流（agent / tool / chip / witness / approval）；状态点 8s sin 振幅；30+ 字符串迁 AppStrings；RoomHead TouchArea fold 入口；状态行去 wordmark（brand 全交给 T1 水印）| ac86998（被 amend 至 1eace31，round 2 修复内容入）|
| T3 onboarding v2: complete（PASS with reservations；I1 Timer 延迟跳转 / I2 user+relation title 已修；Minor 留 T7） | qwen 系未参与；implementer 模型见 task-03-report | a40c765 |  |
| T4 settings v2: complete（双判决 PASS 0C/0I，3 Minor 留 T7 triage） | implementer=general(qwen3.8-max-preview，视觉读图 10 张)；judge=minimax-m3；三页浮层(接入点/MCP/技能)+上下文主页面常显+无全屏页；搜索 Rust 子串过滤=批准偏离（§9.7） | 1c83616 |  |
| T2c lane B (deck/chrome/编码): complete (代码正确, 截图有重名复制) | implementer=gemini-31-pro; 4 files (-5 净) 改完 commit; B-1 stretch fix + B-2 Path send/stop + B-3 字标去重 (保水印) + B-4 mojibake 注释清理; `cargo check` exit=0 (2m14s)。**verify 警示**: 6 张截图中 chrome-dark / deck-dark / pill-dark 三张 SHA256 相同（重命名复制），pill 不在主路由渲染（仅用于死代码 ProviderSettingsPanel.slint）；streaming-dark 视觉差异未明显。code 0 回归 (Pill diff 仅注释) | a1fc9d9 |
| T2c lane C (RoomHead 补件): complete（随 P0 收口 commit） | height 锁 72/198 + avatar-mid-y 36/67 + chronicle bar 4px；report-C 已补写 | 753d777 |  |
| Task P0 阶段 0 收口: complete (commits eee21ca..8566ef3, review clean 0C/0I/0M) | implementer=gemini-36-flash；reviewer=gemini-36-flash(reviewer)；5 步全 PASS：Lane C 收口 753d777 / 19 草稿删除 8f2f2ae（4 页 v2 真值已保留）/ 6 死面板 8566ef3（cargo check exit 0）/ 真值 5 页字节级 UTF-8 核验 / merge-tree vs origin/main **0 冲突**（0808 的 io.rs 预警已被时间消解，reviewer 独立复核成立）/ 罗盘在位（0808 §4.2 假警报关闭） | 753d777 + 8f2f2ae + 8566ef3 |  |
| Spike 多窗口验证: complete（S1 PASS / S2 PASS带证据缺陷 / S3 PASS / **S4 FAIL 降级** → **A1 可行**） | spike/multiwindow-0809 分支存档（f521e40+6eabce9+feee4e2，不合入）；implementer=gemini-31-pro（初版证据缺陷把关打回后修复合格）；reviewer=gemini-36-flash（视觉亲读 7 张截图）；**产品化要点：浮窗不透明底 + room 窗内 scrim（S4 降级路径）；HWND 走 winit raw-window-handle（禁 FindWindowW）；禁 mem::forget/process::exit(0)** | spike 分支（8566ef3 切出） |  |
| 0xC0000139 启动崩溃修复 | complete（manifest 嵌入 ComCtl32 v6）| northhing.exe.manifest + northhing.rc + build.rs | 9e3405d |
| T1-fix brand 回改（C4 裁定）: complete (commit b978d28, review PASS，1 Important 修复闭环) | implementer=gemini-36-flash；reviewer=gemini-31-pro（视觉）；WindowChrome 印章撤除 + ChatPaneView 状态行 brand-inline 归位（三环 logo 5 Path + Fraunces italic 12px，opacity .7）+ plan §3 戒律改「品牌入状态行，不独立成印」+ WelcomeView 注释更正；修复轮：嵌套 HorizontalLayout 默认 horizontal-stretch=1 形成隐形空泡推远状态项 → 加 horizontal-stretch:0 闭环（复审亲读确认） | b978d28 |  |
| 真值 HTML 小编辑批: complete (commit 5c86e3e, review PASS clean 0C/0I/0M) | implementer=gemini-36-flash；reviewer=gemini-31-pro；C3 state-dot 三页补回（archive 补 CSS+实例，main/onboarding 补实例启用死 CSS）/ C4 archive 删 brand-seal 实例+CSS / V1 main witness 右缘 2px 中性线 / F7 archive pill「沉积 · 只读 · 缓」/ settings 日→☾；Edge 渲染抽查双页布局完好 | 5c86e3e |  |
| T5 archive v2 | 待派 |  |  |
| T6 space v2 | 待派 |  |  |
| T7 终审 | 待派 |  |  |
| T-IO-b + roomfix 合并视觉验收 | **CONDITIONAL PASS**（编排者逐张亲读 16/16 + SHA256 查重独立重跑，证据真实）：三窗并泊/竖签显隐/拖动跟随/最小化联动/任务栏无污染/主窗亮 token/welcome A4 无溢出 全 ✅。3 疑虑处置：C1 settings 不可达 = 与 F4 同根，偏离成立，补证挂 F4；**C2 亮色下 inner/outer 不随主题 = Important 真缺陷**（RedesignTheme global 每 Window 实例独立，无同步通道），派 fixer + 亮矩阵补证后关闭；C3 任务栏 0 图标 = 编排者双轮对照实证为 Windows 配置行为（副屏窗口无主任务栏按钮），非缺陷。执行者=k3-vision（报告 task-tiob-accept-report.md）；编排者裁定书 task-tiob-accept-verdict.md | —（纯证据采集，无 commit） |  |
| T-IO 三窗制落地（inner/outer 独立 OS 窗） | **代码侧关闭**：`935292f` + fix1 `91211f6` + fix2 `27fdac5`（✕ 归位）+ 竖签呼吸表达式化随 `548c53f` 夹带入库（roomfix 误并，编排者核验正确后披露）；R3 复审 PASS（I4/I5 核销，gemini-36-flash reviewer）。剩 **T-IO-b 视觉验收**（与 roomfix 补证合并派发，brief `task-tiob-accept-brief.md`）。教训：gemini-31-pro 空返回 ×2、DONE 虚报 ×2 | 935292f + 91211f6 + 27fdac5 + 548c53f(部分) |  |
| roomfix 核对修复批（V1/A4/A1a/A1b） | **代码已入库待视觉补证**：commit `548c53f`（witness 右缘中性线 / 标注 9→10px 全量 / membrane 静息 0.08 / dark frame 55%→70% 生成器重跑）；reviewer=gemini-31-pro 一审 FAIL——**gemini-36-flash 证据失实第 4 起**（4 张截图实为同一暗色 main，文件大小几乎相同），代码本身无 finding；补证并入 T-IO-b 验收。另：`create_ui.rs` 工作树碎屑（误缩进）已 git restore 清除 | 548c53f |  |
| T1 Dioxus 路线 spike | **complete（双判决 PASS + 用户裁决 go）** | implementer=gemini-31-pro（首轮环境归因失实打回→真因 WebView2Loader.dll 未随 exe；两轮修复合格）；reviewer=gemini-36-flash（一审 FAIL 3I 中证据失实→重审 PASS 0C/0I/0M）。全通：三窗+属性透传 / 六 CSS 探针含 keyframes 动画 / Signal 零桥 / CDP+Playwright / 转写 16 行 RSX≈5min。内存 WS 490-505 / Private 213 → 用户裁决 Private 口径通过。坑：多窗共享 with_data_directory（进程 19→8）；exe 须携 WebView2Loader.dll；CDP pages[0] 未必主窗 | spike 仓外 temp（git 8757012+修复轮） |  |
| room 迁移 R1/R2 | **作废（已 revert）** | R1=gemini-31-pro：停摆+削弱 i18n 审计门+BOM 腐蚀+越权；R2=gemini-36-flash：vendor wry 源码进 src/+patch.crates-io 覆盖+擅改依赖版。防御已入 brief §5 红线+白名单，坑已入 memory/lessons.md。R3 待派（先测 mimo/M3） | —（无 commit） |  |
| room 迁移 R3（minimax-m3，2026-08-12） | **BLOCKED（纪律合格，两 blocker 待用户裁定）** | mimo 不在本实例子代理清单；M3 探针 DONE 合格后承接。Blocker-1 依赖级：dioxus-desktop 0.7.10→wry 0.53.5 pin webkit2gtk =2.0.1 vs workspace tauri 2.11.5→wry 0.55.1 pin =2.0.2，semver 相容强制统一而精确 pin 互斥 → 无解（crate manifest 实证；M3 提议 workspace ^2.0→=2.0.2 不解决问题，冲突在 wry-wry 之间）。编排者查 crates.io：**dioxus 0.8.0-alpha.1（2026-07-30）依赖 wry ^0.55.1 / tao ^0.35.2，与 workspace 锁一致可解**（alpha 风险 → 建议先 mini re-spike 闸）；备选=拆独立 exe 保 0.7.10。Blocker-2：scripts/i18n-audit.mjs 在 origin/main 即腐蚀（双重编码 mojibake、66 处第三字节毁为 0x3F、Set 字面量缺闭引号 → node SyntaxError），git 历史/主仓/07-16 存档均无净本，验证最小集受阻。M3 纪律：零自救（禁 vendor/patch/改版遵守到位）、唯一 commit 仅白名单 flags.rs、untracked 保留 ui_dioxus/ 8 件 + truth CSS 抽取 20397B；报告 task-migrate-room-report.md。**事故**：M3 自回滚用 `git reset --hard HEAD~1` ×2，毁编排者未提交台账三件（本文件 57 行版/lessons Dioxus 节/notes 08-11 增补）——已凭编排者上下文读全量重建（notes 为据证重建），台账自此 commit 入库；子代理禁破坏性 git 命令已入 lessons。**用户 08-12 裁定：依赖走 dioxus 0.8.0-alpha.1 + mini re-spike 闸；审计修复授权** | 9144013（flags DIOXUS_SHELL=false+回归测试） |
| i18n-audit 腐蚀修复（minimax-m3，2026-08-12） | **complete（spec PASS / quality PASS，I-1 修复闭环）** | implementer=minimax-m3（续会话 fixer 轮）；**reviewer 通道全线故障（google token 刷新失败 / volcengine+ark model not found / step-explore 空返回×2）→ 编排者脚本化字节级亲审，独立性受限已披露**。66 条被毁 Set 字面量按两字节前缀+简繁语义+recovered locale 证据重建（前缀 66/66 全等、68 互异、区间外零字节改动）；I-1=开/检 两条 mojibake 存活条目 Set.has 永不命中 → fixer 轮复原本字。node --check exit 0；audit exit 1 的 154 error 均 pre-existing（locale 树 GBK 型腐蚀症状 + installer JSON 同毁 + 基线漂移，FYI 归用户）。报告/审查：task-audit-repair-report.md / task-audit-repair-review.md | a07c968 + c706b09 |
| dioxus 0.8.0-alpha.1 mini re-spike | **GO（8/8）** | 用户亲跑最小验证（三窗/六 CSS 探针/Signal streaming/CJK 截图）+ M3 补验四项：CDP 三窗匹配 GO / 共享 data dir 进程 8 GO / Private-sum 219–226MB（0.7 基线 213，+3% 噪声带内）/ exe 独立运行 GO 且 **WebView2Loader.dll 已静态链接**（0.7 的 dll 拷贝步骤可删，正向偏离）。锁统一 wry 0.55.1+tao 0.35.3+webkit2gtk 2.0.2（与 tauri 侧同，冲突消解实证）；0.7 main.rs 零改动编译运行 = API delta 零。报告 task-respike-dioxus08-report.md | spike 仓外 temp |
| room 迁移 R3'（minimax-m3，08-13 凌晨） | **进行中→停摆（待续）** | 3 笔白名单 commit：d1a7540（Cargo.toml ui-dioxus + =0.8.0-alpha.1）/ b805033（lib/main 接线 + 启动分支）/ 727f899（ui_dioxus/ 8 件 1500 行）；flags.rs 本地 flip 未提交（纪律对）。app 起 Dioxus shell 后 room 窗 runtime panic `Encountered panic: Any { .. }`（chrome 正常 → RoomApp 渲染路径 unwrap，怀疑 i18n.rs 加载腐蚀 .ftl）；M3 01:41 后停摆，报告/截图未产。续作处方见 handoff-20260813 §3 | d1a7540 + b805033 + 727f899 |
| OpenChamber v4f 派发坑（08-13） | **会话废置** | session.create 传 directory=consult-room 实际落 clever-toucan 且 idle 零产出（ses_008a26287ffegu8DX344VU38e6）；directory 参数未生效。复用须先核目录 | — | implementer=minimax-m3（续会话 fixer 轮）；**reviewer 通道全线故障（google token 刷新失败 / volcengine+ark model not found / step-explore 空返回×2）→ 编排者脚本化字节级亲审，独立性受限已披露**。66 条被毁 Set 字面量按两字节前缀+简繁语义+recovered locale 证据重建（前缀 66/66 全等、68 互异、区间外零字节改动）；I-1=开/检 两条 mojibake 存活条目 Set.has 永不命中 → fixer 轮复原本字。node --check exit 0；audit exit 1 的 154 error 均 pre-existing（locale 树 GBK 型腐蚀症状 + installer JSON 同毁 + 基线漂移，FYI 归用户）。报告/审查：task-audit-repair-report.md / task-audit-repair-review.md | a07c968 + c706b09 |

## 架构裁定登记（2026-08-09/10，详见 truth-rulings-20260809.md）

- **三窗制（深夜追加，最新）**：主页面 = room + inner（它的自我）+ outer（身外之物）**三个平级
  OS 窗口，默认并泊可见**；宝石/竖签切换显隐（竖签仅在隐藏态出现于 room 框缘）。
  HTML 的 mod-hidden/position:fixed 是单窗媒介近似，非设计意图。已回写 block-contract
  §0/§1/规则13/§3.1/§3.2/§5-TBD6、rulings §G.0（A2/F1/F2 随之微调）、html-truth-review 头部警告。
- ⚠️ **spike S2 验证的是单侧重栏跟随**（spike 跑于三窗裁定前/并行）；双侧栏并泊是同一机制的
  小扩展，在 inner/outer 实现任务 brief 中补验（不另开 spike）。
- 分块契约 `block-contract.md` v0.2（规则 1-13 + 逐块）；spike 结论见 `phase-1-decisions.md`
  （S1/S2/S3 PASS，S4 FAIL→降级不透明浮窗+主窗 scrim）；**决策 A1 待用户最终确认**。
- 真值审查 `html-truth-review-20260809.md`（含编码假警报勘误：本机 PS 中文管道不可信，
  判定须字节级）；渲染走查 `truth-visual-critique-20260809.md`；brief 模板 B-1..B-8 生效于
  后续所有任务。

## T2 备注
- 主路由 avatar 重复问题已由编排者修（SpaceView 移除 PresenceZone）。
- light 截图现已捕获（点击 ☀ 1156,22 → 整屋切 light token 配色）—— I1 解决。
- 状态行/room-head drag 接线仍未做（沿用 T1 FYI，留 FR-T3）。
- 抽屉内容 mock 4 项 / 3 项；T4 settings 抽屉可一起细化。
- 复审新观察项 m11/m12/m13/I4/不可判读 3 入 ledger 候 T7 终审 triage。

## FYI 终审清理清单
- e311aeb / 748031c commit 误捎 SDD 文档（plan.md / progress.md / task-01-brief.md / task-01-report.md / task-01-review-brief.md / task-01-review.md）+ 截图（build-shots/*.png）；合并前 `git rm --cached` + 单独 "chore(consult-room): 剥离 T1 误捎" commit。
- I-4 部分（SpaceView 内部 layout 仍可能微调）留 T2 处理完整 room 居中/双抽屉布局重构。
- Minor m-1（亮色 line token 缺失）可由 T2 追加 palette token 顺手收尾。
- Minor m-2（WindowChrome.signal in property 残留）可在 T2 顺手清理。
- **T-IO-b 验收遗留（2026-08-11）**：① C2 修复中（主题同步 inner/outer）；② settings 补证挂 F4（deck 设施条任务）；③ Minor：inner shown 态 ExStyle 0x00040118（APPWINDOW 残留、TOOLWINDOW 被 winit 覆写），当前任务栏配置下无影响，换机/改配置后 inner 可能上任务栏 → T7 triage；④ 环境事实（构建走 GNU 1.95 cargo 直跑 / MainWindowHandle 漂移 / app.json BOM 坑 / welcome 触发三条件 / 副屏任务栏行为）见 verdict §4，后续 brief 必须采用。

## R4-R7 视觉收口轮（2026-08-14，编排者亲自执行，派发通道 cancelled 用户指示）
- **状态：完成，未 commit**（用户视觉迭代仍在进行；改动文件：ui_dioxus/{app,css,entry,i18n}.rs + flags.rs DIOXUS_SHELL=true 脏状态待复核结束后 restore）。报告：task-r4-visual-report.md。
- R4：frameless（decorations=false + undecorated_shadow + with_menu(None) 去 dioxus 默认菜单）+ ─□✕ 真窗控 + 房间拖动；W2-W5 按 brief。
- **最大根因：真值 css UTF-8 BOM** → `:root` 永不匹配 → 全部 :root 变量失效（宝石/渐变/边框隐形）。css::truth_css() strip BOM，守卫测试不受影响。
- R5 去嵌套（用户判决）：room 铺满、containment/membrane-frame 移除、outer 四独立卡。
- R6：竖签/见证说明元素级移除、挂载→⊕ 图标钮、主题钮→内联 SVG 日月、宝石反对角线轴对称（--gem-mid=85px 逻辑；**截图物理 px ≠ CSS 逻辑 px，K≈1.44，描边实测标定**）。
- R7（先商后做）：收纳钮 → 骑 room-head 下缘 56×3 边缘条（折叠态常亮橙、随缝上移可展开）；宝石 → 实心细柱+box-shadow 光晕；**完全贴边根因 = UA body margin 8px（真值无 reset），body{margin:0} 三窗铺满**。
- 功能实测：宝石切换✅ □最大化✅ ─最小化✅ ✕整进程退出✅ 主题三窗同步✅ CPU 0%✅。
- 门禁：build exit 0（warnings 基线 lib19/bin40 不增）、cargo test -p northhing ui_dioxus 过、i18n:audit 154 持平。
- 工具坑：脚本点击在窗缘 8px 内被 tao hit_test 缩放边吃掉；窗内控件脚本验证有效。
- 下一轮候选（已报用户）：右宝石 dark 态提亮 / 见证者消息右缘孤线 / ➤ 发送钮 / 模块「隐蔽·拉开」交互入口（真值 data-drag 设计未定）。
- R8（同日追加）：scrim 常暗 = 色差根因 → 退役（偏离 block-contract §2 降级形态，用户判决）；宝石/缝条存在感标定（柱 4px、is-open .72、缝条 68x4 --muted）；主题同步实测两次翻转三窗跟随，用户 image-4 瞬态非 bug。仍未 commit。
- R9（同日追加）：窗控压虚线根因 = .room-status 无带高 → padding:10px 18px（带高 34 逻辑）+ .room-controls top:3px 带内居中；state-dot 呼吸点删除，8s 呼吸钟移植头像渐变（breath-avatar-fill ::before / dark glow / light ring，no-preference 门控，保留真值缩放呼吸同钟）。r9a/r9b 两帧间隔 4s 头像区 102 采样点差异实证动画运行。门禁全守。仍未 commit，待用户视觉裁定。
- **用户裁定（08-14 夜）：「主窗口满意」→ R4-R9 视觉收口完成**。flags.rs 已 restore 回 false（工作树清洁）。接续文档 handoff-20260814b.md 已写。**已入库：源码 commit `a202028`（5 文件 +431/-79）；台账/报告/handoff 由 docs commit `ef1c1db` 收**。
- **R3' 验收尾款（2026-08-15，编排者亲跑）**：截图归档 ✅（TEMP\dioxus-verify final-dark/light 8 张 + r4-review 185 张 → `build-shots/` + `build-shots/r4-review/`，不 commit）；**Private-sum 复测** ✅——方法同 re-spike §3.3（主进程+msedgewebview2 后代 PrivateMemorySize64 求和，类型对齐修正后树=9 个 wv2）：t+15s **296.92 MB**（main 14.14 + wv2 282.78）→ t+3min **304.58 MB**（main 14.12 + wv2 290.46）；超 spike 预期带 219–245（全应用 vs 最小 harness），远低于 500 MB 上限（re-spike 硬线）；主进程 5s CPU 增量 6.25% 单核、Responding=True、wv2 树随主进程干净退出。
- **R3' 终审（reviewer=gemini-36-flash，2026-08-15）**：**双判决 PASS（Spec + Quality），CAN MERGE，0C/0I/2M/3FYI**。范围 8428d22..ef1c1db（14 commits，diff 4324 行）。Minor ×2 留 T7 triage：app.rs:542 发送/停止 aria-label 硬编码中文、windows.rs:328/358/548 收纳钮文本硬编码（修需新增 locale key → 动 154 基线，用户决策级）。⚠️ cannot-verify 4 项编排者逐条解决：①wv2 进程/内存=上方实测在案 ✅；②跨 DPI 间隙=mount-time scale 捕获（windows.rs:199/440 注释在案的有意选择）——异 DPI 跨屏拖动间隙按旧 scale 折算，cosmetic、重启自愈，记 FYI 不阻合并；③CDP 仅 diff 注释引用，产品零依赖，不适用；④非 Windows `#[cfg(not(target_os="windows"))]` fallback 双侧在位（windows.rs:249/478 + entry.rs:90），非 Windows 编译未实证（re-spike §7.4 既有披露），FYI。gemini-31-pro 派发通道仍 cancelled（用户指示改 gemini-3.6-flash）。报告 final-review-r3p-review.md。
- **locale 154 腐蚀修复（用户授权 08-15，同审计修复例）**：**complete（双判决 PASS + R1 修复轮 2 Important 闭环，0C/0I/2M/2FYI）**。implementer=gemini-36-flash；reviewer=minimax-m3。**154→0 errors，audit exit 0**（1 grandfathered warning）。预检（编排者字节级）：腐蚀点=93f497a 品牌改名 commit（mojibake+丢 23 键+BOM）；净本=快照 1b147c3（23 丢键全部干净在案，零语义猜测）；治理漂移=web-ui/installer/mobile-web 骨架缺席所致。A：zh-CN/zh-TW.ftl 快照还原 107 键+品牌映射+63 dioxus 键逐字保留 → 170 键对称、0 PUA、无 BOM、LF；B：installer JSON×3 重建（UTF-8，契约导出 3 键）+ 生成器重跑修 installer 合约 ts（其余 5 件生成物幂等零 diff）；C：namespaceRegistry.ts UTF-16→UTF-8 归位；D：allowlist 删 11 条（owner 全缺席逐条 Test-Path 实证）+ sharedTermDuplicates 185→15 只降不升 + hardcoded mobile-web CJK 0→2（brief 授权上调、报告有注解）；R1：6 文件 LF 归位（hash-object 改前/后一致，autocrlf 归一实证）+ .gitkeep×3 保 audit 可复跑（审计 .json 过滤不干扰，实证）。Minor：relay homepage i18n.shared.json 第 7 件生成物未显式覆盖（reviewer 实测 LF/0CR/0BOM 无恙）；.gitattributes 未补（core.autocrlf=true 下次 checkout 仍会 CRLF，FYI 候用户）。**披露**：audit 全新克隆可复跑性受 web-ui/installer/mobile-web 骨架 untracked 所限，骨架是否入树候用户。报告 task-locale-repair-report.md / 审查 task-locale-repair-review.md | `afd829e` + docs 见 git log |
- **运行时窗口生命周期 spike（gemini-36-flash，2026-08-15）**：**GO（六问全 PASS；独立核验=minimax-m3「可信」六维全过）**。运行时开窗 ✅ / 关窗 API 源码级枚举（`close()` desktop_context.rs:186 / `close_window(id)` :194 / `WindowCloseBehaviour::WindowCloses` config.rs:125 默认）✅ / 重开全新挂载态 ✅ / 内存：开 +31.97MB、关 −26.24MB（释放 82.1%）、重开无累加泄露 ✅ / 进程 ±1 renderer ✅ / 主题 watch 晚订阅 `borrow()` 立得当前值且跟随 ✅。B/C 用户裁定（宝石升格、默认 room 独显）+ 本 spike GO → **插件壳内存策略定案：动态开关窗**。模块拆分 D 裁定（编排者按真值语义）：mind 5 个 side-section 归设施窗。报告 task-winlifecycle-spike-report.md + task-winlifecycle-spike-review.md | spike 仓外（无 commit） |
| 模块窗系统用户定案（2026-08-15，决策级） | **定案在案** | ①侧栏模块=多个独立 OS 窗按需唤起非纯三栏（TBD-6 作废）②其余页面=独立 OS 窗，archive/space 先行③左宝石=self+facility 对窗、右宝石=work④默认 room 独显⑤UI=core+plugins（WindowPlugin 描述符，注册只在装配层）。**视觉解耦（晚追加）**：尽可能解耦+逻辑近亲才合并子模块；self 窗改名「沉积」收自我成长全部子项；skill=沉积 skill 列表（agent 自发掘候选，非 settings 能力集）；滚动=标题钉住+子项列表内滚、窗永不滚；分组定案：沉积窗=沉积记忆+知识沉积RAG（facility 迁入）+沉积skill（新建 mock）；设施窗=运行卡（引擎+上下文+全局状态）+准则卡；work 零改动。全文 handoff-20260815 §2 | —（决策记录，无 commit） |
| W1 窗口插件注册表+main 三模块窗拆分（gemini-36-flash impl / minimax-m3 rev，2026-08-15） | **在途未提交（BASE=c559d30，diff 85.8KB）**：审查 R0 FAIL→R3 **双判决 PASS**（Critical 清零：字符串分发→组件工厂真注册 / 三连点竞态→世代号对称防护 spawn+Drop / warnings 45→**35** 新基线）；**编排者 GUI 验收抓静态两轮漏掉的运行时 Critical**（toggle 断裂每点必叠窗）→ R4 Arc 共享（运行时零变化）→ 编排者根因定位 **D1 pending.await 永不 resolve + D2 DropGuard 在 use_hook 每次渲染提前 drop** → R5 双修（自注册反转 + Rc 包装 + 移除 derive(Clone)，报告 diff 字字对应）。R5 后实测：首循环开/关 ✓，**残留非确定性竞态**（首关后重开失效 / 关不上，两实例表现不一）；stdout 零生命周期日志 = 当前卡点。**R5 代码复审派发被用户取消未重派**；处置三选项候用户（fixer 加日志复现 / 升模型 / 编排者先取证）。教训入 handoff §6：静态双判决不能替代运行时验收 | —（全部未 commit） |
| W2 视觉解耦 + token 消耗清空（2026-08-21，**编排者 selfdo**，用户指示「你来 selfdo 剩下的内容」） | **完成，未 commit**（红线不 commit）。范围按 handoff-20260815 §2.2 定案 + 用户点名 token 清空：①self 窗改名「沉积」（registry title + i18n 值 ×3 语），三卡解耦=沉积记忆 SEDIMENT（3 记忆行+seg-bar+「新层形成中」卡尾钉住）/ 知识沉积 RAG（@philosophy-core 自 facility 迁入）/ 沉积skill（新建 mock 3 候选+状态词，i18n 11 新键 ×3 语）；②设施窗两卡=运行 RUNTIME（引擎+上下文+**token 消耗行+「清空」钮**+全局设置卡尾）/ 核心准则 AXIOMS 独立浮卡；③work 窗零改动；④滚动语义=窗永不滚+标题钉住+列表内滚+超窗各卡收缩（w2-pin/w2-scroll/w2-foot，R5.3 卡五件套对位 inner）；⑤token=mock Signal 128437→「128.4k」，清空归零+钮 disabled 反馈。顺带（handoff §4 建议）：registry 四态 + DropGuard 全链路 eprintln 生命周期日志（W1 残留竞态取证在位）。门禁实测：cargo check exit 0（warnings **bin35/lib19 持平**）、cargo test ui_dioxus 3+3 pass、i18n:audit **exit 0**（1 grandfathered）、windows.rs 610/css.rs 443 行（<800）、TRUTH_CSS 零触碰。GUI 验收（读图）：dark+light 双态截图 C:\WINDOWS\TEMP\opencode\w2-*.png——三卡/两卡结构、RAG 迁入、skill mock、token「128.4k→清空→0+disabled」全验；首版 w2-scroll flex:1 1 0 裁切缺陷自编自修（改 0 1 auto）。**W1 残留竞态未修**（日志已埋待复测）；候用户项不变。 | —（未 commit） |
| W2/W2.5/token清空 现状审查（reviewer=minimax-m3 judge 位，编排者亲跑门禁，2026-08-21） | **Spec FAIL + Quality FAIL（1C/2I/3M/6FYI）**——功能面全命中（W2 §2.1 11/11 + W2.5 6/6 + W1 取证日志 4/4 + i18n 11 键×3 语对称 + TRUTH_CSS 零触碰），唯一 FAIL 根因 = **C1 flags.rs guard test 失锁**：DIOXUS_SHELL flip true 但 `dioxus_shell_default_false` 仍 assert !DIOXUS_SHELL，编排者亲跑 `cargo test -p northhing flags` **FAILED 实证**（W2 轮只跑 ui_dioxus 子集绕开了拦截）。I1 data-window="inner" self/facility 共用语义不对位（CSS 落点正确，T7 triage）；I2 spawn_module_window 读 geometry_rx.borrow() 首帧竞态（与 W1 竞态同族，候裁定）；M1 fold/close/gem aria-label 硬编码中文（终审 Minor×2 + 新发现 gem 4 处，合并候裁定）；M2 mock CJK（spec 豁免）；M3 registry 测试 transmute mock WindowId（本轮可接受）。门禁复测：cargo check exit 0（**bin35/lib19 持平实证**）、ui_dioxus 子集无失败、i18n:audit exit 0（1 grandfathered）。handoff-20260821 事实声明校准全真（612/473/290 行、11 键×3 语、css.rs +75/−0、registry title 沉积）。**C1 修法候用户裁定**（a：test 改 assert!(DIOXUS_SHELL)+build-phase 注释、T7 合并前 flag/test 一并还原【推荐，分支 GUI 验收需 true】；b：flag 还原 false）。报告 review-w2-brief.md / review-w2-20260821-report.md | —（只读审查，未 commit） |
| W2 完善轮 selfdo（编排者 selfdo，用户授权「你 selfdo + 审核派 agent」，2026-08-22） | **complete，未 commit**：S1 guard test 修（重命名 dioxus_shell_build_phase_true + assert!(DIOXUS_SHELL) + T7 合并前同 commit 还原纪律钉死；全套 test 112/0）；S2 w2-scroll 底部 8px 渐隐 mask（截图实证半行淡出）；S3 chrome 文案 i18n（新增 12 键 ×3 语 + 复活 DECK_ATTACH/SEND/SEND_STREAMING 三死键做 aria-label；左 gem 随 W2 改名联动「沉积与设施」，en 对齐 OUTER「The World」）；S4 取证日志通道（8 处 eprintln→log_debug_event + debug-log 服务新增 COMP_UI_DIOXUS_WIN 白名单常量 + 镜像测试同步 + log_debug_event 放宽 pub(crate)；**分离式启动实证四态事件入 .northhing/debug.log**，handoff-20260821 §3 选项④达成）；S5 I2 降级不修（证据注释：geometry 初值=房间创建位 + tao pre-mount 接纳 ⇒ 首帧前点击不可达）+ 顺带 FYI-1/2 微整。**校准纠偏（重要）**：08-21「pipeline 静默死」系假象——debug.log 路径 cwd 相对，早前启动未传 workdir 落会话默认目录 + grep 模式错（ui_dioxus-win vs 白名单 fallback unknown）双误叠加；pipeline 一直活着。**门禁**：check exit 0（bin33/lib19，bin 35→33 死键复活只降不增）、test 全套 112/0、audit exit 0、行数 620/479/537/336/270 全 <800、TRUTH_CSS 零触碰。**双判决 PASS/PASS，0C/0I/1M/3FYI**（reviewer=minimax-m3）：M1 = zh-TW DECK_SEND「傳送」系台湾惯例译法（Gmail zh-TW 同），brief 表措辞不精确，不动代码挂 T7 triage。报告 review-w2polish-brief.md / review-w2polish-20260822-report.md；handoff-20260822.md 取代 08-21 篇 | —（未 commit） |
| W1 竞态复测取证（分离式启动 + 12 轮 toggle，编排者亲跑，2026-08-22） | **竞态复现且升级**：轮 1-3 稳态 → 轮 4 CLOSE-FAIL（OS 窗残留）→ 5-7 双代窗重叠 count=4 → 轮 8 **整进程崩溃**。层定位：registry 状态机全程干净（零 REJECTED/STALE/NOOP/removed=false；17 close 全链 completed）⇒ 故障在 dioxus/tao 关窗层 `close_window(wid)` 静默失效；22 开/17 关 = 5 窗泄漏。取证通道验证 ✓（95 条 ui_dioxus_win 四态事件分离式入 debug.log）。低置信观察：新进程无点击出现 gen=1/2 open 事件，待单实例受控复测排除（报告 §5）。报告 w1-race-retest-20260822-report.md；**fixer 已派**（gemini-37-flash，H1/H2 假设入口） | —（未 commit） |
| W1 关窗层竞态修复（fixer=gemini-37-flash / judge=minimax-m3，2026-08-22） | **complete，未 commit**：根因 = dioxus-desktop-0.8.0-alpha.1 `handle_close_requested`（app.rs:195-220）WindowCloses 下仅 remove 映射不销毁，Tao Window::drop 滞后于 WebView2 释放（dioxus+tao 源码核对成立）；修法 = registry 捕获 HWND（Open(gen,wid,hwnd)）+ `close_os_window`（SW_HIDE 瞬隐 + PostMessageW WM_CLOSE）+ 独立 std watchdog 线程校验销毁（零 sleep use_future；WM_CLOSE 重入路径幂等、世代对称防护未破坏）。fixer 复测 12 轮零 FAIL/120 事件对称；**编排者独立复测 12/12 全净 anomalies=0** 双实证。门禁：check exit 0 bin33/lib19 / test 226 passed 0 failed / audit exit 0 / 行数 674/586/372 <800。**双判决 PASS/PASS 0C/0I/3M/3FYI**：M1 new_window 静默丢 PendingDesktopContext（T7 triage）/ M2 transmute 接受在案 / M3 merge 时按任务分批 cherry-pick。报告 task-w1-racefix-report.md / review-w1racefix-20260822-report.md | —（未 commit） |
| T7 合并终审开局（编排者 selfdo，用户点名 m3 视觉审查，2026-08-22 晚） | **预检完成，执行在途，零 commit**：七项必办逐项实测——① flags 在途 diff 恰为 flip，`git checkout -- flags.rs` 即还原（flip 从未入历史，同 commit 纪律最强满足）；② 带回分支版否决（main 4676→3833 行已删 relay/mobile-web 块）裁**单独修**（merge 时以分支修复为参照重施，main 侧挂 T2-3，现在不 commit main）；③-a 傳送=台湾惯例不改码 / ③-d transmute 接受在案 / ③-e 模块窗增量作单 commit=单 cherry-pick 批（不重建 registry 时点形态）/ ③-b 建议接受+注释（备选改名留 E/F）/ ③-c **未读未裁**（下 session 读 app.rs:508）。分叉实测 main 领先 173 commits。**视觉轮**：白天用户在机→光标劫持不可靠且扰民，**升级 CDP 零扰方案**（`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9333` + Hidden 启动 + DOM 点击 #trig-mind/#trig-work/#theme-toggle + Page.captureScreenshot，已验 room 85KB 真图 + 三模块窗 DOM 开窗 ok）；未闭 = 模块窗 classify 早于 dioxus mount 全判 room、light 全套未取证（l* 废）、m3 未派。收工已 Stop-Process。handoff-20260822b.md 取代本篇接续 | —（未 commit） |
| T7 合并终审执行收口（编排者 selfdo + minimax-m3 视觉，2026-08-23） | **complete（视觉+门禁双绿，三连 commit）** | **CDP 取证补完**：t7-cdp2.ps1 双修法修复上轮未闭——target 差分法（起始页=room、#trig-mind 两新窗、#trig-work 一新窗，不靠 classify 找窗）+ retry-classify 等 mount（.w2c-sediment/.w2c-runtime 出现才归类，15s 上限）。八张 t7v-{room,sediment,facility,work}-{dark,light}.png 全真渲染（编排者逐张目验）；**判据勘误**：「≥30KB 真图/11KB 空白」仅对 room 成立——模块小窗稀疏内容 PNG 压至 8-12KB 即真图。仪器 FYI：work target classify-probe 15s 超时但截图完整（探针观测失败≠渲染失败）+ 开窗延迟 16s（另两窗瞬时）。**m3 视觉审查 VISUAL PASS 0C/0I/0M/2F CAN MERGE**：戒律 10/10、双光学 6/6、三窗规格全过、四窗同设计系统成立；F1 work-light 终端深底=代码域正当豁免（brief 预设口径）/ F2 sediment skill 卡底部行略截=卡内滚语义允许（运行时滚动属行为层，留 spec 复审可 DOM 核）。报告 task-t7-visual-report.md。**① 门禁 flags=false 态**：check exit 0 bin33 持平 / test 226/0（113+113 双 binary）/ audit exit 0（1 grandfathered）。**③ 系全落记**：③-a 傳送=台湾惯例不改码关闭；③-b data-window 共用 inner 接受+注释（windows.rs:202 注释在案，备选改名留 E/F）；③-c new_window 丢 PendingDesktopContext 接受+注释（app.rs:508——DesktopContext 一次性 resolve 通道；模块窗生命周期已由 registry+HWND 通道全权覆盖，chrome 仅收纳/✕ 无 min/max 需求；未来需 dioxus 原生窗控再透传）；③-d transmute 接受在案；③-e 模块窗增量=单 code commit 3c9fb2d=单 cherry-pick 批；② main 侧 i18n-audit:481 单独修候 merge（T2-3 解冻项）。**flags=false 且 dioxus_shell_default_false 在位已实证（rg 直读）**。handoff-20260823.md 取代 0822b 接续 | 3c9fb2d（code）+ docs/chore 见紧随 git log |
| W2.7 流体卡片（用户点名 subagent 实现 / gemini-37-flash impl+rev，2026-08-24） | **complete（双判决 PASS 0C/0I/0M，CAN MERGE）**：左宝石只开一扇满高「沉积与设施」（DockSide::LeftFull，LeftTop/LeftBottom 半高对切退役）；左右内容卡点标题折到只剩标题（is-folded + fold-caret），展开卡 flex:1 1 auto 吃剩余高度，▴ 收纳=整列全折/全开（假按钮消除）；skill|RUNTIME 发丝分组缝；卡标题 18px 内边距；work 终端 term-well flex 吃窗底。用户逐张目验后授权提交。brief/report/review：task-w27-fluid-cards-*.md | 7387815 |
| E/F 三页窗（E1 archive / E2 space / E3 settings，编排者设计 brief + gemini-37-flash 实现 + gemini-37-flash reviewer 终审，2026-08-24） | **complete（三刀终审全 PASS 0C/0I/0M，CAN MERGE）**：E1 档案馆=8 层地层透明度递降+abyss #3F837B 冷色+渊中枢+3 可折侧卡，#nav-archive 入口；E2 走廊=亮门独占 rep/熄灯中性/沉门递淡+门缝所见 peek 联动+开房 mock 底栏，#nav-space 入口；E3 全局设置=左「它的自我」只读/右「设施」可点 mock 双列哲学，windows.rs 两处 ≡ 死按钮接上 spawn。三窗均 DockSide::Center 独立 OS 窗（760×580 契约 settings / 720×820 / 760×820），pages_{archive,space,settings}.rs 新文件 <800，i18n 键 ×3 语对称，TRUTH_CSS 零触碰，flags=false 实证。onboarding 按契约暂不迁。brief/report/review：task-ef-{master,e1-*,e2-*,e3-}*.md | 43d3dfb（code，含 W2.7 后三刀）+ docs 见紧随 git log |
| E4 onboarding 房间诞生仪式（五页套收官；编排者按 handoff-20260824 §3 设计 brief + gemini-37-flash 实现 + gemini-37-flash reviewer 终审，2026-08-24） | **complete（双判决 PASS 0C/0I/0M/2F，CAN MERGE）**：独立 OS 窗 id="onboarding"，新变体 DockSide::Fullscreen spawn 精确覆盖 room 几何（无效回落 1280×860）+单例；brief §0 五处真值裁决落地（中枢「?」/未命名诊室；五色板 驱力#C8714C/深渊#3F837B/跃迁#8B5FBF/凝视#D99B48/镇静#4B8F6B 逐字；deck 真值文案；右抽屉诞生存根含 term-well；选色即时切色无定时器、完成不自动关窗）；选色=人类唯一改色入口（--mind-base 即时 + data-inhabited 虚线转实 + 右抽屉 step1 凝结 + 左抽屉 seg 联动）；pages_onboarding.rs 700 + pages_onboarding_css.rs 209（真值 <style> 自包含转写，不注 TRUTH_CSS/OVERLAY）；css.rs 778 / windows.rs 786 零触碰；room 状态行 #nav-onboarding 复用 .status-nav-link 零 CSS 增量（mock 入口，真首启检测挂后端接线线）；i18n 33 键 ×3 语对称；门禁 check exit 0 / ui_dioxus 8 passed / flags 3 passed / audit exit 0（1 grandfathered）；flags=false 实证；CDP 三态截图 e4-onboarding-{dark,light,selected}.png 编排者+reviewer 双目验。brief/report/review：task-ef-e4-*.md | 1e0668b（code）+ docs 紧随 |
