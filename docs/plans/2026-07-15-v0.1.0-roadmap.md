# 0.1.0 human-usable roadmap (2026-07-15)

> 状态: planning | Mavis authored | UNTRACKED (Mavis 不 commit handoff doc)
> Source: `E:/agent-project/northing/` from `northing-impl-b0-smoke` snapshot 2026-07-12 (HEAD b254db80, v0.1.0 tag at 2813b36)
> Initial commit: `1b147c3` "snapshot: from northing-impl-b0-smoke 2026-07-12 (HEAD b254db80)"
> Goal: **0.1.0 human-usable** (CLI/Web/Desktop/Mobile work, installable, documented, P0 bug free)
> Dispatch: LongCat-2.0 直连 (M3 0 burn), OpenCode `task` tool 派 subagent, Mavis plan/spec/review/monitor

## 0. Context

- **事故**: `E:/agent-project/northing/` 2026-07-15 12h 间隔内源文件丢失（无 .git reflog, recycle bin empty）
- **Recovery**: `E:/agent-project/northing-impl-b0-smoke/` 3 天前 snapshot 完整 source (3011 files, 88 MB), subagent verified identity + 5 god files + cargo check 0 error
- **Baseline 数字** (2026-07-12 snapshot):
  - cargo check -p northhing-cli: **PASS, 0 error, 1517 warnings** (mostly unused_import)
  - dead_code warnings: 1382 (历史 refactor 残留, 不是 P0)
  - god files >750: 0 (lib+tests) / 5 (src/apps/cli/src) — B2 target
  - 156 pre-existing cargo fmt 改动 (per R72 守则 deferred, 2026-07-12 时还在 deferred list)
  - 1 pre-existing turn_batch B-3 test fail
- **现有 v0.1.0 tag (2813b36)**: A6 commit 时打的, 是 tech preview 状态, 非 human-usable release

## 1. Critical path (必须串行, 阻塞 0.1.0 release)

| # | Phase | 估时 (LongCat) | 阻塞什么 |
|---|---|---|---|
| C1 | Source recovery + baseline verify | ✅ DONE | (已完成) |
| C2 | B2 god split (5 files) | 2-3h | 后续 cargo fmt + test (因为 god file 改动影响 cross-file) |
| C3 | Pre-existing 156 cargo fmt cleanup | 1-2h | (依赖 C2 改完后整编译) |
| C4 | Pre-existing 1 turn_batch B-3 test fix | 30-60min | 0.1.0 release gate (test pass required) |
| C5 | Full workspace verify (`cargo test --workspace`) | 1-2h | 0.1.0 release gate |
| C6 | HANDOFF.md / README / docs 更新 | 30-60min | 0.1.0 release gate |
| C7 | 0.1.0 human-usable tag + 文档 (this is the "real" 0.1.0 release) | 15 min | release |

Critical path 总估: **5-9h** LongCat work + 1-2h Mavis plan/review (本 session 1h 余, 后续 sessions 继续)

## 2. Parallel streams (可与 critical path 同步, 加速 release)

| # | Stream | 估时 (LongCat) | 并行约束 | 输出 |
|---|---|---|---|---|
| A | Dead code 1382 → categorize + fix or `#[allow]` | 2-3h | 独立, B2 不影响 | warning count -1300+ |
| B | HANDOFF.md / README.md / AGENTS.md 现状 audit + update 计划 | 1h | 独立 | doc-update spec |
| C | CodeGraph reindex (B2 改完后再做, 现在做会被 B2 invalidate) | 30min | 必须在 C2 之后 | fresh codegraph |
| D | Cross-platform build prep (Dockerfile / CI config audit) | 1-2h | 独立, 但 need source stable | ci-config plan |
| E | Installer verification (Windows NSIS / Mac DMG / Linux deb) | 1-2h | 独立 | installer test report |
| F | E2E smoke test infrastructure (CLI basic flow / Web login / Desktop launch) | 2-3h | 独立 | e2e tests |
| G | API rustdoc review + cleanup | 1h | 独立 | doc comments fix |
| H | 15 unused-import warnings fix (in src/apps/cli/src/, B2 scope 内) | 30min | 跟 C2 同步 | 0 unused import |
| I | docs/ audit (cleanup outdated handoffs) | 1h | 独立 | docs cleaned |
| J | .agents/ reference 4-domain mirror refresh | 30min | 跟 C2 同步 | fresh mirror |

Streams A, B, D, E, F, G, I 跟 critical path 完全独立, 可全程 parallel
Streams C, H, J 必须在 C2 之后 (B2 改完 code 才有意义)
总估: **10-15h** parallel LongCat work, 调度 3-5 个 subagent 并发

## 3. Per-phase work spec

### Phase C2: B2 god split (5 cycle, 1 by 1 sequential per memory lesson)

5 cycle 同 B2 plan 之前 (memory 有 B2 plan doc structure, 这次 重新写 5 cycle addenda):
- C2.1 question.rs (803) → 5 files (mod/types/state/handle/render)
- C2.2 input.rs (802) → 5 files
- C2.3 main.rs (797) → 5 files (main + cli/{mod,init,run,exit})
- C2.4 acp_cli.rs (766) → 5 files (acp/... + 2-line shim)
- C2.5 command_palette.rs (754) → 6 files

**Per cycle Done criterion (CRITICAL fix vs 上次)**:
- `cargo check -p northhing-cli` 输出必含 `Finished` 关键字 (NOT just exit code 0)
- subagent 必试编译 target crate 单独 (not just "no new errors")
- 4 HARD import fix (per QClaw review) 写进 cycle spec, subagent 直接 fix, 不 Mavis direct edit
- Mavis spot-check 必看 "Finished" 实际输出, 不只信 subagent 报告

**Per cycle 8 self-check** (R75 batch 1 baseline + codegraph impact):
1. git status --short (D original + ?? new sub-module files)
2. wc -l sum ≈ original LOC (no over-extraction)
3. git diff --stat +N -M
4. cargo check -p northhing-cli output 含 "Finished" (CRITICAL, 不只是 0 errors)
5. grep -c "fn " 保留所有 fn signatures
6. git diff --stat scope (only intended files)
7. npx @colbymchenry/codegraph impact <entry-point> | head -20 (public API 保留)
8. grep -r "use crate::..." cross-module refs intact

**Mavis take-over safety net**: 2 consecutive subagent fail → M3 take-over 5-15min/file (per spawn-subagent skill)

### Phase C3: cargo fmt cleanup (pre-existing 156 files)

- subagent dispatch: `pnpm run fmt:rs` (per AGENTS.md)
- Verify: `git diff --stat` (should be +N -N equal, pure whitespace)
- Per R72 守则: NOT apply to other unrelated files
- 估时: 15-30min for subagent (mechanical), 5min Mavis verify
- Note: snapshot 7/12 时 R72 deferral 还在, 这次 0.1.0 release 必须 clean 0 改动

### Phase C4: pre-existing turn_batch B-3 test fix

- subagent 找 test (per HANDOFF mention: 1 pre-existing turn_batch B-3 fail, 926/927 pass)
- Fix: understand the test expectation + actual behavior, 1-line fix typical
- 估时: 30-60min subagent (debugging), 5min Mavis verify
- This is the only P0 test fix before 0.1.0

### Phase C5: full workspace verify

- 多个 `cargo test` 命令 per crate
- `cargo test --workspace` (full)
- `cargo test -p northhing-cli` (CLI)
- `cargo test -p northhing-core` (core)
- `cargo test -p northhing-desktop` (desktop)
- `cargo test -p northhing-agent-runtime` (runtime)
- Mavis spot-check 5-10 tests per crate
- 估时: 1-2h subagent parallel, 15min Mavis review

### Phase C6: HANDOFF / README / docs update

- 写新 section "v0.1.0 human-usable release"
- Update §0 TL;DR with: 0 god files (B2 done), 0 cargo fmt, 0 dead_code, 0 P0 test fail
- Tag note: re-tag v0.1.0 at new HEAD (per HANDOFF §"Tag drift note" pattern)
- 估时: 30-60min Mavis (大 file), 5min subagent verify

### Phase C7: 0.1.0 human-usable tag

- `git tag -d v0.1.0` (remove existing tech-preview tag)
- `git tag -a v0.1.0 -m "v0.1.0 human-usable: god split done, fmt clean, tests pass, docs current"`
- Update HANDOFF.md "Last verified" line + §0 tag pointer
- NOT push to GitHub (per user "0.1.0 人类可以使用后再上传")

## 4. Stream dispatch strategy (parallel LongCat)

**3-5 concurrent subagents max** (subagent overhead + monitoring cost)

**Wave 1 (now, parallel with C2)**:
- Stream A: dead_code audit (subagent 1)
- Stream B: doc audit (subagent 2)
- Stream F: e2e infra design (subagent 3)
- Stream G: rustdoc review (subagent 4)

**Wave 2 (after C2 done)**:
- Stream C: codegraph reindex (subagent 1, 1-shot)
- Stream H: unused-import cleanup (subagent 1)
- Stream J: reference mirror refresh (subagent 1)

**Wave 3 (after C3 + C4)**:
- Stream D: cross-platform CI prep (subagent 1)
- Stream E: installer test (subagent 1, Mac need user)

**Wave 4 (after all above)**:
- Stream I: docs audit (subagent 1)

## 5. Resource budget

| | LongCat | M3 |
|---|---|---|
| Critical path (C2-C7) | 5-9h | 1-2h (this session 1h 余, 后续 sessions) |
| Parallel streams | 10-15h | 0.5-1h (spot-check only) |
| **Total to 0.1.0 release** | **15-24h** | **1.5-3h Mavis** |

User token 池够 (用户 5h 限额 + 大量 token), Mavis 5h 配额 会跨多个 session.

## 6. Success criteria (Definition of Done for 0.1.0 human-usable)

**Hard requirements** (P0, release gate):
- [ ] `cargo check -p northhing-cli` exit 0 + output `Finished`
- [ ] `cargo test --workspace` 0 fail
- [ ] B2 5 god files split (no file >500 lines in src/apps/cli/src/)
- [ ] `cargo fmt --check` exit 0 (no pending fmt)
- [ ] HANDOFF.md §0 reflects v0.1.0 human-usable state
- [ ] README.md has install + run instructions
- [ ] Git tag v0.1.0 (re-tag) at clean HEAD
- [ ] No new 0 P0 bugs found in E2E smoke test

**Soft requirements** (P1, can defer to 0.1.1):
- Dead code 1382 → ≤ 500 (90%+ reduction, accept 500 warnings)
- Warnings 1517 → ≤ 200 (87% reduction)
- 5 unused-import warnings → 0
- CodeGraph fresh index
- Cross-platform CI green (Windows verified, Mac/Linux manual)

**Out of 0.1.0 scope** (P2+):
- 100% documentation coverage
- Full E2E test suite (only smoke)
- All public APIs documented
- Cross-platform installer tested
- 1382 dead code → 0 (only reduce to ≤500)
- Mobile-web Polish

## 7. Risk mitigation

| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| Subagent 30-min cap (per R72) | Med | 1 cycle 需重做 | Mavis M3 take-over 5-15min/file as safety net |
| 156 cargo fmt + 5 unused import 改时产生新 error | Low | 阻 C5 | subagent 必跑 cargo check 验证, Mavis spot-check |
| Turn_batch B-3 test fix 误改行为 | Med | 阻 C5 | 1-line fix + 跑全 test 套 verify regression |
| Cross-platform build 不一致 (Windows works, Mac 失败) | High | 0.1.0 release 阻 | Mac/Linux 等 0.1.1, Windows-only release OK |
| Installer fail on at least one platform | Med | 0.1.0 release 阻 | Per-platform release 备选 |
| Disk accident 重演 (source 再丢) | Low | catastrophic | (用户已 defer GitHub) M3 docs/ memory capture, worktree daily backup |

## 8. Cron + memory discipline

**Mandatory cron self** (per 2026-07-14 lesson):
- After each phase dispatch: `mavis cron self <name> --every 5m --prompt "B2 C2 progress: <phase>"`
- After phase done: `mavis cron delete <id>` (避免 noise)

**Memory updates** (per Mavis 不 commit handoff):
- After each phase: write phase-report to memory
- Per cycle: write cycle addendum to docs/plans/cycle-addenda/ (UNTRACKED)
- 0.1.0 release: write milestone entry to MEMORY.md

## 9. Next step (this session)

**This session (剩 ~1h M3 配额)**:
1. **Plan doc** (this file) ✅ done
2. **C2.1 cycle addendum** (write spec for question.rs split, ~10min Mavis)
3. **Dispatch C2.1 subagent** (~15min LongCat, then 5min Mavis verify with cargo check Finished)
4. **Decision point**: if C2.1 clean, dispatch C2.2-2.5 in next sessions; if any fail, M3 take-over

**Next sessions (跨 M3 5h 配额)**:
- C2.2-2.5 (4 cycle, 4 subagent dispatches, ~3h)
- C3 (cargo fmt, 1 dispatch ~30min)
- C4 (turn_batch fix, 1 dispatch ~1h)
- C5 (workspace test verify, 1-2 dispatch ~2h)
- C6 (docs, Mavis direct write)
- C7 (0.1.0 tag, Mavis direct)

**Parallel streams** (any time):
- Stream A: dead_code audit (1 dispatch ~2h)
- Stream B: doc audit (1 dispatch ~1h)
- Stream F: e2e design (1 dispatch ~2h)

## 10. References

- HANDOFF.md (snapshot HEAD b254db80)
- 1b147c3 initial commit
- B2 plan (lost, recover from memory)
- spawn-subagent skill SKILL.md
- QClaw review pattern (lost, recover from memory)
- mavis-runtime.md (B2 lessons)
