version: 1
plan:
  name: round41b-4-task-resume-2026-07-06
  max_concurrency: 4
  max_consecutive_failures: 2
  max_cycles: 2
  auto_accept: false
  auto_reject_retries: 1
  verifier_config:
    default_verifiers:
      - verifier
    audit_sample_rate: 0
    strict_mode: false
tasks:
  - id: impl-r41b-retry-tool-pipeline-fix-tests
    title: >-
      R41b retry: tool_pipeline.rs test module fix (12 compile errors from missing imports)
      (northhing-core)
    prompt: >
      # R41b RETRY: tool_pipeline.rs split — TEST MODULE FIX REQUIRED

      ## Context (R41b cycle 1 FAIL)
      Branch impl/r41b-core-tool-pipeline-split already has commit 8a66088b with the split done.
      But the **test module is broken with 12 compile errors** because producer only ran
      `cargo check --lib`, not `cargo test --no-run`. Missing imports in test module:
      - `ToolCall`
      - `ToolExecutionContext`
      - `ToolTask`
      - `ToolExecutionOptions`
      - `NortHingError`
      - `FrameworkToolResult`

      ## Target
      Worktree: E:/agent-project/northing-impl-r41b-core-tool-pipeline
      Branch: impl/r41b-core-tool-pipeline-split (from main 4841f3bd, has commit 8a66088b)
      Package: `northhing-core` (use `-p northhing-core --features product-full --lib`)

      ## Required fix
      1. cd into the worktree
      2. Find the test module (likely in pipeline_state.rs or pipeline_execution.rs)
      3. Add the missing imports at the top of the test module
      4. Run `cargo test -p northhing-core --no-run --features product-full --lib tool_pipeline` to verify test module compiles
      5. If still errors, iterate on missing imports
      6. Once `cargo test --no-run` passes, run `cargo check --workspace --message-format=short` to ensure no regression
      7. Commit the fix: `fix(core): R41b retry add missing test module imports`
      8. Run `cargo fmt -p northhing-core -- --check` to ensure fmt clean
      9. Run encoding verify on all staged .rs

      ## Iron rules (R5-R40 lessons)
      - 0 NEW unwrap/panic/unreachable
      - 0 NEW CRLF/BOM (use Edit tool, not Out-File/Set-Content)
      - 0 Cargo.lock drift
      - 0 cross-crate module reference
      - 0 NEW cargo fmt diff
      - 1 atomic commit
      - mod.rs ≤ 600, sibling ≤ 800

      ## Pre-commit encoding verify
      ```
      py -c "import os, subprocess; result = subprocess.run(['git','diff','--name-only','--cached'], capture_output=True, text=True, cwd=r'E:\agent-project\northing-impl-r41b-core-tool-pipeline'); [print(f'BAD: {f}' if (open(os.path.join(r'E:\agent-project\northing-impl-r41b-core-tool-pipeline', f), 'rb').read()[:3] == b'\\xef\\xbb\\xbf' or b'\\r\\n' in open(os.path.join(r'E:\agent-project\northing-impl-r41b-core-tool-pipeline', f), 'rb').read()) else f'OK: {f}') for f in result.stdout.strip().split('\\n') if f.endswith('.rs')]"
      ```

      ## Self-report (single line)
      `commit=xxx check=pass(no errors) test_compile=pass(fix imports) fmt=pass encoding=pass mod=N max=N baseline_warnings=baseline current_warnings=baseline`

      ## Mavis take-over signal
      - 30 min no commit → Mavis take-over

      ## 工作环境
      PowerShell 5.1, no bash chain. Write .rs with Edit tool. line count: `[System.IO.File]::ReadAllLines().Count`.
    assigned_to: coder
    role: produce
    verified_by: verifier
    depends_on: []
    gates: []
    max_retries: 0
    timeout_ms: 3600000
    hang_alert_after_ms: 1800000
    model: minimax/MiniMax-M2.7-highspeed
  - id: impl-r41e-core-tool-context-runtime-split
    title: >-
      R41e: agentic/tools/tool_context_runtime.rs 1447 -> facade + N sibling
      (northhing-core)
    prompt: >
      # R41e: tool_context_runtime.rs god-split

      ## Target:
      src/crates/assembly/core/src/agentic/tools/tool_context_runtime.rs (1447 lines)
      Worktree: E:/agent-project/northing-impl-r41e-core-tool-context-runtime (branch
      impl/r41e-core-tool-context-runtime-split, from main 4841f3bd)
      Package: `northhing-core` (use `-p northhing-core --features product-full --lib`)

      ## CRITICAL iron rules (R40-R41 lessons)
      1. **0 `_lost_methods.rs`** — every method assigned to sub-domain
      2. **0 `part1.rs`/`part2.rs` mechanical naming** — use sub-domain names
      3. mod.rs ≤ 600: only `mod sibling;` + `pub use super::*;` re-export + struct definition
      4. Each sibling ≤ 800
      5. Fields `pub(super)` for cross-sibling struct field access
      6. Wildcard re-export: `pub use super::*;`
      7. Write .rs with Edit tool, NOT PowerShell Out-File/Set-Content
      8. **MUST run `cargo test -p <crate> --no-run --features product-full --lib <module>`** to verify test module compiles (R41b lesson)
      9. pre-commit encoding verify

      ## Sub-domain split
      Suggested: `context_init.rs` / `context_state.rs` / `context_invoke.rs` / `context_types.rs` / `context_helpers.rs`
      `git grep 'tool_context_runtime\|ToolContextRuntime\|ToolUseContext' -- ':!<file>'`

      ## PREFLIGHT
      ```
      $p = "src/crates/assembly/core/src/agentic/tools/tool_context_runtime.rs"
      $wc = [System.IO.File]::ReadAllLines((Resolve-Path $p).Path).Count
      Write-Host "tool_context_runtime.rs canonical wc-l = $wc (expected 1447)"
      $env:Path = "C:\msys64\mingw64\bin;" + $env:Path
      cargo check -p northhing-core --features product-full --lib --message-format=short 2>&1 | Tee-Object baseline.log
      $baselineErrors = (Select-String -Path baseline.log -Pattern "error\[" | Measure-Object).Count
      Write-Host "BASELINE_ERRORS=$baselineErrors (expected 0)"
      ```

      ## Implementation + Commit + Self-report
      Commit: `refactor(core): R41e split tool_context_runtime.rs 1447 -> facade + N sibling`
      Self-report: `commit=xxx check=pass test_compile=pass(fmt::check) fmt=pass consumers=pass encoding=pass mod=N max=N sub-domain=Y 0_part1N=1 0_lost_methods=1 new_warnings=0 baseline=N current=N`

      ## Mavis take-over signal
      30 min no commit → Mavis take-over

      ## 工作环境
      PowerShell 5.1. Edit tool for .rs. `[System.IO.File]::ReadAllLines().Count` for line count.
    assigned_to: coder
    role: produce
    verified_by: verifier
    depends_on: []
    gates: []
    max_retries: 0
    timeout_ms: 5400000
    hang_alert_after_ms: 3600000
    model: minimax/MiniMax-M2.7-highspeed
  - id: impl-r41f-core-code-review-tool-split
    title: >-
      R41f: agentic/tools/implementations/code_review_tool.rs 1422 -> facade + N sibling
      (northhing-core)
    prompt: >
      # R41f: code_review_tool.rs god-split

      ## Target:
      src/crates/assembly/core/src/agentic/tools/implementations/code_review_tool.rs (1422 lines)
      Worktree: E:/agent-project/northing-impl-r41f-core-code-review-tool (branch
      impl/r41f-core-code-review-tool-split, from main 4841f3bd)
      Package: `northhing-core` (use `-p northhing-core --features product-full --lib`)

      ## CRITICAL iron rules
      Same as R41e, plus:
      **MUST run `cargo test -p <crate> --no-run --features product-full --lib <module>`** to verify test module compiles (R41b lesson)

      ## Sub-domain split
      Suggested: `review_parse.rs` / `review_diff.rs` / `review_lint.rs` / `review_report.rs` / `review_types.rs`
      `git grep 'code_review_tool\|CodeReviewTool' -- ':!<file>'`

      ## PREFLIGHT + Implementation + Commit + Self-report
      Same as R41e. Commit: `refactor(core): R41f split code_review_tool.rs 1422 -> facade + N sibling`
    assigned_to: coder
    role: produce
    verified_by: verifier
    depends_on: []
    gates: []
    max_retries: 0
    timeout_ms: 5400000
    hang_alert_after_ms: 3600000
    model: minimax/MiniMax-M2.7-highspeed
  - id: impl-r41g-core-lsp-workspace-manager-split
    title: >-
      R41g: service/lsp/workspace_manager.rs 1415 -> facade + N sibling
      (northhing-core)
    prompt: >
      # R41g: lsp/workspace_manager.rs god-split (R27b workspace manager pattern)

      ## Target:
      src/crates/assembly/core/src/service/lsp/workspace_manager.rs (1415 lines)
      Worktree: E:/agent-project/northing-impl-r41g-core-lsp-workspace-manager (branch
      impl/r41g-core-lsp-workspace-manager-split, from main 4841f3bd)
      Package: `northhing-core` (use `-p northhing-core --features product-full --lib`)

      ## CRITICAL iron rules
      Same as R41e, plus:
      **MUST run `cargo test -p <crate> --no-run --features product-full --lib <module>`** to verify test module compiles (R41b lesson)

      ## Sub-domain split (mirror R27b pattern)
      R27b `workspace/manager.rs` was split with this pattern: facade + lifecycle + accessors + types.
      Same applies here. Suggested: `lsp_workspace_init.rs` / `lsp_workspace_state.rs` / `lsp_workspace_query.rs` / `lsp_workspace_types.rs`
      `git grep 'lsp::workspace_manager\|LspWorkspaceManager' -- ':!<file>'`

      ## PREFLIGHT + Implementation + Commit + Self-report
      Same as R41e. Commit: `refactor(core): R41g split lsp/workspace_manager.rs 1415 -> facade + N sibling`
    assigned_to: coder
    role: produce
    verified_by: verifier
    depends_on: []
    gates: []
    max_retries: 0
    timeout_ms: 5400000
    hang_alert_after_ms: 3600000
    model: minimax/MiniMax-M2.7-highspeed