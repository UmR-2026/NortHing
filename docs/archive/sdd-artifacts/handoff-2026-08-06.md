# Handoff — growth core 编排（2026-08-06）

> 给下一个 session 的启动文档。读完这份 + `.superpowers/sdd/progress.md` 的最后三条即可接手，不必回读旧会话。
> 用户**无代码能力**：一切结论要用通俗话讲，不要甩术语和行号让他自己判断。

---

## 1. 一句话现状

`northing` 的「agent 成长核心」正在 `feat/growth-core-0804` 分支上按 SDD 计划逐任务推进，**T5 已全部收口，HEAD = `8b64aa8`**，所有文档已回填，下一步卡在一个需要用户拍板的范围决策（见 §3）。

工作目录（**所有改动只准落这里**）：
```
E:\agent-project\northing\.worktrees\growth-core-0804
```
主仓库 `E:\agent-project\northing` 只用来放 `.superpowers/sdd/` 下的 brief/report/review 文档，**不准在主仓库改源码**（曾有子代理落错仓库）。

---

## 2. 用户当前的指令模式

**连续执行，不逐任务确认**；只在真正需要拍板时（范围取舍、计划与现实冲突、破坏性操作）才打断他。他上一句原话是"继续"。

---

## 3. ⚠️ 接手后第一件事：一个待拍板的决策

计划里下一个任务是 **T4c（把对话回合的几个 hook 收敛成单一 `on_turn_finalized` 门面）**。侦察后发现**计划这一节的前提已全部过时**：

- 计划写的文件位置和行号（`turn_persist.rs:458-512`、`dream.rs:52-62`、四处 hook `:310/:324/:590/:606`）全错 —— 文件实际在 `src/crates/assembly/core/src/agentic/coordination/dialog_turn/`，且已拆成 `turn_persist.rs`(433) + `turn_persist_facts.rs`(385)
- 计划想消除的"散落判定逻辑"**已经被 T4a / T4b / R-7 拿走了**：调度判定已是 crate 纯函数（`scheduler.rs`）、三个裸 DB 键已收进成长状态 blob、facts 门禁已是纯函数 `should_distill_facts`
- 现在 hook 处只剩 `growth_adapter::begin_distill_turn` / `finish_distill_turn` / `boost_turn_topics` 这种薄调用

结论：T4c 剩下的只是**纯形式的门面包装**，收益远低于计划撰写时的预期，而它要动**活跃对话主路径**（风险不低）。

已向用户提出三选项；**2026-08-07 已回答并完成侦察**：
1. **跳过 T4c，直接进 G2（T8-T13）** ← 编排者推荐。T4 的实质收益已到手，形式门面留到 T12 改园丁时顺手做（那时本就要动这条路径）
2. 照原计划做 T4c
3. 先把 T4c 剩余差距完整侦察成报告再定

**最终裁定（2026-08-07）**：用户先选第 3 项完成差距报告，随后采纳第 1 项。T4c 不单独实施，统一门面并入 T12；计划已加入四条防遗忘验收。报告：`.superpowers/sdd/task-t4c-gap-report.md`。下一步进入 G2-T8。

---

## 4. 已完成任务（ledger 是权威，这里只给索引）

权威 ledger：`E:\agent-project\northing\.superpowers\sdd\progress.md`（**已完成的绝不重派**，从第一个未完成处续）

分支提交序：
`7e96126`(T1) → `5eb5fbf`(A1) → `c9dcb58`(A2) → `7e3e279`(A3) → `6294760`(A4) → `1488a0d`(A5) → `a150339`(T2H) → `1c986a4`(T4a) → `985bbb9`(T4b) → `27c9738`(T6a) → `e62a8a3`+`6365cf5`(R-7) → `d1d6d92`(R-2) → `38d1e8d`..`c3d2b31`(S-1) → `e8bb6a2`+`b6157d9`+`0afac87`+`4f7ba93`(R-4) → `fd61f5e`(T6b) → `258d2ea`+`39fadea`(T3a) → `9f261cd`(T3b) → `9a9fb8a`(T7a) → `2e986ce`(T5c) → `71df0dd`(T5a) → **`8b64aa8`(T5b, HEAD)**

本轮（T5 三件）要点：
- **T5a** distiller 纯逻辑迁入 crate：`distill/prompt.rs`(128) + `distill/parse.rs`(374)，`distiller.rs` 656→416。crate 侧用**中性类型**（无 uuid / 无时钟 / 无 provenance），host 补齐。prompt system 段经独立复算 **2886 字节逐字一致**。
- **T5b** dream 判决解析迁入并**白名单参数化**：`review/verdict.rs`(161) 的 `parse_verdicts(json, item_count, allowed_actions)`；顺带把 `strip_json_fence` 的**两份**副本合成一份 `llm_output.rs`(48)。`dream.rs` 348→335。
- **T5c** §5.4-C 的 `auto_memory` 提示词四条，`auto_memory.rs` 595→675。

---

## 5. 模型派发（**逐字复制，禁止用简称推导**）

台账：`E:\agent-project\northing\.opencode\model-capability-notes.md`（顶部有防误表）

| 用途 | 精确 `subagent_type` | 备注 |
| --- | --- | --- |
| implementer / fixer 首选 | `general/deepseek-v4-flash-free_general` | 台账简称 "dsv4f-free"，**5 战 5 胜**；能识别 brief 自相矛盾并按意图取舍且如实上报 |
| reviewer（diff ≲500 行） | `minimax-m3` | 台账简称 "judge-m3"。**注意它属 coding subagent 族，不是 `reviewer/*`** |
| reviewer（diff ≳1000 行 / m3 失败时） | `reviewer/step-explore_reviewer` | 与编排者同源，独立性有折损；但审查极扎实 |
| 逐字转录小单 | `general/ling-3.0-flash-free_general` | 输出会退化成数万行空行、不写报告、遇冲突编空洞断言；**必须他人收尾，"已完成"以 `git diff` 为准** |

**禁用（不得出现在任何派发里）**：任何 `*kimi-k3*`（用户明令 k3 不干活，额度留编排者）、任何 `*gemini*`（用户禁用）、任何 `volcengine-agent-plan/*` 及 `*deepseek-v4-flash_*`（该端点 ≡ ark，不可用）、`ds-v4-flash`、`volcengine-agent-plan/glm-5.2`。

**m3 画像已收敛**：不是能力退化，是**大包体会失败**（T5a 的 1100 行 diff 连挂两次：一次空返回、一次中断；T5b 的 445 行一次通过且判断准确）。按 diff 规模分流。

派发纪律：一次一个实现任务、不粘会话历史、brief 是需求唯一来源、brief/report/diff 全走文件路径不进上下文、派发正文只含「位置一行 / brief 路径 / 跨任务接口 / 已解决歧义 / report 路径」、**不得出现 "do not flag" / "at most Minor" 这类预判审查者的话**。

---

## 6. 验证（每任务编排者亲验，不采信报告）

cargo 前必须：
```powershell
$env:PATH = "C:\msys64\mingw64\bin;" + $env:PATH
```

基线数字（HEAD `8b64aa8`，全部已核）：

| 命令 | 期望 |
| --- | --- |
| `cargo test -p northhing-agentic-growth` | **165**（单一 lib target；"154=139+15" 是加法不是两个 target） |
| `cargo test -p northhing-core --features product-full <filter>` | `dream` **2** / `distill` **31** / `auto_memory` **8** / `memory_db` **28** / `self_cognition` **19** / `turn_persist` **12** / `prompt_injection` **4** / `growth_adapter` **30** / `system_prompt` **21** |
| `cargo check -p northhing-core --features product-full` | warnings **19**（不得增加） |
| `node scripts/check-core-boundaries.mjs` | exit 0 |

core 测试**必带** `--features product-full`。行数用 `(Get-Content -LiteralPath ... -Encoding UTF8).Count`。

⚠️ core `lib.rs:3-4` 有 `#![allow(dead_code)]` / `#![allow(unused_imports)]` → **warning 数没涨不能证明没死代码**。

---

## 7. 硬纪律

- **禁止 `cargo fmt`**；warn-only 语义；English-only 无 emoji（源码与文档）；生产 `.rs` < 800 行
- **不在 main 直接实现**；不并行派发多个实现子代理
- 只派 fixer 处理 **Critical / Important**；**Minor 记 ledger 留终审 triage**
- 双判决（spec + quality）缺一不算通过；不重跑 implementer 已跑的测试（report 即证据），但**关键等价性编排者要亲自复算**
- 审查者标"Cannot verify from diff"的项，**编排者亲自逐条解决**后才可标完成
- 计划要求本身有问题（plan-mandated finding）= **用户决策**，把 finding 与计划原文一起交他，问哪个为准
- 不确定的数据不输出（说"需要确认"，不编数字）

---

## 8. 血泪陷阱（重复踩过的）

1. **写 brief 前必做两次 grep**（编排者连续两轮在此翻车）：
   - 要求"零某字面量 / 禁止出现某符号"之前，先 `rg` 该字面量在**测试 fixture 与断言**里的出现（T5b 栽在这：既要 fixture 逐字保持、又要零 `keep`/`supersede`，不可兼得）
   - 要求"逐字保持/纯追加某文本"之前，先 `rg` 现有的 `!contains(...)` / `assert!(!` 否定断言（T5c 栽在这）
   - 两类要求同时出现时，明写哪条优先、测试数据是否豁免
2. **正则 / 词边界类论断必须实测**：m3 曾自信声称 `\bself_cognition\b` 命中 `load_self_cognition`（实测 `false`，下划线属 `\w`）
3. `rg` 高亮会吃掉匹配文本 → 取原文加 `--color never`
4. PowerShell `Set-Content -Encoding UTF8` 会加 BOM → 还原文件一律用 `git checkout --`
5. **计划里的文件路径和行号可能早已过时**（T4c 整节前提失效）。动手前先侦察现场，以 `git`/文件为准，不要相信计划或自己的记忆
6. 子代理可能把改动落错仓库 → 派发必写"只准落 worktree"，验收查 `git rev-parse --show-toplevel`

---

## 9. 关键决策（D1-D16 有效，勿重议）

全文在计划 §12。高频用到的：

- **D12** 重复证据只晋升 `evidence_strength`，**永不触碰话题权重**
- **D13** 不做写时隐私分类（无敏感类目负面清单、无分类器）
- **D14** §5.4-C3 用"如实版"措辞
- **D15** 记忆捕获量下降必须**可观测**（`was_empty_array` 区分"LLM 返回 `[]`"与"解析失败"）
- **D16**：`score = bm25_pos * two_layer * recency_boost`；`confidence → entry_score` high=1.0 / med=0.6 / low=0.3；归一化 `weight / 5.0`（否决 `(w-1)/4`）；无命中时 `tw_norm` 回落 `1.0/5.0`；禁用 `rank_candidates`。副作用已更正为 `two_layer ∈ [0.144, 1.0]`
- 话题权重域 `[1.0, 5.0]`：floor 1.0、`boost_keyword` = `(weight + 1.0).min(5.0)`、日衰减 ×0.99
- facts 门禁主信号 = `SessionKind`，**不能用 `agent_type`**
- **用户裁定**：800 行上限的目的是防腐化；memory 天天自用 → `memory_db.rs` 拆分**不阻塞**（= T7b，最后做）

---

## 10. 已知延后项（勿当成遗漏，都已写进计划验收）

| 项 | 归属 | 原因 |
| --- | --- | --- |
| dream/园丁/review 路径禁 `supersede` 的边界规则 | **T12**（同一提交内加规则并证明会触发） | 生产至今在调 `db.supersede_fact(...)`，现在加规则会立刻变红 |
| dream **候选筛选**迁入 crate | **T12** | 依赖 core 的 `Fact` 类型需中性化；且 T12 要重写筛选语义，现在搬等于搬两次 |
| `memory_db.rs` 拆分 | **T7b**（最后做） | 用户裁定不阻塞 |
| §5.4-B（园丁 prompt 双模式等三件） | **T12** | — |
| dream 硬作废处方的设计文档回写 | 并 main 前 | 已被 D6/D8 推翻 |

**T12 的现成资产（勿重写）**：`review::verdict::parse_verdicts` 的白名单是入参，园丁四动作只需换实参；`llm_output::strip_json_fence` 是全 crate 唯一副本，别造第三份。

---

## 11. 文件索引

| 路径 | 说明 |
| --- | --- |
| `.superpowers\sdd\plan-2026-08-04-growth-core.md`(450) | 17 任务计划 + §12 对照修订 + D12-D16。T5 节已标完成并含拆分裁定；T12 节含两条转入条款 |
| `.superpowers\sdd\progress.md` | **进度 ledger（权威）**，末尾有"下次续点" |
| `.opencode\model-capability-notes.md` | 模型台账，顶部是派发防误表 |
| `.superpowers\sdd\task-t5{a,b,c}-*.md`、`task-t7a-*.md` | 本轮各任务的 brief / report / review / diff 全套 |
| `src\agentic\AGENTS.md`(46) | crate 契约：§1 只准依赖 contracts 层、§2 权限矩阵、**§3 garden/review 路径禁 supersede 语义**、§4 参数登记、§5 验证命令 |
| `src\agentic\src\` | crate 纯逻辑：`review/verdict.rs`(161) `llm_output.rs`(48) `distill/{parse.rs(374),prompt.rs(128)}` `ports.rs`(271) `topics/{extract,score,competition}.rs` `negation.rs` `scheduler.rs` `state.rs`；`garden/{cleanup,health}.rs` 与 `review/{merge,route}.rs`、`selfcog.rs` 仍是占位 |
| `src\crates\assembly\core\src\service\agent_memory\` | `distiller.rs`(416) `dream.rs`(335) `auto_memory.rs`(675) `memory_db.rs`(980) `facts.rs`(905) `self_cognition.rs`(320) 等 |
| `src\crates\assembly\core\src\agentic\coordination\dialog_turn\` | 对话回合主路径：`turn_persist.rs`(433) `turn_persist_facts.rs`(385) —— **T4c 的现场** |
| `scripts\core-boundaries\rules\source\forbidden-rules.mjs`(3177) | 边界规则；T7a 三组在 `:2248`/`:2308`/`:3140` |
| 仓库根 `AGENTS.md` / `AGENTS-CN.md` | **仓库级事实源**：六层分层、骨干不变量、i18n、日志、平台边界、验证表。就近 AGENTS.md 优先 |

---

## 12. 可清理（低优先）

`.worktrees\growth-a1` .. `growth-a5` 五个 Wave A 单任务 worktree 已 cherry-pick 完毕，可删。
