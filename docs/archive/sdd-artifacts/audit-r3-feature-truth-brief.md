# 审计单 R3 — 功能实现真值核查（只读）

仓库：`E:\agent-project\NortHing`（main，HEAD `f5dc0ef`）。**只读**：禁止改任何文件，唯一可写 = 你的报告。

## 背景与目的

`docs/product/requirements-vs-current-2026-08-29.md` 刚被刷新（10 处过期行已改），但**刷新依据是文档对文档**。本单要的是**文档对代码**：表里每个 ✅ 是不是真的接了线，每个 ⚠️ 缺的是哪一段，每个 ❌ 是不是真没做。

产出要能直接回答用户："我现在到底有个什么样的东西"。

## 必须回答的清单

### A. 逐条验真（按需求表顺序，每个功能域都要查）
对每个域给出：**真接线 / 半接线（UI 有但数据假或动作空）/ 摆设（mock/硬编码）/ 未做**。判定证据 = file:line（UI 侧 + API 侧 + 后端侧三处最好都有）。

1. 会话系统 SE-01~08（创建/列表/切换/持久化/删除/重命名/导出/**搜索**）
2. 对话系统 CH-01~08（输入/**Markdown 渲染**/流式/停止/重新生成/编辑消息/文件引用/图片输入）
3. 工具与确认门 TO-01~06（三档确认门是否真生效、单工具级禁用、超时取消）
4. 技能系统 SK-01~07（后端 60+ 技能、匹配注入、管理 UI、创建、分享）
5. 模型/provider MO-01~06（配置/选择/连接测试/默认模型/参数/本地优先）
6. 工作区 WS-01~06（打开/类型检测/文件树/预览/最近项目/项目规则 AGENTS.md 读取）
7. 设置系统 ST-01~04 + 六张卡（引擎/接入点/MCP/工作区/显示模式/左列四卡）
8. 记忆系统 TH-3（浏览/搜索/导出 JSONL；后端 facts/episodes 到 UI 的通路）
9. 身份系统（名讳/位格/准则：真数据源还是擦边映射）
10. 成长演化 TH-5 / 半被动 cron TH-6 / PCS 插件 / 降级即报错 / 遥测边界

### B. 摆设与假数据猎捕（重点）
11. 全仓扫 mock/占位/假数据：`mock`、`Mock`、`MockEntry`、`// TODO(data)`、`placeholder`、`硬编码中文假数据`、`unwrap_or_default()` 充当数据源、`Vec::new()` 当列表。逐个判定是否出现在**生产路径**（非 test）。
12. `session_mock.rs`（`src/apps/desktop/src/ui_dioxus/session_mock.rs`，293 行）：它是测试数据还是被生产 UI 引用？**这条必须明确回答**——名字很像坑。
13. 左列四卡（编年史/沉积/身份/准则）接的"真实数据源"分别是哪个后端 API？哪几张卡其实还是空态/占位？
14. 设置页六张卡逐张给结论（真接线 / 半接线 / 摆设）。

### C. 后端能力未接 UI 清单（能力浪费盘点）
15. 列出"后端完整但 UI 零接入"的能力：记忆（facts/episodes/蒸馏/回顾）、cron、dream/growth 自评审、snapshot、DeepReview、ACP、MCP、LSP、terminal、git、workspace_search(flashgrep)。每条给后端入口 file:line + 是否有任何 UI 引用。

### D. 前后面
16. `src/web-ui` 是否存在（AGENTS.md 写 missing）？各 surface 实际状态：desktop(Dioxus) / cli / server / installer / acp / e2e，逐个给：是否可构建、是否 shipping、最近一次改动时间（`git log -1 --date=short -- <path>`）。

## 输出格式

三张表：
- **表1 真接线**（功能 / UI file:line / API file:line / 后端 file:line）
- **表2 半接线**（功能 / 缺哪一段 / 补完成本 S/M/L）
- **表3 摆设或 mock**（位置 / 假在哪 / 做真成本 S/M/L）
外加：后端未接 UI 清单（第 15 项）+ 各 surface 状态表（第 16 项）。
最后一段：**这个产品现在"能用"到什么程度**（一句话用户视角描述）+ 离"个人 AI 同事"这个产品目标还差哪几个硬骨头。

## 纪律

- **禁止运行 cargo/pnpm**（编排者另跑）。
- 禁止 git 写操作；禁止修改除报告外任何文件。
- **禁止编造**：每条判定必须有 file:line；无法判定的写「无法判定（原因）」，不要猜。
- 判定"真接线"的标准要严：**UI 有元素 + 有事件处理器 + 处理器调到真实 API + API 到后端有实现**，四段缺一段就降级为半接线，并在表里写明缺哪段。
- 报告中文，英文标识符原样。
