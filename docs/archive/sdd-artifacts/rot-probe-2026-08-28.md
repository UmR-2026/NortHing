# Rot Probe 2026-08-28 — 11 God-File 全面体检 + 下一波清账候选

> **检查口径**：`scripts/verify-rot-budget.mjs` — `countLines`（归一化换行，末尾空行不计数）/ `collectRustFiles`（排除 `tests` / `_tests` / `target` / `tests.rs` / `_tests.rs`）。

---

## 一、11 文件健康度速览（Task A）

| # | 文件 | 当前行数 | Ceiling | 余量 | 走势 | 判定 | 依据 |
|---|---|---|---|---|---|---|---|
| 1 | `app_state/callbacks_lifecycle.rs` | **已删除** | 1011 | 0 | W4-1 物理删除 | — | Slint 回调层 100% 死代码，W4-1（707e414）已全删 |
| 2 | `ui_dioxus/app.rs` | 959 | 962 | **3** | ↑ 涨 | **更纠结** | 476L（727f899）→ 887L（HEAD~5）→ 959L 现在；含 `room_app_root` RSX + `win_ops` 模块 + `watch channel` 管理，三职责合体 |
| 3 | `app_state/callbacks_settings/refresh.rs` | **已删除** | 834 | 0 | W4-1 物理删除 | — | Slint 回调层 100% 死代码，W4-1（707e414）已全删 |
| 4 | `ui_dioxus/css.rs` | 830 | 830 | **0** | → 稳 | **持平** | 纯 CSS 嵌入，无逻辑膨胀；任何新增 CSS 规则即超线 |
| 5 | `ui_dioxus/pages_onboarding.rs` | 859 | 866 | 7 | → 稳 | **更清晰** | 最近提交是清理（12/−19）；单一 onboarding 模块窗，职责清晰 |
| 6 | `kernel_facade/tests.rs` | 823 | 823 | **0** | ↑ 涨 | **持平** | 测试附带增长（P0c 归属 wiring +28）；测试文件天然扩张，属正常 |
| 7 | `cli/ui/theme.rs` | 989 | 989 | **0** | ↑ 涨 | **持平** | 854L（1b147c3）→ 989L；Theme 结构 + builtin JSON 数据占体积，结构清晰 |
| 8 | `core/service/agent_memory/memory_db.rs` | 918 | 918 | **0** | ↑ 涨 | **更纠结** | dream sweep + judge-mom + SQLite 三层功能摊在同一文件；含 75L 单次提交（6574b01），增长无人拦 |
| 9 | `cli/ui/startup/selectors.rs` | 875 | 875 | **0** | ↑ 涨 | **持平** | 767L（1b147c3）→ 875L；选口聚合器，增长来自固有选口数量，功能可理解 |
| 10 | `core/service/lsp/manager.rs` | 836 | 836 | **0** | → 稳 | **更清晰** | 最近变更 8/14（FU-2），单职责（LSP 协议管理），无增长压力 |
| 11 | `cli/modes/chat/input.rs` | 802 | 802 | **0** | → 稳 | **持平** | 自 7/12 snapshot 后零提交；单事件处理职责，但已 dormancy |

### Slint 残留专项排查

- **结论：无 Slint 残留**。
- W4-1（707e414） `callbacks_lifecycle.rs`（1010 行）和 `callbacks_settings/refresh.rs`（834 行）为纯 Slint 回调层，100% 依赖 `slint::ComponentHandle`、`slint_glue::AppWindow`、`slint::Weak` 等 Slint 类型。已随 Slint 壳物理删除，`git show 707e414` 确认 1844 行全删。
- 全仓 `src/**/*.rs` 零 `slint::` 引用；无 `.slint` 文件；Cargo.toml 无 Slint 依赖项。
- `.agents/reference/session/06-app-state-slint-wiring.rs` 为参考文档（非 `src/` 树内生产代码），countLines 不计入，rot-budget.json 不计其 gree。

### 拆分之刀（一句话）

| 文件 | 最自然第一刀 |
|---|---|
| ui_dioxux/app.rs | 将 `win_ops` + `close_os_window` / `ShellWindowManager` 逻辑抽到 `app/window_ops.rs`，room_app_root 留原地 |
| memory_db.rs | 按领域拆：`memory_db/facts.rs` + `memory_db/reviews.rs`（judge-mom review 逻辑独立出去） |
| kernel_facade/tests.rs | 迁测试到 domain 自身 modules 下（已有 domain 拆分的自然延伸，grep 看 `tests.rs` 是否拥有对所有 tests 的导出）。 |

---

## 二、下一轮清账候选（Task B）

### 2.1 `#[allow(dead_code)]` 剩余 106 处 — top 10 分布（ checker 口径：src/ 仅算，排除 tests.rs / *_tests/）

| 排名 | 文件 | 数量 | 可清性 | 注释 |
|---|---|---|---|---|
| 1 | `adapters/ai-adapters/stream/types/openai/openai_types.rs` | 8 | 观望 | 协议类型 stub，可能是 API 兼容性有意保留 |
| 2 | `assembly/core/agentic/coordination/coordinator.rs` | 7 | **可清** | 重构进行中，已有配合 `port_types.rs`（6）的 dead code — W6-1 清理后的新残余 |
| 3 | `assembly/core/agentic/coordination/port_types.rs` | 6 | **可清** | 同上 coordinator 伴生 dead code |
| 4 | `cli/ui/syntax_highlight.rs` | 5 | 观望 | CLI UI 组件实现未完全落地时 stub |
| 5 | `cli/ui/theme.rs` | 5 | 可清 | Theme variants（terminal/raw 等）未用但保留 |
| 6 | `assembly/core/agentic/coordination/handoff.rs` | 5 | 观望 | coordination 模块同批次 dead code |
| 7 | `assembly/core/agentic/tools/implementations/computer_use_input.rs` | 5 | 观望 | 实现未完成的功能 stub |
| 8 | `cli/ui/chat/state.rs` | 4 | 可清 | CLI chat state 未用渲染逻辑 |
| 9 | `desktop/app_state/settings/keyring.rs` | 4 | 观望 | keyring mock + fallback 的可选实现 |
| 10 | `desktop/ui_dioxus/state.rs` | 4 | 观望 | Dioxus state 初始化 fallback stub |

**下一波优先**：`coordinator.rs`（7）+ `port_types.rs`（6）= 13 处同批次 dead code，源自同一 coordination 模块的中途重构，对标 W6-1 模式（真死代码删码），可一次提交清掉 13 处，从 106 降到 93。

### 2.2 `unwrap` / `expect` 生产热点 top 5（排除测试惯例文件后 checker 口径 469 / 937）

**unwrap 前 5**：

| 排名 | 文件 | 数量 | 转换难度 | 备注 |
|---|---|---|---|---|
| 1 | `core/service/agent_memory/facts.rs` | 33 | **易** | 函数已返回 `NortHingResult`，只需把 `.unwrap()` 换成 `?` |
| 2 | `core/service/agent_memory/auto_memory.rs` | 30 | **难** | 深调用链：async 闭包 + await + 嵌套 match，需大量重构才能传递 Result |
| 3 | `services/services-integrations/remote_ssh/password_vault.rs` | 28 | **易** | `ssh-key` 解析 + 密码 vault 操作已有 Result 上下文 |
| 4 | `core/agentic/episodes/store.rs` | 25 | **易** | 返回 NortHingResult 的函数群，直接 `?` |
| 5 | `desktop/app_state/settings/keyring.rs` | 24 | **中** | keyring 操作已有 Result 类型，但部分调用在 `block_in_place` 同步上下文中 |

**expect 前 5**：

| 排名 | 文件 | 数量 | 转换难度 | 备注 |
|---|---|---|---|---|
| 1 | `core/agentic/persistence/session_subhandlers.rs` | 30 | **难** | 深持久化层，多层嵌套 + 事务边界 |
| 2 | `execution/agent-stream/src/tool_call_accumulator.rs` | 29 | **中** | Stream 处理逻辑，部分点已有 Result 上下文 |
| 3 | `core/agentic/persistence/session_branch.rs` | 28 | **难** | 同上持久化深链 |
| 4 | `core/agentic/persistence/metadata_subhandlers.rs` | 27 | **难** | 同样持久化子处理器 |
| 5 | `core/service/config/types.rs` | 27 | **易** | Config 解析层，函数已返回 Result |

**ROI 最高**：`facts.rs`（33 unwrap → 易，一次提交可降 33）+ `password_vault.rs`（28 unwrap → 易）= 61 个一次性清零，占 unwrap 总量 469 的 13%。

### 2.3 `let _ =` 388 贴线 — 5 个最值得补错误处理

这些是 fire-and-forget 吞掉了真实错误信号的地方：

| 优先级 | 文件 | 数量 | 错误性质 | 为什么值得修 |
|---|---|---|---|---|
| 1 | `core/service/mcp/server/manager/auth/auth_oauth.rs` | 12 | OAuth 会话状态全部静默失败 | 12 处 `let _ = MCPServerManager::update_oauth_snapshot/fail_oauth_session` — 整个 OAuth 状态机不可观测，token 刷新断了用户完全无感知 |
| 2 | `core/agentic/tools/browser_control/actions/navigation.rs` | 7 | CDP 浏览器指令全部静默失败 | `let _ = self.client.send("Page.enable")` 等 — 浏览器控制无效时无日志 |
| 3 | `core/service/mcp/server/manager/lifecycle.rs` | 11 | MCP 服务器生命周期 | 插件启停、snapshot 更新、server 状态变更全部静默失败 |
| 4 | `services/terminal/src/pty/process.rs` | 8 | PTY 进程事件 | `event_tx.try_send` / `flush()` / `child.kill()` — 进程退出事件丢失，但部分场景下 kill 本身就是 best-effort（级别稍低） |
| 5 | `execution/agent-stream/src/stream_processor.rs` | 9 | Stream 事件入队 + finalize | `event_sink.enqueue` 和 `finalize_all_pending_tool_calls` 静默失败 — 流式输出中断时无告警 |

> checksum.rs 的 `let _ = std::fs::remove_dir_all(&tmp)`（7 处）**不属于** high-value 修复目标 — 临时目录清理本身就是 best-effort，加 `.ok()` 是合理默认。

---

## 三、意外发现

1. **两个 Slint 回调文件已彻底消亡**：`callbacks_lifecycle.rs` 和 `callbacks_settings/refresh.rs` 在 707e414 中一次性物理删除（−1844 行），本体检无任何 Slint 引用残留。rot-budget.json 中仍登记了这两个 god-file ceiling（1011 / 834），但 `verify-rot-budget.mjs` 只检查 `src/` 下一个 exist 文件的 line count（natural: 它们已不存在磁盘上）。**建议**：清理 rot-budget.json 中两条死登记，避免未来误报或混淆审计。

2. **`let _ =` 388 已触 ceiling**，但扫描中 `ui_dioxus/api.rs` 的 22 处几乎全是测试函数内的 `let _ = submit_turn("test-session"...).await` — 属于 test harness 内合法忽略，不计入生产 gas。若 checker 口径纳入其内，需确认是否与 `tests.rs` 排除规则形成冲突。

3. **sync.rs 死代码残留**（来自 handoff 2026-08-28 第 23 行提及）：I1 修复随 Slint 回调层已删后，`sync.rs` 产生两个 dead helper。本体检未触及 sync.rs，建议后续清账时一并处理。

4. **`memory_db.rs` 三次大 commit 增长轨迹**（5328fb8 fact_type → 582b3a judge-mom → 6574b01 dream sweep），每次 commit 增加 50-75 行，当前 918L 已触 ceiling。三个功能域（fact store + review + dream sweep）有清晰的拆分边界——dream sweep 逻辑（batch 质量评审 + 24h gate）可独立为 `dream_sweep.rs`。

5. **`lsp/manager.rs` 6 个 commit 后仅 836L 且近一个月零涨**：Wave2 B6+B7 修复后进入稳定态，是当前 11 个文件中健康度最高的 god-file 候选——不下沉但不膨胀。

---

## 四、Rot 全局快照

| 指标 | 当前值 | Ceiling | 余量 | 状态 |
|---|---|---|---|---|
| `unwrap_production` | 469 | 502 | 33 | ✅ |
| `expect_production` | 937 | 1,089 | 152 | ✅ |
| `let_underscore` | **388** | **388** | **0** | 🔴 **触顶** |
| `allow_dead_code` | 106 | 109 | 3 | 🟡 临顶 |
| `unix_epoch_inline` | 69 | 69 | 0 | 🔴 **触顶** |

---

## 五、Top-3 建议行动

| 优先级 | 行动 | 预期释放 | 难度 |
|---|---|---|---|
| **P0** | 清理 rot-budget.json 两条已删文件（callbacks_lifecycle / callbacks_settings/refresh）dead entries | 消除误报 + 审计噪音 | 低 |
| **P1** | 清 coordinator.rs（7）+ port_types.rs（6）dead code 同批次 → 目标降 dead_code 106→93 | 13 处 dead code | 低 |
| **P2** | 补 auth_oauth.rs（12）+ navigation.rs（7）+ lifecycle.rs（11）的 `let _` 错误处理 | 30/388 let _ 补观测 | 中 |
