# Handoff — 2026-09-08 W18 波过半：外部审查并入 + checker 加固 5/8 单闭环

> freshest session state。旧篇：`2026-09-07-workflow-improvement-verdicts.md`（同目录）；再早见 `docs/archive/handoffs/`。

## 0. 一句话状态

W18（Phase 0 checker 加固）完成 5/8 单（W18-0/1/2/3/4 + 5a），全部双 judge APPROVE、零 Critical 残留；外部审查效率六条 + 三项拍板已并入工作流；**队列下一单 = W18-5b（退役机械化，brief 已审至 FAIL 后修完，差一轮复审即可派发）**。全部 commit 在本地 main（HEAD `6f3ec9c`），**未推送**。

## 1. 需求基线

- 波次计划：`.superpowers/sdd/plan-2026-09-07-w18-phase0-checker-hardening.md`（v2.1，W18-5 已拆 5a/5b）。
- 用户拍板累计（全部 2026-09-07）：D 表全批（外部审查）/ 严格一增一退 → 后改**净不得增** / 混合准入 / **三个 ceiling 登记 847/2300/4025 + 两条 lease（2026-10-15 复审）** / **代码级批准委托双 judge 车道（不再逐单等用户）**。
- 流程新规（memory `facts/workflow-efficiency-2026-09-07.md`）：效率六条（增量审/证据复用/流水线准备/重试分类/决策卡）、耗时账（ledger 两时间戳 + 返工轮数）。

## 2. 已完成（commit 表，BASE `df5c1ce` → HEAD `6f3ec9c`）

| commit | 内容 | 审查 |
|---|---|---|
| `a32b4f7` | 外部审查 handoff + W18 计划 + 归档轮换 | — |
| `bdc8c35` | W18-0：task-gate rename/copy 双端校验（`--name-status -z`），selftest 11→13 | m3+53 双 APPROVE |
| `785355e` | W18-1：validateManifest fail-closed + 5 负向 fixture + 800/801 边界，selftest 11 | 双 APPROVE（1 轮修复：53 抓 `__proto__` TypeError） |
| `05b5f94` | W18-2：only-down（`--base`）+ headroom floor + cap-and-archive action + 零余量 warning（实测 5 项），selftest 23 | 双 APPROVE |
| `7f05336` | W18-3：>1000 exception lease + allow-god-file 禁令 + floor 旁路收口，selftest 33；scripts 44→45/48 | 双 APPROVE |
| `519c220` | W18-4：exempt-list 入 manifest（**口径修正**：双 h 笔误死豁免修复 + src/shared 死路径删除）+ config 审计，selftest 38 | 双 APPROVE |
| `6dc9b13` | W18-5a：rotScanScope attestation + 收集器扩面（installer 11 + scripts 18，checkedFiles 1368→1397，grep 零漂移）+ 3 登记 + 2 lease，selftest 48 | 双 APPROVE |

中间夹 docs(sdd) ledger commit 若干（每单即时提交）。

## 3. 队列（续单从这里接）

1. **W18-5b**（scripts 退役机械化，净不得增）：brief 已写好（`.superpowers/sdd/w18-5b-brief.md`），reviewer-53 第三轮 FAIL 的 1 条 Important（blob-only 判别负例形态）已按处方修完——**先跑一轮 53 增量复审，过了直接派 `gemini-38-flash-agy`，BASE 填 `6dc9b13`**（派发前把 brief 里「W18-5a 的 TIP（待填）」占位替换为实际 commit，两处处：BASE 节 + 验证节第 3 条）。
2. W18-6：verdict rubric 进 SSOT + dead registration 30 天限期 + P06 文档同步（**含 W18-3 carry-forward：AGENTS.md/CN 家规 3 的 allow-god-file 注释表述必须改为 exception lease**，否则家规与机械禁令矛盾）+ P03 最小版（可选）+ **W18-2 Minor-3 处置：rot-budget.json 加入 metaRatchetPaths**。
3. W18-7：fixture registry sha256 锁定 + D-1 replay + E02 新旧规则对照。
4. 波级终审（reviewer-53，review-package `df5c1ce..HEAD`）→ finishing → 推送（推送需用户授权）。

## 4. 待决与卡点

- **Minor 台账（波末 triage）**：attestation 校验常量非收集器行为；注释禁令正则盲区（`///` 与块注释）；isDateLive/containment 复用抽提；todayUtc hoist；悬空 lease warning；verify-task-gate「续单」子串误报（"后续单" 触发，两次）；`--flag=value` 静默忽略（两脚本统一未知 flag fail-closed）。
- **移交项**：config.godFileThreshold/excludeFiles 文档承诺（W18-4 审计）→ 另开文档单。
- **P2-23 / P2-24** 仍挂账（非本波）。
- **风险观察**：`verify-rot-budget.mjs` 现 2042/2300（lease 覆盖）；波后须兑现 selftest 外置拆分（lease next_action），否则 2026-10-15 复审撞期。

## 5. 子代理运维（本波实证）

- coder `gemini-38-flash-agy`：5 单全交付，W18-1 一次修复轮（续会话有效）。
- brief review `reviewer-53`：每单都抓到真问题（W18-1 FAIL 4I / W18-4 FAIL 1C——test.mjs 冻结用例依赖死路径 / W18-5 FAIL 1C——ceiling 算术不可交付 + 拆单裁决）。**该环节是本轮最大质量杠杆，不可省**。
- judge `minimax-m3` + `reviewer-53` 双车道顺畅；53 一次派发遇 "Service Unavailable" 基础设施错误，等 3 分钟重派即恢复。
- judge 修复轮增量审（上轮 findings + 修复 diff）已实证省回合。

## 6. 环境坑（本会话新增）

- `decide_confirm` 工具调用失败率高——用户拍板改用 `question` 工具或纯文本决策卡。
- hygiene 闸会扫 report 里的 pnpm banner 绝对路径——report 贴输出用 `<REPO_ROOT>` 占位（已写进 W18-3 起 brief 模板）。
- brief 里避免「后续单」字样（含「续单」子串，触发 task-gate 误报）——用「后续任务」。
- `git ls-tree <sha> scripts/` 计数含 tree 条目（49 = 45 blob + 4 tree）——W18-5b 已钉 blob-only。

## 7. Suggested skills

- 续 W18-5b/6/7：`subagent-driven-development`；brief 前置 `anti-rot-system`
- 波末收口：`handoff`（本文件即模板）；终审前 `verification-before-completion`
