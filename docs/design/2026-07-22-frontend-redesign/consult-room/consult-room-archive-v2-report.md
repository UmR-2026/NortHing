# 档案馆 v2 报告（2026-08-04）
**继承**：chrome（标题废除 / 窗控四键入流右上 / 品牌印章左下水印 / cont+mem 双边界 / room-fog 底雾）·门铃宝石（左骑 depth-marker 中心线 --gem-mid，右 bottom:230px 对齐 strata-flow 区）·中枢 depth-marker 作 avatar（breath-avatar + 顶晕染色 + ▴ 收纳胶囊）·#mind/#work 模块化侧栏 mod-hidden 默收 + station-head 拖移把手 + ▴ 收纳·8s 单钟振幅分级无新增 infinite·戒律 rep 仅属 agent，无 dashboard 数字 / 无 emoji / 阴影与双光学同款。
**三要素**：冷 abyss（默认 mind = 深渊 #3F837B）；地层透明度递降（data-depth 1..12，1.00 → 0.09）；统计叙事化（"二十三段对话沉在这里 / 最深处停着去年冬天的回响"）。
**戒律自检**：rep 仅染 depth-marker / 状态 pill / 选中激活条；地层、见证标记、节气刻度、底部 abyss-foot 说明均用 var(--line) / var(--faint) 中性色——色权收口于 agent。
**最大一注**：右宝石 bottom:230px 沿用 v4 数值；若不同窗口高度下不对齐 strata-flow 中部，再按容器测调。
**遗憾**：选中地层详情靠右宝石唤起，默认空——可考虑默认开态让"读它"立刻可及；未做地层条目与详情面板的单向数据绑定（点击仅切换 active 类）。