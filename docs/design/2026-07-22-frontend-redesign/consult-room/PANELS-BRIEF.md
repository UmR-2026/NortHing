# 剩余面板重制 Brief — 2026-08-03

> 视觉真值（唯一）：`consult-room-main.html` —— 先完整读。
> 旧版内容参考（同目录）：onboarding/settings/archive/space 的 v1 文件（见派发正文指定）。
> 任务：用真值风格**重制**你负责的页面，产出 v2。内容结构继承 v1，chrome 与系统一律以真值为准。
> 本 brief 是唯一需求来源。不要问问题。

## 1. 必须继承的系统（逐条照抄真值）

1. **chrome**：无标题栏；状态行左=brand-inline 真 logo SVG + 页面身份；窗控四键（☀─□✕）在主体区右上；containment + membrane-frame 双边界；room-fog 沉积底雾。
2. **deck**：发送/停止**合一按钮**（空闲➤ / 流式■，.send.streaming）；见证注右对齐；无 HALT 独立钮。
3. **中枢**：有 agent 头像的页面用 room-head 可收纳胶囊（顶晕染色 + 状态 pill + ▴ 收纳）。
4. **抽屉**：有左右模块的页面用 membrane-node 触发器（左=头像中心线 mind 色辐射 / 右=背景相反色辐射，位置与弹出同步），模块默认隐藏、可拖移。
5. **呼吸纪律**：8s 单钟、振幅分级；不新增 infinite；双光学完整。
6. **戒律**：rep 只属 agent；无 dashboard 数字；无 emoji；阴影仅真值同款；Slint-safe（无 backdrop-filter/color-mix 新增）。

## 2. 页面特则

- **onboarding**：保留大五色板——那是**人类唯一改色入口**（诞生仪式），不违反"人不可改色"（仅限诞生时）。起始灰寂→选色着色唤醒。
- **settings**：它的自我（只读）/ 设施（可调）双区；上下文 CONTEXT 段独立收纳的语法可复用。
- **archive**：冷 abyss 领域，沉积地层禁 rep；地层透明度递降；统计叙事化。
- **space**：走廊门语法（亮门独占光源）；门即会话，新房=开一间。

## 3. 交付

同目录：`consult-room-<页面>-v2.html` + `consult-room-<页面>-v2-report.md`（≤15 行：继承清单核对 / 页面三要素 / 戒律自检 / 最大一注 / 遗憾）
最终回复只给：一句话摘要 + 两个文件绝对路径。
