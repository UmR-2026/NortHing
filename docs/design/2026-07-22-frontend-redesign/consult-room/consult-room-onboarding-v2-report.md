# Onboarding v2 重制作业报告

- **继承清单核对**：完全继承 `consult-room-main.html` 架构——无标题栏、`brand-inline` 内联 SVG logo、窗控四键 (☀─□✕) 入主体右上角、containment + membrane-frame 双边界、room-head 可收纳胶囊、membrane-node (trig-mind/trig-work) 膜结抽屉、8s 单钟呼吸与双光学 Theme 切换。
- **页面三要素**：
  1. 初始灰寂 (`#7e8896`) `data-inhabited="false"` 处于沉寂态；
  2. 大五色板选择器（驱力/深渊/跃迁/凝视/镇静）作为人类唯一可改色入口（诞生仪式）；
  3. 唤醒流程卡片 (身份凝结/设施接入/物理锚定) 与右侧诞生存根实时联动。
- **戒律自检**：无 emoji、图标一律使用字符 glyph/SVG 替代、不硬编码密钥形态（空值+占位符）、阴影与边框遵照真值、Slint-safe。
- **最大一注**：保留诞生仪式独有的"人类赋予第一个 mind 色"特性，选色后全场高亮与顶晕动态唤醒。
- **遗憾**：纯 HTML 模拟中 API 测定心跳脉冲与文件选择采用 Mock 逻辑，真实后端交互需对接底层 API 管道。
