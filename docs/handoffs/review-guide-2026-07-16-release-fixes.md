# QClaw Code Review — v0.1.0 Release Fixes (3 commits)

> Review scope: Code changes in commits `6c8aadc` + `a973316` + `997e14e`.
> HEAD: `997e14e`.
> Reviewer: QClaw (code-only review).
> Goal: Find bugs, regressions, and missed cleanup — produce a fix list.
> Do NOT modify any files. Output a structured findings report.

---

## How to review

```bash
cd E:\agent-project\northing
git diff facc9c3..997e14e --stat
git diff a973316..997e14e  # desktop + CI fixes
git diff 6c8aadc..a973316  # paths + names + cleanup
```

Focus: correctness, completeness, side-effects missed. Skip style nits.

---

## Commit 1: `a973316` — hardcoded paths + installer names + cleanup

### Files changed (24):
`.cargo/config.toml`, `AGENTS.md`, `CHANGELOG.md`, `Cargo.lock` (new), `Cargo.toml`, `README.md`, `README.zh-CN.md`, `package.json`, `pnpm-workspace.yaml`, `.github/workflows/desktop-package.yml`, `.gitignore`, `.release-please-manifest.json`, 12 junk-file deletions

### Verify:

**`.cargo/config.toml`**
- [ ] `target-dir = "E:/tgt/northing"` removed — was the whole `[target.x86_64-pc-windows-gnu]` section removed too? Is the empty `[build]` section (only comments) valid TOML that cargo accepts?
- [ ] Run `cargo check -p northhing-cli` and confirm it still builds WITHOUT the hardcoded target-dir (uses default `target/`)

**`package.json`**
- [ ] 5 `installer:*` scripts changed `northhing-Installer` → `northing-installer`. Confirm no remaining `northhing-Installer` in package.json.
- [ ] `copy-monaco` and `copy-icons` — do they gracefully skip when `src/web-ui` doesn't exist? Test by reading the actual script logic.
- [ ] Are the 4 new scripts (`desktop:build:linux`, `desktop:build:arm64`, `desktop:build:x86_64`, `desktop:build:nsis`) functional cargo wrappers or stubs? Will `desktop-package.yml` actually work with them?

**`pnpm-workspace.yaml`**
- [ ] `northhing-Installer` → `northing-installer`. Confirm no remaining old references.

**`AGENTS.md`**
- [ ] 4 occurrences changed. Confirm no remaining `northhing-Installer`.

**`CHANGELOG.md`**
- [ ] New entries follow existing format (## [version] - YYYY-MM-DD + bullet list).
- [ ] URLs at bottom are correct.

**`Cargo.lock` (new file, ~13k lines)**
- [ ] Is this generated by a recent cargo version? Run `cargo check` and confirm it doesn't update further.

**Junk file deletions (12 files)**
- [ ] Each deletion was justified. None contained useful code that got lost.

**`.gitignore`**
- [ ] New patterns (`*.py`, `*.cjs`, `r60c-*`, `r63a-*`, `{}`, `VERSIONS_DIR=.`) won't accidentally ignore legitimate files.
- [ ] `Cargo.lock` removed from ignore — correct for binary workspace.

**`README.zh-CN.md`**
- [ ] Expired banner removed at L19-26.
- [ ] `src/web-ui/` reference removed at L181.
- [ ] No new broken references introduced.

**`.release-please-manifest.json`**
- [ ] `0.1.1` → `0.2.10`. Consistent with workspace.package version.

---

## Commit 2: `997e14e` — desktop stabilization + CI fixes

### Files changed (6):
`.github/workflows/ci.yml`, `.github/workflows/desktop-package.yml`, `README.md`, `docs/releases/2026-07-16-v0.1.0-human-usable.md`, `package.json`, `pnpm-workspace.yaml`

### Verify:

**`.github/workflows/ci.yml`**
- [ ] `frontend-build` job has `if: false` or equivalent bypass.
- [ ] `rust-build-check` no longer `needs: frontend-build` (or if it does, the soft-fail is documented).
- [ ] `cargo check --workspace --exclude northhing` — is `--exclude` cargo's actual flag or should it be `-p` per-crate?
- [ ] Any remaining `src/web-ui` path references inside this file?

**`.github/workflows/desktop-package.yml`**
- [ ] Linux/macOS matrix targets are commented out (not silently skipped).
- [ ] `northhing-desktop.exe` → `northhing.exe` in artifact paths.
- [ ] `--target x86_64-pc-windows-msvc` uses correct triple.
- [ ] `installer:build:only` script exists in package.json.

**`package.json`**
- [ ] `copy-monaco` / `copy-icons` inline Node scripts are correct JS (no syntax errors from PowerShell quoting).
- [ ] 4 new `desktop:build:*` scripts exist.

**Desktop build evidence**
- [ ] `cargo build -p northhing` (with MSVC) succeeds — binary exists at `target\debug\northhing.exe`.
- [ ] `cargo run -p northhing` starts without panic — session creation log line appears.
- [ ] Warning count is reasonable (< 50 new warnings introduced by our changes).

---

## Commit 3: `6c8aadc` — QClaw review guide (no code changes)

Just confirm this commit only added `docs/handoffs/review-guide-2026-07-16-v0.1.0-human-usable.md` and didn't touch code.

---

## Cross-cutting verification

1. **Version consistency**: `Cargo.toml` workspace.package=0.2.10, workspace.metadata=0.2.10, package.json=0.2.10, release-please-manifest=0.2.10, tag=v0.1.0-human-usable @ HEAD. No conflict.

2. **Workspace test**: `cargo test --workspace --exclude northhing` — confirm 932/933 (same as baseline, no regression from our changes).

3. **CLI build both modes**: `cargo check -p northhing-cli` (with syntax-highlight) and `cargo check -p northhing-cli --no-default-features` (without). Both must succeed.

4. **Desktop build**: `cargo build -p northhing` (MSVC). Must succeed.

5. **Git hygiene**: No untracked files, no junk files reintroduced, `git status` clean.

6. **No lingering `northhing-Installer`**: `grep -r "northhing-Installer" --include='*.md' --include='*.yml' --include='*.yaml' --include='*.json' --include='*.toml' --include='*.cjs' .` returns 0 (except historical handoff docs that narrate past fixes).

---

## Output format

```
## Per-commit findings

### a973316 (paths + names + cleanup)
- [CRITICAL/MAJOR/MINOR] Description + fix suggestion

### 997e14e (desktop + CI)
- [CRITICAL/MAJOR/MINOR] Description + fix suggestion

## Cross-cutting findings
- ...

## Fix list (actionable items)
1. [P0/P1/P2] Specific file + specific change needed

## Score: X/10
## Verdict: APPROVE | REJECT | APPROVE WITH FIXES
```

Last line of output MUST be one of: `APPROVE`, `REJECT`, `APPROVE WITH FIXES`.
