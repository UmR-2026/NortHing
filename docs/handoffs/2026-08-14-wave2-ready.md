# Handoff — Wave 2 就绪 + 插件化融入（2026-08-14）

接手者：从 `E:\agent-project` 启动的编排者会话。
前序交接：`2026-08-06-backend-followups-wave1-done.md`（Wave1 收官，其 §7 陷阱 / §8 并行禁区仍有效，本文只记增量）。
事实源：计划 `.superpowers/sdd/plan-2026-08-04-backend-followups.md`（§3 已更新）；插件化方案 `docs/architecture/plugin-system-proposal.md`。

## 1. 一句话结论

Wave1 已收官合并（08-06）；本 session 完成**插件化方案 + 融入决策**并落盘；**Wave 2（B5/B6/B7）计划已更新、待派单**，分 **2 worktree 并行**。

## 2. 基线状态

- northing `main` @ `42fa087`，**ahead origin/main 28 commit（未 push）**。
- 主线空档：Wave 2 未派单；growth-core 线仍由另一 session 推进（`task-g*` 产物勿碰）。

## 3. 本 session 已完成（commit 表）

| commit | 内容 |
|---|---|
| `42fa087` | docs: plugin-system proposal + fold guard semantics into Wave 2 |

落盘内容：
- `docs/architecture/plugin-system-proposal.md` —— 插件化引入方案（Cordis/dsh 源码级调研结论 + 分阶段 + YAGNI 清单 + §8 决策记录）。侦察报告全文在 `C:\WINDOWS\TEMP\opencode\cordis-recon.md` / `dsh-recon.md`（临时）。
- `.superpowers/sdd/plan-2026-08-04-backend-followups.md` §3 更新（见 §4 决策 3）。

## 4. 关键决策（本 session，用户拍板）

1. **mobile-web、MiniApp 现在不搞**（范围冻结，追加于 v0.1.0 surface 基线）。
2. **插件化**：P0-P2（可逆注册原语 + registry guard + 配置驱动重组）**暂缓不启动**；P3（真·代码热重载）**列为规划**，随后端推进持续寻找更优雅解法（不锁 wasmtime；dylib 已否决；进程外 MCP 兜底）。
3. **融入 Wave 2（采纳"两个交汇点"）**，不单独开插件化工作流：
   - **B5 T2-M2** 修复方向锁定为 **RAII guard**（局部 `ConnectionSlotGuard`，`try_acquire_connection` 返回 guard、`Drop` 释放，panic 也释放）—— P0"可逆回收"首个真实用例；
   - **B7 新增 T8-NEW**：LSP `uninstall_plugin` 三步（unregister → stop_server → 删文件）事务化/guard 化，失败逆序回滚 —— P1 落点；
   - 通用 `DisposableList` crate **不抽**（仅 2 用例，YAGNI）；P2 **不融入**（与 Wave 2 无交汇）。

依据与详情：`plugin-system-proposal.md` §2/§3/§8。

## 5. 队列 —— Wave 2 分块并行（下一步唯一主线动作）

计划原文：`.superpowers/sdd/plan-2026-08-04-backend-followups.md` §3（三批任务明细与验证命令）。

**分块（2 worktree 并行）**：

| worktree | 任务 | 文件集 | 验证 |
|---|---|---|---|
| A | **B5 relay 批** | relay-core + relay-server（不碰 core） | `cargo test -p northhing-relay-core -p northhing-relay-server` |
| B | **B6 services 批 → B7 desktop/lsp 批**（内部串行） | services-integrations + core + desktop | B6: `cargo test -p northhing-services-integrations --features product-full` + `cargo test -p northhing-core --features product-full --lib remote_connect`；B7: `cargo test -p northhing --lib settings` + `cargo test -p northhing-core --features product-full --lib lsp` |

- **无跨批 blocking 依赖**；B6→B7 同 worktree 串行（都验证 `-p northhing-core`，避免 core 编译/测试竞争）。
- 开独立 worktree（main 工作区被 growth 线占用）；沿用 brief → implementer → judge 双判决 → 证据链入库循环。
- 派单前：复核 B5 锚点行号漂移（Wave1 + growth 线动过代码）+ 对目标 crate 跑 focused 基线（家规 6）。

**选派（08-13 更新，以 BOOTSTRAP 为准）**：实现位 `coder-sn` + `coder-dv4g`；审查 `judge-m3` / `judge-dv4p`。

## 6. subagent 运维变更

- 8-13 外部库机制吸收 5 批落地（validate-skills.py / context-cost-guard / verify-gate.ps1 / pty-tools / decision-ui），已注册。**插件冒烟≠生产可靠**，Wave 2 首次实战（长 cargo / 决策点）盯紧。详见 `.opencode/absorb-plan-2026-08-13.md` + BOOTSTRAP。
- 选派池已换（停用 Kimi/StepFun/GLM 全家桶），详见 `.opencode/memory/BOOTSTRAP.md`。
- 待办仍在账：28 commit 未 push；17 个 skill "Use for" 触发词违规待裁定。

## 7. 陷阱

沿用 `2026-08-06-backend-followups-wave1-done.md` §7（PowerShell 非 ASCII GBK / 不裸 cargo fmt / desktop 测试不碰真实 app.json / tokio Mutex 非重入 / worktree 缺 generated_locale_contract.rs / 进程级 OnceLock 共享），不重复。

## 8. Suggested skills（下个会话）

- `subagent-driven-development`（Wave 2 B5/B6/B7 循环）
- `verification-before-completion`（合并前回归扫 + 家规 6）
- `handoff`（Wave 2 中断或收官时）
