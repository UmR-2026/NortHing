# Handoff — 2026-07-17 Session Wrap

> 交接给下一个 agent 的必读文档。下一次 session 开始时先读这份 + `README.md` + `AGENTS.md`。

## 本次 session 时间

2026-07-17 (跨夜)，约 6h

## 核心成果

### 1. v0.1.0-human-usable 发布审查 + 修复

| 项 | 产出 | Commit |
|---|---|---|
| AGENTS.md 原版恢复 | 2003 行英文 + bilingual link | `8e61991` |
| C5 workspace test | 932/933 pass | `612677a` |
| 桌面端 MSVC 构建+启动 | cargo run -p northhing 成功 | `997e14e` |
| 安全注释 (pairing/encryption/relay) | P0 安全 | `31a0ccc` |
| README 中英文失真 | P0 README | `31a0ccc` |
| BitFun 品牌清除 | GCWing → UmR-2026 | `c335746` |
| 代码地图 .graph/ 迁入 | FTS5 + MiniLM | `a08d0d2` |
| WelcomeView _gate 修复 | 不再卡 provider | `a7bc3fd` |
| 全量 review (Kimi K3 + QClaw) | 30 个 hallucination + 10 个 blocker | 多个 fix commits |

### 2. GitHub 发布

- Repo: https://github.com/UmR-2026/NortHing
- HEAD: `a08d0d2` (后更新到 `a7bc3fd`)
- Tags: `v0.1.0` @ `facc9c3`, `v0.1.0-human-usable` @ `a7bc3fd`
- Visibility: Public

### 3. 用户给的测试凭证 (已存 memory，**不会**进 git)

- Provider: LongCat-2.0
- Base URL: `https://api.longcat.chat/openai/v1`
- Model: `LongCat-2.0`
- Key: 见 `E:\agent-project\northing\memory\credentials.md` (本地 only)

## 已知问题 (从两份全量 review 提炼)

### P0 主线五连 (用户旅程阻断)

1. **安装包构建失败** — installer/src-tauri/ 下 8 个空 .rs 文件
2. **配置双写不同步** — 桌面写 `~/.northhing/config/app.json`，agent 读 `%APPDATA%/northhing/config/app.json`
3. **引导回调缺失** — pick-folder / test-provider / on-test-provider 无 Rust handler
4. **事件桥缺失** — UI 永远不更新流式 delta、turn 完成/失败事件
5. **移动端入口不存在** — RemoteConnectService 无 desktop 调用点

### P1 安全兜底

- `skip_tool_confirmation=true` 默认，agent 可无确认删任意文件
- API key 明文存两处，无 0600 权限
- 配置非原子写，断电崩溃
- shell 拒绝名单可被绕过

### P1 体验

- 取消链路无超时（默认 365 天确认超时）
- 双击发送 → 并发 turn，token 双倍扣
- 中文路径 slug 冲突

## 关键架构决策

- **代码地图系统** (`.graph/`) 用 FTS5 + MiniLM 做记忆/语义搜索
- **workspace_search** (`src/crates/.../workspace_search/`) 用 flashgrep+ripgrep 做代码搜索
- **两套搜索独立运行**，未整合

## 环境注意

- **GCC**: `C:\msys64\mingw64\bin` (不在 PATH，须前置)
- **MSVC**: 桌面构建用 `stable-x86_64-pc-windows-msvc` (GNU 链接器失效)
- **target-dir**: 已清除硬编码，默认 `target/`
- **embed_server.py**: 首次启动需加载模型 (~14s)，端口 7788

## 下一步建议

### 立即 (下次 session)

1. 修 P0 主线五连（按 1→2→3→4→5 顺序，每修完跑 `cargo run -p northhing` 验证）
2. 配置双写并一（最核心 bug，影响所有用户）
3. 事件桥（AgenticEvent → Slint 属性）

### 短期

4. P1 安全兜底（guard 接入生产 + 回收站删除 + key 入钥匙串）
5. 移动端重配对引导
6. 上下文截断可见标记

### 中期

7. CodeGraph 索引（语义代码搜索）
8. installer 后端重写
9. 单实例锁 + 文件锁

## 验证命令

```bash
# 桌面构建 (GCC 路径)
$env:Path = "C:\msys64\mingw64\bin;" + $env:Path
cargo run -p northhing

# workspace 测试
cargo test --workspace --exclude northhing

# 代码地图搜索
cd .graph; py query.py "关键词" --mode hybrid --limit 5

# .graph 备份还原 (不会进 git，因为 /*.db 在 .gitignore)
py backup.py create
py backup.py verify --name <file>
py backup.py restore --name <file>
```

## 关联文件

- 项目级 skill: `.agents/skills/northhing-onboarding/SKILL.md`
- 深度审核: `C:\Users\UmR\WorkBuddy\2026-07-17-02-25-47\northing-deep-review.md`
- 代码审核: `C:\Users\UmR\WorkBuddy\2026-07-17-02-25-47\northing-code-review.md`
- Kimi K3 全量 review: (本次 session 产出，未落盘到 repo)

---

*Session 结束: 2026-07-17 03:00*
*Next session 启动必读: `README.md` + `AGENTS.md` + 本文档*
