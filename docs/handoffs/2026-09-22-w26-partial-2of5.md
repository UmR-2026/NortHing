# Handoff — 2026-09-22 凌晨：W26 进度 2/5 闭环（W26-3/W26-4 已审 APPROVE_AWC）；W26-1/2/5 WIP 留存待续派

> freshest session state。旧篇：`2026-09-21-w26-planned-briefs-ready.md`（同目录）；再早见 `docs/archive/handoffs/`。

## 0. 一句话状态

W26 五单中 **W26-3 / W26-4 已完整闭环**（实现 + report + m3 双判决 PASS + ledger 行已 commit）；**W26-1 / W26-2 / W26-5 三单有未提交 WIP**（形态已核查正确，中断于收尾阶段，无越权迹象）。当前 HEAD=`7c3de6b`（= origin/main `3e9f4b5` + 本波 8 个 docs/feat commit，**未推送**）。token_rhythm（5.3）额度仍未恢复——本轮全部复审/审查用 minimax-m3 完成，实证顺手。

## 1. 下次 session 第一事（按序）

1. **git status 核查**：确认 W26-1/2/5 三单 WIP 仍在（25 文件 M + 5 untracked，清单见 §3）。若被外部还原，按 §3 逐单状态重新派发（brief 已就绪，无需重写）。
2. **续派收尾 W26-2**（最小依赖、WIP 最完整：dialog_turn.rs error_detail 字段 + save.rs 重建合成错误消息 + 两处 struct literal None 已成形）→ m3 审查 → ledger。
3. **续派收尾 W26-5**（最大单：runtime-ports credentials.rs + core infrastructure/credentials.rs + services-integrations set/clear 哨兵化 + desktop/cli adapter 注册 + 测试全部已成形；上次审查实证其三处 Critical 修复均已落实）→ m3 审查 → ledger。
4. **续派收尾 W26-1**（WIP = events.rs UserSteeringInjected→Banner + cli chat/exec 两臂 + agentic.rs:296 注释；m3 报告后 ledger）。
5. **波级终审**（m3 或 gemini-38-flash 补位；波级 allowlist 组装）→ 用户授权推送 → 盯首个 CI run。
6. **波收口三件套**（家规 2）：① 台账翻转 P2-5 / P2-18 / P2-2 三条（翻转文案见各单 report；P1-8 只加注记不翻转）；② 两个 AWC 遗留关闭：W26-3/W26-4 的隔离 worktree 测试复证——CI 跑完后复跑 `cargo test -p northhing single_instance` + `cargo test -p northhing-core --features product-full --lib lsp`；③ handoff 更新。

## 2. 本 session 已闭环（2/5）

| 单 | commits | 审查 | 核心 |
|---|---|---|---|
| W26-4 | `c5f2b9c` code + `5e8294e` report + `18344f8` ledger | m3 PASS（spec+quality 0C/0I/0M；AWC=隔离树 15 tests 复证） | lsp/manager.rs 836→833 净减；uninstall_plugin 预留标注（workspace.rs:450 先例）；stop_server 两处死分支清理；rollback_registration :137 活调用方保留 |
| W26-3 | `6e324f8` code + `f5c0f6c` report + `3ffb757` 审查后 Minor + `7c3de6b` ledger | m3 PASS（spec+quality 0C/0I/0M；AWC=CI 复跑） | Win32 named mutex `Local\NorthHingDesktopSingleton` 手写 extern 零新 crate；main.rs:66-73 启动守卫 exit 1；Guard RAII；非 Windows stub；4 测试 PID 隔离 |

- 编排者侧本波已做：W26-5 brief 经 m3 复审 REVISE→修订（3 Critical：Cargo.toml mcp feature 加 runtime-ports / 测试须 `--features mcp` / desktop 注册落 entry.rs launch()；2 Important：trait 改名 McpCredentialStore 避 rmcp 同名 / set_remote_authorization 链式零 caller 措辞；4 Minor）；5 份 brief 文末补 Rust 约定块；BASE 指针统一订正为 cc586ae（W26-3 续派时因 W26-4 先落，实际承接 18344f8，经 m3 核实属必要零交集）。

## 3. 未闭环三单的 WIP 现状（续派直接接手，勿推倒重来）

BASE for 续派 = `7c3de6b`（当前 HEAD）。三单文件集互不相交、与已闭环两单也不相交。

**W26-2（session error_detail，允许集 6 文件+brief/report/allowlist）**：
- `src/crates/services/services-core/src/session/dialog_turn.rs`（+118 行）：DialogTurnData 加 `error_detail: Option<AiErrorDetail>`（alias 已设）
- `src/crates/assembly/core/src/agentic/session/session_persistence/save.rs`（+132）：重建合成错误消息（format_turn_error_message + error_category_label 中文标签）+ 内联测试
- `turn_lifecycle.rs` / `turn_persist.rs`：fail_dialog_turn 落盘已接
- `distill.rs` / `service/session_usage/service.rs`：两处 struct literal 补 None
- 剩：通读 diff 对齐 §4 Spec → 跑 §6 验证（host 树有 W26-5 WIP，必要时隔离树+疑虑节披露）→ commit（前缀 `feat(session): W26-2`）

**W26-5（CredentialStore port，允许集最大）**：
- `runtime-ports/src/credentials.rs` 新建（81 行：McpCredentialStore + NullMcpCredentialStore + MCP_AUTH_SENTINEL + account 函数 + 2 测试）；lib.rs 已导出
- `core/infrastructure/credentials.rs` 新建（99 行：set/get global + GlobalCredentialStore 调用时解析代理 + 测试）；infrastructure/mod.rs 已注册
- `services-integrations`：Cargo.toml mcp feature 已加 runtime-ports；service.rs new 第二参 + set/clear 哨兵化（含 save 失败 best-effort delete 回滚）；tests/common/mod.rs（+54 InMemory double）+ config_and_server_lifecycle.rs（+78 断言改哨兵 + 新用例）
- `core/service/mcp/config/service.rs`：wrapper new 传 GlobalCredentialStore；`server/manager/lifecycle.rs`：runtime_server_config 哨兵解析 fail-closed（改 pub(super)）；`manager/tests.rs`（+118 core 侧解析用例）
- `desktop/settings/keyring.rs`：DesktopMcpCredentialStore（PRODUCTION_KEYRING 委托，NoEntry→Ok(None)）+ register 函数；`ui_dioxus/entry.rs` launch() 起始注册（3 行）
- `cli/keyring_keys.rs`：CliMcpCredentialStore（复用 store_model_key/keyring_get）+ register；`main.rs:381` 邻近注册
- 剩：同 W26-2 收尾流程（前缀 `feat(mcp): W26-5`）

**W26-1（steering banner）**：
- `kernel_facade/events.rs`（+58）：UserSteeringInjected→Banner(Info, display_content) + 内联测试
- `cli/modes/chat/run.rs`（+17）/ `modes/exec.rs`（+9）：两臂
- `contracts/events/src/agentic.rs`：:296 注释单行更新
- 剩：同 W26-2 收尾流程（前缀 `feat(events): W26-1`）

**工作树注意**：`src/crates/contracts/runtime-ports/` 下 port_core.rs / runtime_facade_tests.rs / session_workspace.rs 显示 M 但 git diff 为空——纯 CRLF 行尾差异，**不要动、不要 commit**。W26-5 的 `manager/tests.rs` 曾有编译错误（W26-3/W26-4 report 提过 E0425），续派时先 `cargo check` 确认现状。

## 4. 盘面

- 本地 main HEAD = `7c3de6b`，领先 origin/main（`3e9f4b5`）8 commit，**未推送**（等 W26 全闭环 + 用户授权）。
- 无 stash、无在跑子代理、无 lock 文件残留。
- 余量（W26-3 复跑读数）：task-gate 13 行 / checker 993/1050 / scripts 45/48 / sdd 145/400；rot verdict at-limit 0 violations。
- 波级 allowlist 未建（终审时按五单允许集并集 + 各 brief/report/allowlist + reviews/ 目录组装）。

## 5. 子代理运维

- **token_rhythm（glm-5.3）额度仍未恢复**（用户本 session 确认）。judge-53/reviewer-53 继续停派；brief 复审 + 任务审查 + 波级终审全部走 `minimax-m3`（本轮 3 次复审/审查实证顺手：W26-5 brief 复审 REVISE 精准抓出 3 Critical；W26-3/W26-4 审查双判决 PASS）。
- coder 主力 `gemini-38-flash-agy` 本波实证可靠（W26-3/W26-4 续派收尾一次到位，无假汇报）。
- 派发正文一律绝对路径；续派模式（接手已有 WIP）比全新派发省一轮——W26-3/W26-4 均一次闭环。
- **curfew 提醒**：m3 指出 W26-3 代码 commit 时间 03:53 越 03:00 宵禁 53 分钟。下次派单在 brief/派发正文写明「最后 commit 须在 03:00 前完成，否则留待次日」。

## 6. 待决与卡点

- 无推送遗留（等本波闭环）。
- 缓办在案（不变）：D2 汇率 / D4 速率档 / E01 / P2-17 合并（frozen）/ K4b / K3 慢线。
- W26 之后：W27 未规划（plan 只到 W26 + K3 慢线）。

## 7. Suggested skills

- 续 W26：`subagent-driven-development`（三单串行续派收尾；brief 已齐）。
- 收口：`handoff`；CI 盯梢 `ci_status`；波级终审组包用 assemble-review-package.mjs。
