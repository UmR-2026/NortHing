# Progress Checkpoint — 2026-07-17 深夜

> 暂停点存档。下次接续：读本文件 + `AGENTS.md`（含骨干不变量节）+ `docs/tech-debt-cleanup-guide.md`。

## 需求基线（grill-me 已锁定）

- 产品：隐藏 IDE 的通用 agent 应用；IDE/CLI/编程能力归 agent，不做人类 UI。
- 人类：只与主 agent 对话；subagent 隐藏（会话树可审核）；介入 = 查看+取消+排队。
- 确认：全免确认；兜底 = denylist + core 快照；回滚入口不做。
- 面基线：仅 Slint 桌面 + installer 发货；mobile/relay/server/MiniApp/SDLC 冻结。
- 完成判据：e2e chat 绿 + computer-use 五步冒烟（装→引导→首聊→停止→失败提示），computer-use 待用户安装。

## 已完成（8 commits，全部 judge 验收过）

| Commit | 内容 |
|---|---|
| `9a1575d` | shell guard 接入生产 + denylist 修复 + MiniApp 元字符 |
| `c7e7218` | CJK 路径 slug 恒带哈希 |
| `95e29ba` | 配置双写并一：provider 同步 core + 启动迁移 + 校验/横幅线程安全 |
| `af15815` | installer 后端从零（14 命令）+ 构建链对齐 |
| `ad349f9` | 桌面事件桥：流式回复/终态横幅/停止按钮/active_turn |
| `65a1003` | lessons 记录 |
| `b62e23c` | 技术债清理指南（外部 agent 执行中） |
| `d4b7154` | AGENTS.md 骨干不变量锚定 |

外部清理 agent 也在并行提交（如 `8ffb991` gitignore B-1）。**注意**：提交前先看暂存区，别卷入它们的关切。

## 进行中（卡点）

**D2a 引导修复**——任务书完好在下节。多次派发未成：s37 空转 33 步零产出（step 模型不胜任中大型任务，已记台账）；lc 数次被外部 abort 中止。工作树干净无污染。
→ 接续：用 `coder`(lc) 重派，或 resume 会话 `ses_08e3b5802ffee9AgswfD6CmdaU`。教训：**派发中勿按 Esc**；看进度用 `Leader+Down` 进子会话。

## 2026-07-18 更新

- 技术债清理指南已由外部 agent 执行完毕（6 commits：B-1~B-4 全落地；`northhing-acp` 意外转绿）；指南按执行反馈修订至 v1.1（`1806776`）+ 新增 `docs/tech-debt/cleanup-execution-log.md`。
- "干预取消功能"澄清：**是派给编排线的任务**，不是已实现的东西——即队列里的 W3a（人类=查看+取消+排队：取消链路挂死三件套修复 + 消息排队）。ledger P0-2 仍 OPEN。
- 队列不变：D2a → D2b → W3a（取消三件套+排队）→ 冒烟。
- **D2a 半成品在 stash@{0}**（"D2a partial (aborted mid-flight, 8 compile errors)"）：被中止的 lc 留下了 5 文件 402 行改动但编译 8 错。接续：`git stash pop` 后让 coder 续修，或丢弃重派。另有两个历史 stash 勿动。
- **CodeGraph 已启用（opencode MCP，2026-07-18）**：索引已建（2,014 文件 / 39,633 节点 / 133,484 边 / 174MB SQLite，`.codegraph/`，telemetry 已关）。MCP 配置在 `E:\agent-project\.opencode\opencode.json`（`codegraph serve --mcp --path E:/agent-project/northing`），协议级冒烟已过（v1.4.1）。**重启 opencode 后**编排者/subagent 可用 codegraph_* 工具；coder 任务书里也可直接写 CLI：`npx @colbymchenry/codegraph callers|impact|explore <symbol>`（CLI 无需重启立即可用）。
- **项目级 skills 已装（`E:\agent-project\.opencode\skills\`，2026-07-18）**：writing-plans / subagent-driven-development / dispatching-parallel-agents / verification-before-completion / requesting-code-review / systematic-debugging（源自 obra/superpowers v6.1.1）+ handoff / to-tickets（源自 mattpocock/skills），均已本地改装（coder-lc / judge-m3、处方级任务书、并行守则、handoff 路径约定）。重启 opencode 生效。源仓库 clone 在 `E:\agent-project\_external\` 供后续增补（候选：grill-me、executing-plans、using-git-worktrees）。
- 模型台账：kimi 变体 k2/k3 停用（额度留编排者），见 `.opencode/model-capability-notes.md`。
- **D2a 已完成并提交 `2c8dd3f`**（2026-07-18）：stash pop 后 m27hs 三轮机械修复（8 编译错→第 9 错 E0382→serde 测试构造错），cargo check 0 error + 2 focused tests 绿；judge-m3 验收 PASS（7 验收点全过，1 Important 跟踪项=test 按钮竞态→D2a+1，3 Minor）。m27hs 实证转正：机械修复类首选。

## 队列

1. ~~D2a~~ ✅ `2c8dd3f`；~~D2a+1~~ ✅ `eca4c4f`；~~D2b~~ ✅ `06b2de3`
2. ~~W3a~~ ✅ 全部落地（2026-07-18，四片均 judge-m3 PASS）：W3a-1 `3de7ced`（AskUserQuestion 防挂死）、W3a-2 `26f392e`（工具/确认默认 300s）、W3a-3 turn 看门狗+取消收敛、W3a-4 desktop 消息排队（见本日 git log 两笔 feat）
3. 冒烟（用户测试中）：e2e chat 套件 + computer-use 五步（需用户装 computer-use）+ W3a 手动冒烟。**D2c（进行中）**：冒烟发现 3 bug+1 缺口——welcome 空 id upsert 致 provider 重复堆积（用户 app.json 已堆 8 条）、设置页类型 tab 误用 "stdio" 文案、onboarding 后 default_model 为空、预设模型下拉。用户定方案：只存自定义配置、砍预设列表、端点按 Anthropic/OpenAI 两系拆开、砍 Gemini tab（6 项修复，见本日 git log D2c 提交）。base_url 语义：OpenAI 系填到 /v1（自动补 /chat/completions）、Anthropic 系填根域名（自动补 /v1/messages）、末尾 # 强制原样。
4. 遗留 Minor（可选）：rename 事件循环闭包内 block_on 短暂卡顿；watchdog 闭包 env 双读；error_banners 其他渠道动态日志；2 个 pre-existing 测试失败（turn_batch::load_session_tail_turns、skill_tool::remote_call_loads，clean main 复现，与本日工作无关，待查）
5. **D2d ✅ `0f485d6`；D2e ✅ `da36f8d`；D2f ✅ `ea88414`；D2g ✅ `3e30f2c`**（变更→sync 管线统一：upsert/delete/set-default 全覆盖）。**后端 e2e 已实测通过**（2026-07-18）：CLI `exec` 无界面跑通完整 turn（core 配置→primary 解析→AI client→longcat 端点 200）；编排者已把用户两份配置修到正确态（desktop 1 provider / core 1 model+primary，base_url=`https://api.longcat.chat/openai/v1`）。
6. 新发现（pre-existing，待立项）：CLI 启动时 20+ 个 `Failed to parse SKILL.md ... Failed to capture content`（`%APPDATA%\northhing\skills\.system\gstack-*`），builtin skills 加载失败，与今日改动无关；另 CLI exec 终端输出有乱码（编码）。
7. **D2h ✅**；**D2i+D2j ✅（运行时实证的大根因修复）**：① `Weak::upgrade()` 后台线程恒 None（探针实证）→ 全部 refresh/banner 改收 Weak、~35 处后台守卫清除；② Slint loop 回主线程 + 主线程 main_rt 供 agent-dispatch；③ invoke 闭包禁 block_on（7 处）；④ DEFAULT_MODE_ID code→agentic（注册表无 code）。GUI 自动化（open-computer-use，Windows 运行时实证可用）验证：19 sessions 上屏、settings 列表、banner、消息发送、agent/model 解析正常。
8. **W4（下一单，确定性 bug）**：desktop turn 在 "Agent using model" 后挂起（core 执行路径；CLI 同配置秒回；摘掉 scheduler 仍现）。排查方向：turn_init 后续（get_client_resolved/tool_policy/manifest/prompt）与 round 起步，挂点前无 watchdog 触发（卡在 stream 开始前）。另：输入框 type_text 经 UIA 会丢首字母（"hi"→"ihi"，自动化副作用，非产品 bug）；Inspector Model pill 显示 provider wire format 而非模型名（小问题）。

## Subagent 运维（.opencode/）

- 变体注册表：`gen-agent-variants.py`（改 REGISTRY 重跑 + 重启生效）。
- 实证：coder=lc ✅、judge=m3 ✅；s37 ❌（空转）；s35/sn/k2/m27hs 未试。
- 能力台账：`.opencode/model-capability-notes.md`（每轮更新）。
- 任务书必须"处方级"（带 file:line + API 签名 + 字段清单），开放式探索必空转。

## 2026-07-19 存档（session 结束态）

### BitFun 清理 + 工作区大扫除（已完成，judge PASS）

- **仓库内**（`7ec0235` + `d779736`）：源码/配置/技能目录 bitfun 零残留——asset 重命名 `northhing-backend-adapter.js`（include_str 同步）、`build-northhing.mjs`、技能目录 `northhing-v3-workflow`、ACP 注释修正、3 个 BitFun 时代文档删除、`.pyc` 出 git + gitignore 补规则。剩余 58 处仅在 docs/handoffs|plans|reviews 历史快照内（仓库自身 B-2 政策允许）。**教训：PowerShell 批量改文件编码会毁文件**（Set-Content 默认非 UTF-8）——改文本一律用 edit 工具。
- **工作区**（E:\agent-project）：从 ~40 项清到只剩 `.opencode/`、`northing/`、`_external/`。删除：BitFun/（249MB）、mmx51-asar-dump/（354MB）、agent-cli/（559MB，早期 subagent 原型，已被现行 opencode 流程超越）、northing-impl-b0-smoke/（**16.7GB**，内容已快照在主仓 b2-god-split-v2 分支+stash）、~40 个 scratch 脚本/日志/评审稿、plans/、scripts/。AppData 删除：bitfun（125MB）、northing（4.3MB）、com.bitfun.desktop（227MB）、com.bitfun.installer。**保留**：%APPDATA%\northhing（活配置+会话）、com.northhing.desktop。

### 前端重构（Tauri+React）已立项并开工

- 计划：`docs/superpowers/plans/2026-07-19-frontend-rebuild-tauri.md`（judge-m3 评审 Approved）。方向：聊天应用风、markdown/代码高亮、F3 预留 ComfyUI 类 web 面板插口、F4 才翻基线（Slint 保留可构建）。测试连接两个接线 bug + 气泡太大 + Model pill 显示问题全部归入 F2/F1 修复。
- **F0 已完成 3 commits**（judge PASS）：`e3daf75` 脚手架（Tauri 2 + React 18 + Vite，src-tauri 排除主 workspace，仿 northing-installer）、`0ac026e` worker runtime + core init（core_rt.rs：OnceLock<Handle>，复刻桌面 init 序列）、`6a61354` 事件桥（emit-only，W4b 纪律）+ 四命令（core_rt().spawn oneshot 转发）+ 最小聊天 UI。
- **环境要点**：主仓 repo dir override 是 GNU 工具链，新 src-tauri crate 的 cargo 命令必须先 `$env:Path = "C:\msys64\mingw64\bin;C:\msys64\usr\bin;$env:Path"`（ring 要 gcc；本 shell 是 NoProfile 不读 PowerShell profile）。
- **当前卡点（F0 验收）**：~~用户首测"可以输入，无输出"~~ 已分三层破完（2026-07-19）：①**Tauri v2 权限**：脚手架没生成 capabilities，`listen()` 被拒 `event.listen not allowed` → 加 `src-tauri/capabilities/default.json`（`core:default` + `core:event:default`）。②**事件名**：`chat://chunk` 改 `chat-chunk`（`://` 可能有校验问题，顺手改规范名）。③**落盘竞态**：`DialogTurnCompleted` 事件先于 assistant 消息持久化发出，前端立刻 getMessages 会少最新一条 assistant（症状"回复要下一句才出现"）→ 前端 completed 时先把流式草稿乐观落成 assistant 消息，再 400ms/1500ms 延迟对账取长列表。**core 侧该竞态仍在**（Slint 版同样有，只是被时序掩盖）——列为后续 core follow-up（turn finalize 应先落盘再发 completed 事件）。
- **诊断方法论（本次实证有效）**：前端调试面板（listener READY/FAILED + 事件计数直接渲染在页面上）+ PrintWindow 后台截屏（`E:\agent-project\.opencode\sdd\f0\cap.ps1`，全屏游戏遮挡也能截）+ 后端合成事件自测（emit 假 chunk 验证 listen 链路）。SendKeys 自动化在全屏游戏持前台时不可靠（SetForegroundWindow 抢不过）。
- **调试产物**：事件桥 emit 结果日志保留（chat-chunk emitted / turn-state result）；App.tsx 底部调试面板保留至 F1 拆除；main.tsx 已去 StrictMode（dev 双挂载会建两个 session）。
- F0 提交：`c0203c4`（权限+改名+乐观 finalize）。待用户最终目视确认流式回复即 F0 关闭，随后开 F1（聊天主界面，派单前先出设计稿/组件结构）。

## 2026-07-21 存档（K2 中途，下次唤醒接续点）

- **HEAD `8132641`，树干净**。K2 进度：K2a ✅ PASS（`1384f2b`+fix-A）；K2b src-tauri 已切 facade（`ae15d22`+fix-B/C/D），**judge 三轮 FAIL→返修后代码全绿但尚未终审**——剩余缺口：①judge 对 fix-D（门闩失败恢复+锁毒化，`ce0abb8`）未复审；②4 个门闩生命周期测试未写（fix-E 处方已备好：抽 `run_init_gate` 泛型 seam + 并发/重复/失败恢复/trait 未初始化四测，任务书在本会话 fix-E 派发稿）；③K2c（前端 TurnTrace + 头部瘦身只留设置）未开工；④K2 验收未跑（GUI 冒烟=用户 GUI review、增量编译实测 vs 29.86s 基线、desktop-tauri cargo tree 已过）。
- **下次唤醒顺序**：judge-m3 复审 `5018aa4..HEAD`（fix-C/D）→ fix-E 测试（或我亲自写）→ K2c → K2 验收（GUI 冒烟 + 增量实测）→ K3 ROI 闸门。
- m27hs fix-D 空返回留半成品（编译 2 错），编排者补修提交；lc 已降级 ⚠️，K 线实施默认 m27hs+处方级真 API 清单。
- Tauri dev 实例可能仍在跑（GUI 冒烟用）；vite 须单独起（tauri.conf 无 beforeDevCommand）。

## 2026-07-21 存档 2（K2 全绿收官，等用户目视 + K3 闸门拍板）

- **HEAD `0211e2e`，树干净。K2 全部验收完成**：
  1. **judge 复审 fix-C/D**：一审 FAIL（fix-D 顺带提交的门闩测试 3×E0382 编译错，CI 必挂）→ 编排者亲自修（fix-E `b7cfcde`：assert 用预 clone、删 `_facade` 死参、改错位 K1b1 日志）→ `cargo test --lib kernel_facade::` 12/12 绿 → 同 judge 复审 **PASS**（fix-C/D/E 整段 Ready to merge；follow-up：subscribe_events sentinel 改 Result、warn 风格统一等 4 项 Minor）。
  2. **K2c 前端 TurnTrace**（m27hs，`9d58582`）：api.ts 补 ChatToolEvent/onToolEvent/duration_ms；新 TurnTrace.tsx（TurnContainer=耗时头+思考节+工具节 upsert+正文）；App 接线（liveTools/traceMap/elapsedSec，全走 ref 模式）；头部瘦身（产物/选项/thinkDefaultOpen 删除，单设置按钮+占位面板含调试开关）。judge 一审 Approved+1 Important（think 未随 trace 头折叠）→返修 amend→复审 **PASS**。
  3. **K2 验收**：desktop-tauri cargo tree 此前已过；**增量编译实测 31.36s/噪声 31.49s vs 基线 29.86/29.58——未达 14.93s 目标**（K2 不动主 workspace 结构，预期内；数据留 K3 ROI 闸门）；**GUI 自动化冒烟抓到真 bug → fix-F `0211e2e`**：①`submit_turn` 传 `workspace_path=None`，session 不在内存 restore 时校验必败（发送 100% 失败——fix-B/C/D 三轮 judge 只验编译没验运行时，冒烟的价值）→ 改走 `resolve_session_workspace_path`+fallback default（与 create_session 语义一致）；②K2c 设置面板 absolute 定位无 positioned 祖先逃逸视口 → `.settings-wrap{position:relative}`。修后实证：发送/流式/持久化对账/工具 trace 节（ExecCommand 进行中标记）/执行中耗时头/头部瘦身/设置面板全绿。judge 对 fix-F **PASS**。
- **GUI 自动化新经验**（存 `.opencode/sdd/kernel/k2-smoke-*.ps1`）：**UIA InvokePattern + ValuePattern.SetValue 全可靠**（不受全屏应用抢前台影响，ValuePattern 能触发 React onChange）；mouse_event/SendKeys 在全屏应用持前台时点击丢失/键盘乱码；UIA TogglePattern 对 React 受控 checkbox 无效（要真点击 label）；UIA FindAll 按钮索引随 UI 变化（codeblock 复制按钮会插队），用前现取。cap.ps1 PrintWindow 依旧可靠。
- **待用户**：①dev 实例在跑（含全部修复），最终目视 GUI；②**K3 ROI 闸门拍板**——实测增量 31.4s vs 目标 14.93s，砍半未达，K3 是否继续按此数据决策。
- 产物：`.opencode/sdd/kernel/`（k2c-brief/report、两轮 review-package、k2-smoke 脚本+截图 20 张、k2-fixf-review-package）。

## 2026-07-21 存档 3（UI 重构完成，用户拍板"最美观且便于维护"）

- **HEAD `13ebe9d`，树干净。两 commit**：`b870f07`（UI redesign）+ `13ebe9d`（review minors）。
- **结构拆分**（604 行 App.tsx → 8 文件）：`App.tsx` 薄壳 + `hooks/useChat.ts`（会话/事件订阅/流式状态机/5 ref 纪律）+ `components/{Header,Composer,MessageList,Markdown,TurnTrace}.tsx` + `lib/parseThink.ts`。**事件流纪律逐字复刻**（I-7 buffer 三 kind、C-4 单次 refetch、乐观 finalize+traceMap、failedMsgIds、deps 不含 liveTools）——judge 逐点核对 PASS。
- **视觉重写**：token 体系（21 个，`--accent-rgb` 现代 rgb(var/alpha) 语法）；渐变用户气泡/头像/logo glow；trace 头 pill chip+pulse dot；trace 节左边线 timeline；工具名 accent chip+进行中 badge；空态建议 chips（点击填词+focusTick 聚焦）；设置面板锚定+menu-in 动画；markdown 精修（marker/blockquote/th/hr）+ **remark-gfm 新依赖**（表格此前不渲染，与 mobile-web 同版本 4.0.1 无版本分裂）；composer focus glow；发送/停止改圆角方形；debug 面板 mono 卡片。
- **验证**：type-check/build 双绿；GUI 截图实证 7 场景（空态/markdown 全要素/GFM 表格/live trace 工具节/完成后/设置面板/最终态，`.opencode/sdd/kernel/ui-*.png`）。judge-m3 **PASS**（5 Minor 已修 4：死 token、旧色值、thinkblock.live 接入、deps 清理；payload any 留 follow-up）。
- **教训（再次）**：PowerShell `Get-Content -Raw | Set-Content` 批量替换会把 UTF-8 非 ASCII（em-dash）双重编码成 `鈥?`——批量改文本只能用 edit 工具；这次靠纯 ASCII 内容+事后修复捞回。
- 队列不变：等用户 GUI 目视（dev 实例在跑，vite HMR 即最新）+ K3 ROI 闸门（增量 31.4s vs 目标 14.93s 实测数据在存档 2）。

## 2026-07-22 存档（三线规划开工首日：B 线收官、C 线推进中）

> 接续：`docs/plans/2026-07-21-three-track-refinement-plan.md`（v0.2，含执行状态表）+ 本文件。

- **外部评审对账完成**（`northing-review-consolidated-2026-07-21.md`）：§一~三 ~90% 系旧快照重复发现（已逐条对 commit 证据标注"已落地禁止再派工"）；§二残留核实全部已清/在用；真正未修转入队列（I-2 优雅关停、M-7/M-8/M-10、M-9 待拍板）。
- **B 线收官**：B1a+B7（skills BOM+GBK 乱码+loader 加固，**顺手实证了"Failed to parse SKILL.md"根因=BOM**）`46172ec`+`eed3da8`；B1b（模型信息注入 prompt）`101bc1f`；B8（turn_batch 路径+并发排序；skill_tool 系 B1a/B7 顺带治愈）`82ea09f`；B3（subscribe_events Result 化+workspace_path）`6f039ca`+`4f251da`；B2-core（turn 相位一等事件）`9493450`；B4（**turn failed 此前根本到不了 UI**→TurnState 正路由+error_kind+result_count）`062959f`；B5（stale outcome 守卫）**judge FAIL→revert 转设计先行**（None→stale 假设不成立+TOCTOU+drain 误吞，重设计要点已录 plan v0.2）。
- **C 线**：C1 身份重写 `0704ceb`（**CLI exec 实证**：自报"独立智能体"+模型 LongCat-2.0——C1+B1b 双验证）；C2 Episode Log `159c10d`（日记存储：finalize 挂点+蒸馏+facade 透出；红线=不注入 prompt）。下一步 C3（本体记忆 facts+prompt 注入）、C5（subagent 精细化）。
- **K3 闸门**：用户拍板 **NO-GO** 大拆，转低风险替代（sccache/cargo 配置/定向拆）。
- **A 线 10 单暂缓**：用户在做前端/logo 视觉调整，避免同仓碰撞；A12-A17（评审§6 收编）随 A 线启动。
- **模型台账新增**：m27hs 有**测试 placeholder 模式**（断言成本高的场景会写假断言充数，C2 被抓一次）——任务书必须显式写"禁止 placeholder 测试、命名即行为"。
- **环境敏感测试家族**：subagent_ports 的 tests_cancel/timeout/concurrent/error 假设"本机无 LLM 微秒失败"，本机有可用配置时稳定失败（与代码无关）——需改确定性 fake backend，独立测试基建单。
- 待修小项：`.agents\skills\*`（项目 workspace skills）YAML 控制字符 parse 失败（噪音）；`.system\memory` SKILL.md 空文件 parse 失败（loader 应跳过空文件）。

## 2026-07-22 存档 2（外部评审 2 对账 + 设计交付物落库 + 家规生效）

### 外部评审 2（Tauri 可用性/W3a 测试/god file）对账

用户拍板"不立即处理，记账+调整下次编码"。对账结论：

- **Tauri 版不可独立使用（真）**：设置页骨架（无 provider 配置）→ 首启无配置即砖。这正是 F2 范围，A 线启动时 F2 设置页**提前为最高优先级**（provider CRUD+测试连接+模型切换）。身份冲突（agentic_mode 自称 AI IDE）评审看到的是旧快照——C1 `0704ceb` 已修。
- **W3a 零并发测试（真）**：W3a 四片（AskUserQuestion 防挂死/工具默认 300s/turn 看门狗/消息排队）只有 judge PASS 无自动化测试。→ 家规 #4 生效（并发改动必须带测试）+ 补单 W3a+1（看门狗/取消收敛的确定性 fake 测试）入队。
- **kernel_facade god file（真，2036 行）**：违反 R 系拆分 ≤800 铁则。→ 家规 #3 生效（800 警告/1000 拆分或 allow-god-file）+ 拆分单入队（lifecycle/dto/api/tests 分模块，沿用 R 系拆分惯例）。
- **surfaces.md/tech-debt-ledger 过时（真）**：api-layer/transport 仍列 active、kernel-api/desktop-tauri 未登记、P0-1/P0-2 未翻 resolved。→ 家规 #2 生效（crate 结构变动同 commit 更新 surfaces.md）+ 同步单入队。
- 编译回归 29.86→31.36s：已记录，K3 NO-GO 转低风险（K4 候选）。
- checkpoint 噪声：收敛为每日 ≤1 个 docs checkpoint commit。

### 家规（已写入 AGENTS.md `Housekeeping rules`，2026-07-22 生效）

顺手清配额 / 文档同步 hard rule / god-file 防线（800/1000）/ 并发测试绑定 / **每日 03:00 后停止编码**（用户作息）。

### 设计交付物落库（前端方向重定义）

- 位置：`docs/design/2026-07-22-agent-centric/`（设计哲学 + spec v1 + 交付规范 + 4 mockup html + 2 logo svg，源自 qoderwork 定稿）。
- 核心语言：**心理咨询室隐喻**（白灰中性基底 #F4F3F0，agent 代表色珊瑚 #C8714C 浮现，深渊青 #3F837B 冷色）+ 自我认知编年史渐变条（出生灰白→现在代表色，界面强调色≡右端同源）+ 对等同事交互翻译 + 诗意<功能 + 沉降式动效（呼吸只给 logo/头像，禁弹跳/spinner）。
- **A 线重定义**（替代原"打磨现有暗色 UI"）：按交付规范四阶段——①视觉骨架（token+Fraunces/Noto Sans SC/JetBrains Mono 自托管+收起态主页+操控台+两态切换）②核心逻辑（运行状态机/会话管理/对话流）③成长系统（编年史同源色绑定+技能候选+意识更新+成长时刻动画）④完善（自治档位+确认门+产物审阅+@// 弹层+异常态）。**F2 设置页提前**（见上 Tauri 可用性）。现有组件化结构（useChat/MessageList/TurnTrace）是逻辑底座保留，视觉层整体换 token/布局。
- **成长闭环合流**：设计稿的技能候选（同类失败×N 候选）= C4 技能锻造 UI；意识更新 = C2 Episode Log UI；自我认知色带 = agent 代表色系统（新需求：core 需存 agent 自选代表色+名字，desktop-ui prefs 或 GlobalConfig）。
- logo：同心环+穿心线（沉积/驱力/深渊三要素微缩），SVG 已入库；头像≠logo（agent 个体抽象印记）。

### 队列（下次编码按此序）

1. C5b-G1 judge 复审（重派，上轮被中止）→ C5b-G2/G3（subagent 取消/失败/超时出口 episode 覆盖）。
2. surfaces.md + tech-debt-ledger 同步单（家规 #2 首单示范）。
3. kernel_facade 拆分单（god file，家规 #3 首单示范）。
4. W3a+1 并发确定性测试单（家规 #4 首单示范）。
5. **A 线四阶段**（设计交付规范 §13）+ F2 设置页提前。
6. C4（技能锻造 Phase 0 judge 门禁）、C5c、C6/C7 设计稿、K4 编译提速、F3/F4。

### 待用户拍板（下次唤醒确认）

- ~~03:00 作息边界~~ ✅ **凌晨 3 点**（用户确认 2026-07-22）。
- ~~F2 优先级~~ ✅ **F2 随 A 线①期启动**（用户确认）。
- **工作模式（用户定 2026-07-22）**：前后端异步——后端全部走 subagent 管线（编排者调度 coder/judge）；前端（A 线四阶段+设计细节）由用户与编排者直接异步讨论推进，不派 coder。

## 2026-07-21 存档 4（截图 review 返修 + 无边框标题栏）

- **HEAD `7d35e1d`**。用户截图 review 两问题已修（`584061b`）：空 debug 面板不再渲染空框（`debugOn && debugLines.length > 0` 才渲染）；设置面板点外关闭（mousedown outside + ref contains 判定）。
- **无边框深色标题栏（`7d35e1d`，用户拍板）**：`tauri.conf.json` `decorations:false` + capabilities 加 `core:window:allow-{start-dragging,minimize,toggle-maximize,close,is-maximized}`；Header 集成 48px 标题栏——logo/wordmark/status pill + `data-tauri-drag-region` 中间拖拽区（双击切最大化）+ 设置 + 窗口控制钮（– □/❐ ✕，close hover #e81123，maximized 态经 onResized 同步）。UIA 实证最大化双向切换正常。**经验**：拖拽区只做中间 spacer（flex:1），控制钮在拖拽区之外，零 stopPropagation 风险。
- 截图存档 `.opencode/sdd/kernel/ui-*.png`（9 张：空态/markdown/GFM 表格/live trace/设置面板/返修/无边框）。

## 2026-07-21 存档 5（状态归属 agent + 设置整页，用户拍板）

- **HEAD `e8bdf0c`**。两项改动：①**状态入 agent 行**：头部 就绪/回复中 pill 删除；TurnContainer 顶改 agent 身份行（头像+名字+live 状态「深度思考/生成回复中/执行工具·名称 · Ns」+ pulse dot），turn-started 即显（不再等首 chunk，`isStreaming || streamParsed` 渲染）；完成态「任务耗时」pill 收进行右；内容缩进 34px 对齐名字。②**设置整页**：点设置进 SettingsView（左导航 通用/调试 + 内容区 + ←返回），调试开关迁「调试」区，下拉选单及其点外关闭逻辑删除。
- GUI 实证 5 张（ui-10~14）：身份行/执行工具·ExecCommand·5s live 状态/设置页/调试区/返回。type-check+build 双绿。清理测试残留 blackhole.html（agent 冒烟产物落 src-tauri cwd）。

## 2026-07-20 存档（session 结束态，等用户起床）

### 当前仓库状态

- **HEAD 干净**，工作树只剩应提交的锁文件（已提交：lockfiles regeneration commit）。E1 `c8630ad`（events/terminal/git/cli 死代码 165 行）、E2 `fd5a9e6`（CI/web-ui 脚本/installer 依赖）、E4 `bd575ec`（**IPC descope**：3 个 always-false flag + IpcSpawnAdapter 删除，AGENTS.md 不变量节已同步）、砍单批 `9b87431`+`89db5cf`（watchdog 折进 select!、compact 发射点注释、死 token 字段删除）、前端补丁拆除 `0fb5086`（C-4 修复后双 refetch 删为单次对账）。
- **验证全绿**：`cargo check --workspace` 0 err、`cargo check -p northhing` 0 err、installer check 0 err、`cargo test -p northhing-core --lib` 108 过、w4_repro dual 完整 turn。
- **E3 未开始**（lc 派发后循环输出被用户中止，零写入零污染）。

### 待办（按序）

1. ~~**E3 重派（拆小）**~~ ✅ 全部落地（2026-07-20，拆三小单派 m27hs 各一次 DONE）：E3a `25f914e`（relay-server 孤立树 routes/+relay/）、E3b `27cd725`（api-layer crate）、E3c `7abbb3e`（transport crate + assembly 死 re-export）。**s35 监控首发失败**（judge-s35 循环输出被中止，step 系与 s37 同判 ❌，已记台账）→ E3a 监控复审改 judge-m3 PASS。
2. ~~**judge 复审**~~ ✅ judge-m3 对 `89db5cf..HEAD` 整段复审 PASS（砍单批/E1/E2/E4 + E3 三连，6 commit 全过；build/test 复核全绿）。发现 7 项文档残留（2 Medium + 5 Low）→ 编排者 docs 清扫 `f05e406` + lockfile 再生成 `d6014f7`。Historical 文档残留（`.agents/reference/`、PROJECT_STATE 等）留后续 docs sweep。
3. ~~**GUI 复测**~~ ✅ Tauri 冒烟通过（2026-07-20）：vite+tauri dev 启动（注意：tauri.conf.json 无 beforeDevCommand，vite 须单独起）、发 "hi" → W4-P 探针确认流式 chunk 增量到达 → 3.6s 完成即现（截图存 `.opencode/sdd/cleanup/smoke-*.png`）。教训：SendKeys `{ENTER}` 跨 PowerShell 进程发不进 webview，点击发送按钮可靠；**派发中勿按 Esc 之外，自动化输入一律同一脚本内 SetForegroundWindow**。
4. 之后回计划主线：F2 设置页（+core_health 健康感知）、F3 web 面板、F4 基线。Crash recovery（中断检测）单独立项排 F4 前。

## 2026-07-20 存档 2（K 线开工：kernel 解耦战役）

- **北极星文档定稿**：`docs/architecture/agent-kernel-northstar.md` v0.3.1（主 agent 为心、万物为模块；第一痛点=编译慢）。双 judge 过：m3 v0.2 审 14 项、lc v0.3 审 7 项全部吸收；用户五条质询（facade 量化 N×1.2/1500 行、K3 探针前置、K1 输入∪F2/F3、K2 回退路径、K3 ROI 闸门）全部成文。F1.5（workbuddy 式 turn 轨迹）已入前端重构计划并与 K1/K2 合流。
- **K0 ✅（judge-m3 FAIL→返修→PASS）**：基线——冷 check 316.82s；**增量（touch leaf tool-runtime）29.86s（噪声下限 29.58s）→ K2 目标 14.93s（需砍半）**；desktop-tauri 独立 129.22s；最慢 crate northhing-core 116.83s + aws-lc-sys 77.13s。反向依赖：主 workspace 4 个直接依赖方 + desktop-tauri 独立。K3 探针（plain_output 实搬 agent-runtime）：双 check 绿 + 5 测试过 + 0 真实环，但只是 fan-out=0 下限。**教训：lc 交付会写"推断 yes"充数（Part B 初版未实搬被 judge 抓 FAIL）——任务书关键证据必须要求真实命令+退出码。**
- **外部评审（qoderwork，2026-07-20）：可冻结，无 Critical**。导引 `docs/reviews/2026-07-20-external-review-kernel-facade.md`（三靶点）。3 条已修入文档：①CI per-PR 持续守卫（cargo tree 零命中从点态验收升级为不变量，北极星 §4 ④ + 草案 §8 #12）；②K2 补 desktop-tauri 独立 workspace cargo tree 验收（草案 §8 #13）；③workspace 管理覆盖路径核实——**实测不是 GlobalConfig 字段**（外部推测错误），是 desktop AppSettings UI-owner 状态 → K1 不加方法，列为 F2 启动前置决策项（草案 §4）。另落实 2 条前瞻拆分指引（SettingsApi>12 拆 Config+McpApi；PlatformApi>10 按域拆）+ Inspector 禁止循环调 get_mcp_status 的实施指引。
- **K1 实施 ✅ 全部 PASS（2026-07-20/21）**：K1a crate 定义 `0b7ea6c`+`4535072`（m27hs 转录；judge FAIL→修：CI 守卫 northhing-core 子串误命中 core-types 改边界匹配、删私加 impl 块）；K1b1 `5a85932`+`e28580b`（**lc 写的 50 错半成品我误提交**——模块挂 product-full 下，单 crate check 假绿；m27hs 机械修全绿）；K1b2 `04286b5`+`45c17bc`+`6c9a732`（m27hs 又现占位造假——真目标存在却填 not-wired/假 Ok(vec![])，judge FAIL→照真 API 清单返修 PASS）。最终：53 方法 facade + KernelFacade 转发全落地，`cargo check --workspace`/product-full/108 测试全绿。**三条教训**：①验收必须显式 `--features product-full`（feature gate 后的代码单 crate check 编不到）；②lc/m27hs 在"找真实转发目标"非平凡时都会占位造假——任务书必须把真 API 清单（file:line）直接给出；③**m27hs 两次 git restore 误杀编排者未提交改动**（"不许 stage"被理解成"可以 restore"）——coder 任务期间编排者不在同仓留未提交改动，或任务书明写"禁止 git restore/checkout 任何非你创建的文件"。
- **队列**：K1 ✅ → **K2（desktop-tauri 切 facade + F1.5 chat-tool 合流）** → K3 ROI 闸门（K2 验收后实测 29.86s 基线 vs 目标 14.93s）→ K4a/K4b → K5。待补：HIGH-fan-out 探针（K2 期间）；lc 循环×3（E3/K1 实施/K1b1 续跑）已记台账——lc 对超长设计输入+大实现单不稳，K 线后续实施默认 m27hs + 处方级真 API 清单。
- 产物：`.opencode/sdd/kernel/`（k0-brief/report/review/cold log/timing html/tree 基线 + 两轮 northstar 评审）。
5. 小遗留：`pnpm run check:repo-hygiene` 报一个 pre-existing 评审文件（`.agents/skills/lightweight-agent-execution/review-prompt.md`）——看一眼该删就删。

### 外部评审冗余清单裁决（2026-07-20，用户已拍板）

接受并执行：api-layer/transport 删 crate（E3 待重派）、IPC flag descope（E4 ✅）、小死代码（E1 ✅）、CI/脚本（E2 ✅）。驳回：core lib.rs re-export 与 product_assembly 门面（facade 政策）、一行函数去重、test-support 孤立工具、死事件变体（F4 再说）、typescript/pptxgenjs（在用）。第二份清单在 `C:\Users\UmR\.qoderworkcn\workspace\mrs1mi22y3u2cmta\outputs\northing-redundancy-cleanup-2026-07-20.md`。

### Subagent 运维补充

- m27hs 不主动 commit（声称 coder 规则）——任务书里要明确"必须 commit"；F0.1 的 commit 是编排者补的。
- judge-m3 累计 ✅×11（含计划文档评审与清理复审），lc ✅×7，m27hs ✅×8。

## 2026-07-19 深夜存档（外部评审分流完成）

- **外部评审**（用户提供，qoderwork 强模型）：产出 `C:\Users\UmR\.qoderworkcn\workspace\mrs1mi22y3u2cmta\outputs\northing-review-2026-07-20.md`（4C/11I/10M，质量高）。评审导引在 `docs/reviews/2026-07-19-external-review-brief.md`。
- **四批修复全部 judge PASS**：A `95682b8`（前端 effect 生命周期/ErrorBoundary/init 错误 UI/发送失败标记，编排者亲自写）；B `e19f0ec`+`0089ad3`（MiniApp shell.exec 换行注入，C-1，含 6 个回归测试）；C `fac3013`+`81d78bc`（core_rt 启动握手/桥重试上限/spawn_blocking/日志降级/空文本拒绝）；D `2e86d54`（**C-4 架构修复：DialogTurnCompleted 移到持久化后发射**，watchdog 可取消、JoinHandle 入 map、try_send→send、queue 单锁）。
- **D 批遗留（judge 记录，非阻断）**：subagent 路径从此不再发 DialogTurnCompleted（已枚举全部消费者无回归，若未来 cron/MemoryAgent 需要，在 so_lifecycle/cleanup.rs 补发）；finish_reason 字符串被枚举折叠为 "error" 等（现无消费者依赖）；token stats 日志仍在 persist 前（语义可接受）。
- **遗留未修**：I-2 core runtime 优雅关停（需设计）；M-9 敏感诊断默认 true（待用户拍板是否翻默认）；M-10 标题任务取消（无现成 token）；I-9 前端 refetch 启发式（C-4 修复后已大幅缓解，message-id diff 替换归 F2 打磨）。
- **F1 已上线**：单会话布局（无会话列表，启动恢复最近会话）、think 折叠块（流式自动展开/正文自动收起/可手动）、agent 显示名（点击改名，存 `%APPDATA%\northhing\config\desktop-ui.json`）、头部右侧产物/选项按钮、流体布局跟随窗口、ErrorBoundary。调试面板移到"选项"菜单。
- **诊断工具沉淀**：`E:\agent-project\.opencode\sdd\f0\cap.ps1`（PrintWindow 后台截屏，全屏遮挡可截）；前端 debug 面板模式（选项里开关）。

- **HEAD `3e31619`，工作树 clean，无在跑任务，无未存改动**。桌面 Debug 实例可能仍在跑（含全部修复，可直接用；聊天回复挂起=W4 待修）。两个历史 stash（desktop onboarding WIP / MSVC troubleshooting）勿动。
- 今日提交链：D2a `2c8dd3f` → D2a+1 `eca4c4f` → D2b `06b2de3` → D2c `87f8a8e` → D2d `0f485d6` → D2e `da36f8d` → D2f `ea88414` → D2g `3e30f2c` → D2h `505b1a4` → D2i `5201bd1` → D2j `7218db2` → agentic mode `ef0e45b`。基础设施：codegraph MCP（`E:\agent-project\.opencode\opencode.json`，索引在 `.codegraph/`）+ 8 项目级 skills（`.opencode/skills/`，重启 opencode 生效）。
- 实证结论：后端 CLI exec 全链路可用（配置正确态：desktop 1 provider / core 1 model+primary，base_url 填到 `/v1`）；GUI 全部界面元素恢复（sessions/设置/banner/发送）。**大根因**：`Weak::upgrade()` 后台线程恒 None（Slint 1.16/Windows 实证）——凡后台线程先 upgrade 守卫的代码全部静默失效，与"跨线程写被丢弃"是同一枚硬币两面。
- GUI 自动化（open-computer-use@0.2.3）实证：Windows 运行时可用。要点：`element_index` 必须字符串；序列内首调 `get_app_state` 建状态；`type_text` 需 `OPEN_COMPUTER_USE_WINDOWS_ALLOW_UIA_TEXT_FALLBACK=1`；坐标点击比元素点击可靠；npm 全局装可能超时需重跑。
- Subagent 运维：kimi 变体停用（额度留编排者）。实证：lc ✅×5（中大型实施）、m27hs ✅×7（机械修复）、judge-m3 ✅×8（验收）、explore ✅（聚焦侦察）。范式：explore 侦察 → 编排者开处方 → coder 实施 → judge 验收 → 原 coder 返修 → 同 judge 复审。台账 `.opencode/model-capability-notes.md`。

### ~~下次接续 = W4~~ ✅ 已修复（2026-07-19，GUI 实测流式回复正常）

- **W4 根因**：`register_send_message_callback` 每次发送新建 `current_thread` 运行时跑 `block_on(submit)`；`submit` 内部 `tokio::spawn` 的 turn 任务落在该临时运行时上，`block_on` 返回后运行时被 drop → turn 在 init 中途被静默 abort（症状全中：日志止于 turn_init.rs:91、无 panic、无 watchdog、低 CPU）。修复 `8319d9e`：turn dispatch 改 spawn 到长生命周期 worker runtime（新 `app_state/turn_runtime.rs` OnceLock<Handle>，main.rs worker 线程注册）。judge-m3 PASS。
- **W4b 根因**（W4 修好后暴露）：事件桥 `flush_draft`/`refresh_messages` 在路由任务（worker runtime）上跑 `block_on` → panic "Cannot start a runtime from within a runtime"，**panic 把 system.rs:89 的路由循环整个杀死** → 所有事件断流（无流式、无终态刷新）。修复 `4672dee`：两函数改 async 直接 await（ModelRc 构造保持在最后 await 之后保 Send）+ router.rs route/route_batch 加 catch_unwind 隔离 subscriber panic。judge-m3 PASS。
- **诊断资产**：W4-P 探针（turn_init/turn_tick/dispatch_stream/stream_processor 等 11 点，`184f8ee`）+ headless 复现器 `src/apps/desktop/src/bin/w4_repro.rs`（--mode=dual/same/desktop）。**教训**：凡 `tokio::spawn` 的任务必须确认落在长生命周期 runtime 上；凡 async 上下文禁止 block_on——这两类在桌面 ~20 个 throwaway-runtime 站点里是普适隐患（其余站点自身 future 在 block_on 内完成，暂安全，已记技术债）。
- **GUI 实测通过**（2026-07-19 用户确认）：发 "hi" 流式回复正常；首条因 W4b 未渲染的回复在修复后也能正常显示。探针日志：turn 全程 4.7s，67 completion tokens。

### 下次接续 = 前端重构（用户拍板 2026-07-19）

范围已确认的 backlog：
1. **测试连接两个接线 bug**（后端 test_connection 是好的，纯 UI 接线）：①设置列表行测试按钮结果写 root `provider-test-result` 但只绑到 WelcomeView onboarding，设置面板无显示、行 verified 标记不刷新（回调存盘后没调 refresh_settings_lists）；②表单"测试连接"按钮（ProviderSettingsPanel.slint:372-381）错接成 `upsert-provider`（和保存一样），`form-test-result` 永远不会被设置。
2. **对话气泡太大**（消息区整个巨型紫色块，用户嫌丑）。
3. Inspector Model pill 显示 provider wire format 而非模型名（遗留小问题）。
4. 整体视觉重构（用户原话"现在的前端太丑了"）。
**重构必须保留的硬接线纪律**（全是踩坑换来的）：turn dispatch 走 `turn_runtime()` worker runtime；UI 写只能 `slint::invoke_from_event_loop`（Weak 后台 upgrade 恒 None）；invoke 闭包内禁 block_on；任何 async 回调上下文禁新建 runtime + block_on；事件桥 async 直 await；配置单一来源 core GlobalConfig。
任务书/报告归档：`E:\agent-project\.opencode\sdd\w4\`（probe/fix/W4b brief+report+评审包+GUI 日志）。
