# Handoff — 2026-09-02（凌晨收口）：W14-1c 三切片落地，剩切片 4/5

> 上一份：`2026-09-01-w14-1e-done-w15-1-next.md`。本份覆盖：资产整理日全部动作 + W14-1c-1/2/3 实施验收 + 用户改判。
> 写于 04:30（超宵禁，用户在场指挥），用户起床后由本文件继续。

---

## 0. 一句话状态

HEAD `655e96a`，**已推送 origin/main（ahead 0）**，工作区干净。
W14-1c 测试隔离：**5 个切片完成 3 个**（1=A 类迁移 ✅ / 2=B-1 批 ✅ / 3=B-2+E 类+CI 五路并行 ✅）；**剩切片 4（init gate 重写 + C/D 扫描）与切片 5（CI 双轨 5 轮观测）**。

---

## 1. 本段 commit（倒序）

| commit | 内容 |
|---|---|
| `655e96a` | ledger：W14-1c-3 五路并行波全验收 |
| `867ae2d` | W14-1c-3e：ci.yml 新增 rust-tests-serial job |
| `419e537` | W14-1c-3a：prompt_stability 测试复用 AgentRegistrationGuard 清理 |
| `4da37a2` | W14-1c-3c：REMOTE_STDIO clear seam |
| `c8fd3fa` | W14-1c-3d：project_memory_dir RAII seam + cli keyring mock |
| `d6245b8` | W14-1c-3b：DeepReview 双 tracker reset seam |
| `9cd72f4` | W14-1c-1 fix：doc(hidden) 标记 3 处测试专用可见性 |
| `3e085f1` | W14-1c-1：A 类 5 测试进程隔离迁移 |
| `d4ecc29` | W14-1c-2：B-1 批（diagnostics 迁移 + settings seam + INIT_GUARD） |
| `1d7e5ad` | E-13 翻转登记 + W14-1c-1 brief |
| 更早 | `0ff4903`/`fab148d`/`f7379cb`/`e151b54` 资产整理 4 连（handoffs 归档 19 篇 / D1-D6 入册 / sdd 归档 372 件 / 文档同步） |

memory 仓同日 8 commits：GBK 修复 / 蒸馏 / 热路径瘦身 / 决策回填 / 台账切分 / LEARNINGS+2 / qwf 台账×2。

## 2. 用户 09-02 改判（已登记 decision-register）

- **E-13 翻转**：W14-1 测试隔离线先于 W15（推翻 09-01 D4）。
- **W15-1 依赖准入已批**：允许引入 markdown crate，选型走技术仲裁闭环（不问用户）。

## 3. 关键机制教训（本 session 新增，已入 memory LEARNINGS）

1. **「Task cancelled」≠ 没跑**：取消信号只到编排者侧，活可能已跑完——派发返回异常先 `git log`/`git status` 查磁盘。
2. **`#[cfg(test)]` 对 tests/ 集成测试不可见**：迁出即需无条件 pub + `#[doc(hidden)]` 标记（仲裁书已补遗修订，w14-1b-arbitration.md 末节）。
3. **五路并行 coder 可行**：文件集不相交 + cargo 锁排队无死锁；qwf×2/vertex/agy×2 全一轮交付。

## 4. 模型台账变更（memory 仓已记）

- **coder-qwf（qwen3.8-flash）定级上调**：3 单实战（1c-2/3a/3d）——复用判断力超档（敢用证据顶回错误 brief）、证据链质量最高档（实机前后对比）、唯一毛病 = report 数字要磁盘核。可接中型机制单。

## 5. 队列

1. **W14-1c 切片 4**：init gate 测试局部重写（仲裁步骤 7，`kernel_facade/tests.rs:404`，并发敏感需审慎）+ C/D 锁纪律扫描（步骤 8，grep 清单 + 抽样 patch）
2. **W14-1c 切片 5**：CI 双轨 5 轮连绿观测（job 已入库 `867ae2d`，push 后看 CI 跑）
3. **follow-up**：`turn_persist.rs:546` 同类 project_memory_dir 泄漏候选（3d 发现）；settings push 测试触真实配置目录（3c/2 发现，归 E 类）
4. W15-1 Markdown（选型仲裁 → 实现）→ W15-2/3 → W16 → W17（后置）→ W18（设计已出可并行）

## 6. 环境/雷区（全仍有效）

- cargo 必走 `rustup run stable-x86_64-pc-windows-msvc`；输出 `cmd /c` 重定向（禁 PS 管道）；重复测试 `--no-run` 后直跑二进制；先读 skill `long-running-shell`。
- rot 闸：`let _ =` 371/388、`css.rs` 790/790、`unix_epoch_inline` 69/69；unwrap 483/502（本波 +6，闸内）。
- 推送先试直连，失败上 clash `127.0.0.1:7897`；SSH 不可用。
- worktree 零悬挂；分支 7 支（growth×6 + spike×1，勿动）；stash 全清。
- 宵禁 03:00（本 session 系用户在场指挥的超时执行，非先例）。

## 7. 起床后第一件事

1. `git status -sb` 校准（应 ahead 0 干净）。
2. 看 CI：`rust-tests-serial` job 首轮结果（`867ae2d` 推送后触发）。
3. 派 **W14-1c 切片 4**（brief 从仲裁书步骤 7/8 + 补遗规则出发；init gate 重写单派一个审慎的 coder——vertex 或 qwf，C/D 扫描可并行另一路，注意文件集）。
4. 切片 4 验收后 W14-1c 收官，转 W15-1（先派 markdown crate 选型仲裁）。

## Suggested skills

- `long-running-shell`（任何构建/测试前）
- `subagent-driven-development`（切片派发）
- `verification-before-completion`（验收口径）
