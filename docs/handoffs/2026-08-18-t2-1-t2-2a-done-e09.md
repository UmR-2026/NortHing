# Handoff 2026-08-18：T2-1 CI 收官 + T2-2a 删除批收官 + E-09 删留裁决 → 明日续 T2-2a'/T2-2b

> 上一篇：2026-08-17-cleanup-t0-done。本篇之后权威状态以本篇 + 登记册 + roadmap 为准。

## 需求基线状态

- 不变：thesis v1.4 / 登记册全生效 / roadmap T0-T6（0.3a = T0✅ + T2-1✅ + T2-2（进行中）+ T1×5）。
- **新决策 E-09（2026-08-18 用户拍板）**：tool-provider-groups 与 harness 实为活接线（T2-2a 侦察推翻 full-review R-18/SW2-2 的"零调用/直接删"定性），用户裁决**仍删，以计划为准**——执行方式 = 先拆装配面再删 crate（T2-2a' 单）。详见登记册 §3。
- PCS-3 权限框架语义已固化（roadmap T2.5 末「PCS-3 权限框架语义」段，commit `e65d98e`）：MiniApp `permission_policy` 默认拒绝三段式 + 路径作用域 + policy/执行点分离，**T2-2 MiniApp 删码前置①已清**。

## 今日已完成（commit 表）

northing 仓库（main 分支，未 push；repo 不可公开推送状态未变）：

| commit | 内容 |
|---|---|
| `05905ee` | **T2-1**：CI 补齐——`cargo check --workspace` 去 exclude（desktop/CLI 入 3 OS matrix 硬门，= 家规 6 技术面落地）+ `cargo test --locked --workspace` ubuntu-only；roadmap 过期描述修正；P2-15 翻 resolved（同 commit，家规 2）。双判决 PASS/0C/0I/4 Minor |
| `72397ea` | sdd: T2-1 台账行 + brief/report/review 归档 |
| `e65d98e` | **PCS-3 权限语义提炼**（编排者亲自写，T2-2 前置①） |
| `2dfb8e4` | **T2-2a**：死代码删除第一批 11.3k rs 行（insights 4,393 + webdriver 6,044 + plan-compliance-checker 894 + 19 条孤儿 workspace 依赖 + cli-internal 9 条死依赖含 northhing-core product-full 拽入，R-22 关闭）；boundary 规则 + surfaces + AGENTS.md/CN 同 commit 同步。双判决 PASS/0C/0I/5 Minor，judge 独立复跑全绿 |
| `1e6926b` | sdd: T2-2a 台账行 + recon/brief/report/review 归档 |

记忆仓：`e8744a3`（gemini-37-flash 首单实证 + judge-m3=minimax-m3 type 映射 + gemini-36 reviewer 静默失败记录）。

## 进行中卡点 / 立即下一步（明日）

1. **T2-2a'（拆接线 + 删 tool-provider-groups & harness，E-09）**：编排者已用 codegraph 摸过消费面（见下「T2-2a' 侦察要点」）；下一步 = 补一处 recon（materialization.rs 对 ToolProviderGroupPlan 的运行时消费语义 + `default_product_harness_registry` 的全部调用方）→ 写 brief → 派 `gemini-37-flash` → review（`minimax-m3`）。
2. **T2-2b（judge_gate 适配层 1,690L 删，协议层 1,473L 保留转 TH-5 词汇）**：需先做适配层/协议层切分侦察（G15 修正的边界），再写 brief。judge_gate 的 receipt 持久化教训写入 ledger（SW2-2 附带要求）。
3. 其后按 roadmap T2-2：remote 栈（embedded relay 入口先摘后删）→ MiniApp（前置①已清；**前置②未清：UI 入口摘除需前端 session 配合，见下**）→ relay-server/relay-core 整删。

## 队列（blocking 边）

- **T2-2 前置②未清**：MiniApp/remote 栈的 UI 入口摘除（feature/配置/UI）需**前端 session** 配合——三 session 撞点之一。编排线可先派不含 UI 的删除子批；涉及 UI 入口时必须先与前端 session 对齐。
- T1 五项安全在 T2-2 瘦身后做；0.3b blocked by 0.3a。
- 并行可行性不变：growth = agent_memory/growth crate；前端 = slint UI；编排线删除批避开两者文件集。

## T2-2a' 侦察要点（已查，直接可用）

- **tool-provider-groups（package `northhing-tool-packs`）删除面**：根 Cargo.toml:24 member；core Cargo.toml:100（optional dep）/ :203（"tool-packs" 编入 product-full）/ :232（feature 定义）；`core/src/agentic/tools/product_runtime/materialization.rs:9` 的 `ToolProviderGroupPlan` 消费（**待查：该 plan 在运行时如何 materialize 进 tool registry——拆接线语义关键点**）；product-capabilities Cargo.toml:15 + lib.rs:15-16 + `try_tool_provider_group_plan`/`tool_provider_group_plan`/`tool_provider_group_ids`（lib.rs:369-390）+ try_build_assembly:413 接线 + tests；boundary 四文件（crate-layout.mjs:16 / crate-rules.mjs:16,:36,:63,:91 / feature-rules.mjs:15,:138,:145-150 / self-test.mjs:133,:161,:713-733,:1972-1976,:2313-2326——self-test 断言 manifest 原文存在，必改）；surfaces.md:36；execution/AGENTS.md:20；根 AGENTS.md:27（L5 行）；core/AGENTS.md:83。
- **harness（package `northhing-harness`）删除面**：根 Cargo.toml:22 member；core Cargo.toml:91（非 optional）+ `agentic/harness.rs` 整文件（facade）+ `agentic/mod.rs:26` + `product_assembly.rs:9` re-export；product-capabilities Cargo.toml:13 + lib.rs 的 HarnessProviderDescriptor 段（:207,:261-263,:316-322,:392-407,:414,:465-552，含 DeepReview/DeepResearch/MiniApp 三 provider 常量与 `product_harness_registry_for_profile`/`default_product_harness_registry`）+ tests；boundary crate-layout.mjs:14 / crate-rules.mjs:9；surfaces.md:25（顺带修「SDLC Harness」误标）；execution/AGENTS.md:18；根 AGENTS.md:27（L5 行）；core/AGENTS.md:78。
- **已知事实**：harness registry 仅在测试中构建、provider `execute()` 按设计报错（"PR4 must not move concrete workflow execution out of legacy paths"）——是已接线脚手架，拆除不改变运行时行为，但 diff 面在活装配代码上，review 要盯"不多拆"。
- `harness plan()`（lib.rs:225）另有调用方 `core/src/agentic/agents/definitions/modes/mod.rs`——**明日 brief 前必须查清该处语义**（agent modes 是否运行时消费 harness plan；若是，删除影响面升级）。

## Minor 台账（全部指向 0.3a 终审 triage）

- T2-1：M-2 ledger 措辞粒度、M-3 报告行号 cosmetic（M-1/M-4 已闭）。
- T2-2a：M1 dev.cjs watch 段 6 旧路径并 1 广义路径（commit msg 已注）；**M2 `scripts/test_reference_skill.cjs:56-58` 残留指向已删 pcc 的测试条目（孤儿脚本，会 FAIL 但无 caller）**；**M3 `scripts/dev.cjs:99/:105` pre-existing 语法坏（mojibake 字面量，`node --check` 报错）**；M4 历史 docs 残留 webdriver/pcc/insights 字符串（`docs/AGENT_ONBOARDING.md` 是活文档，`--exclude northhing-webdriver` 示例已失效）；**M5 `check-core-boundaries.test.mjs` 1 条 pre-existing 失败（self-test.mjs:2941 tool-contracts anchor rule 未同步）**。
- 建议：M2/M3/M5 可合成一个 XS 清理单；M4 走 docs-cleanup 单。
- i18n-contract 24 个预存失败：冻结期漂移，登记给 T2-3（非 T2-1 范围，CI 无 i18n job）。

## Subagent 运维变更（2026-08-18）

- **gemini-37-flash 首两单实战全绿**（T2-1 + T2-2a）：探针 PASS、报告与 diff 逐条一致、冷/热编译处理得当、主动同步 AGENTS-CN.md 中文镜像（brief 未列，正确主动行为）。coder 主推位坐实。⚠️ 按量计费端点（$0.75/$3.75 MTok）。
- **reviewer/gemini-36-flash_reviewer 一次静默失败**（空返回、零 assistant 消息，疑似瞬态）；**minimax-m3 两单 judge 质量在线**（独立复跑 cargo/boundary、spot-check 纪律、commit-time 项正确归为编排者侧）。本环境 task 工具 **judge-m3 实际 type = `minimax-m3`**（`judge-m3` 直名会报 Unknown agent type）。
- explore 子代理（内建）T2-2a 侦察质量高：推翻 full-review 两条定性（tool-packs/harness 活接线）均被人工抽查坐实——侦察单可放心派 explore，但关键结论仍须抽查核对。

## Suggested skills（下个 session）

- `subagent-driven-development`（T2-2a'/T2-2b 任务循环）
- `requesting-code-review`（每单 reviewer 派发）
- `verification-before-completion`（每个收口前）
- `handoff`（本轮结束时）

## 验证基线（本 session 实测）

- T2-1：cargo check --workspace（MSVC 冷编译后增量 2.55s）pass；check-core-boundaries pass；git diff --check 0 误。
- T2-2a：cargo check --workspace pass（judge 独立复跑增量 5.94s）；cargo check -p northhing pass（2.35s）；check-core-boundaries pass；cargo metadata 解析 0 错；28 条旧 warning 无新增。
- ⚠️ 环境：默认 PATH cargo 是 GNU 缺 gcc.exe；一律 `rustup run stable-msvc cargo`。
