# Handoff — 2026-08-31：W10/W11 收官 + 需求校准裁决 + 391 commit 推送

> 焦点：god-file 拆分波（W10）、CLI 侧清理波（W11）全部收口，全量套件 2480 通过；需求冲突 C1–C8 用户裁决完毕。**唯一残余 = 真机实测 10 项（用最新构建，仍未跑）。**
> 上一 handoff：`2026-08-28-slint-removed-dioxus-sole-shell.md`（W9 收官，覆盖到 08-29 13:11）。本文件覆盖 08-29 13:11 → 08-31 的全部进展。

## 状态一句话

W10（api.rs 799→266、windows.rs 800→`windows/` 目录模块，全量测试 113 套件 2480 通过 / 0 失败）与 W11（css.rs 死规则清理 829→790、selectors 克隆集群 A 层、CLI chat 编辑丢 key 修复）全部收官；法官盲审校验两轮完成（judge checklist 有效性质证）；需求《论题 v1.4》vs《PRD v0.1.0》冲突 C1–C8 已由用户拍板；防腐生长实验 v1.0 立项待选实验组。

## 需求基线

- 权威：`.superpowers/sdd/progress.md`（波次台账，逐任务含 commit + 判决）、`docs/product/requirements-vs-current-2026-08-29.md`（需求 vs 现状校准表，C1–C8 裁决）、`docs/development/anti-rot-growth-experiment.md`（实验设计 v1.0）。
- 已裁决（用户 2026-08-29，不再重开）：C1 远程工作区作废 / C2 多会话 CRUD 要做（含 subagent 详情可见）/ C3 子代理低显著度可见 / C4 文件树进右面板（W9-6 已交付）/ C5 Dioxus 唯一壳 / C6 selectors 页面级合并走薄抽象层 / C7 编辑模型留空=保留（W11-3 已修）/ C8 选模型保持强写。
- W9 功能补缺七项（确认门三档、记忆面板、降级横幅、会话管理、技能管理、文件树、卡片做真）已于 08-29 13:11 全部交付，终审 0C/0I/3M。

## 已完成（08-29 13:11 → 08-31，倒序）

| 波 | 内容 | commits | 判决 |
|---|---|---|---|
| W11-3 | CLI chat 编辑模型丢 key 修复（决策 C7） | `69fb851` + doc `cfd5ece`（C6/C7/C8 记录） | CLEAN 0C/0I/0M（step-explore_reviewer；implementer minimax-m3），cli 52/52 |
| W11-2 | selectors 克隆集群 A 层（33 处 `block_in_place` 迁 W8-1 bridge；helper 提取至 model_selector.rs 唯一 owner；魔数常量化） | `33bb4a4` + doc `b203ec5` | PASS 0C/0I/0M（step-explore_reviewer；implementer minimax-m3），selectors 861→827，ceiling 同步下调 |
| W11-1 | css.rs 死规则/死声明清理 + 闸口游戏回滚 + R7.2→R8.1 属性迁移 | `76d2c33` + doc `5a5c1a6` | PASS 0C/0I/1M（minimax-m3；implementer step-explore），css.rs 829→790，ceiling 830→790 |
| 盲审校验 | 5 项 judge checklist 盲审校验 ×2（theme.rs / 5 个对照文件） | `600f21b` `2667aeb` | 二轮：0 漏判 0 误报，3 条首轮判定被推翻 → checklist E 项加固 |
| 防腐实验 | 大文件不腐化边界条件实验设计 v1.0 | `ac2dfb1` | 用户立项，执行待开 |
| W10-3 | 全量测试收口（编排者实跑） | `dbc0fe6` | 113 套件全 ok / 0 FAILED / 2480 通过（日志 `C:\WINDOWS\TEMP\opencode\w10-3-full-test.log`）；check + rot + hygiene 全绿 |
| W10-2 | windows.rs 800 → `windows/`（mod 114 / self_app 281 / facility 221 / work 241） | `b50ba6e` + 修复 `b284fa4` | 一轮 FAIL 1C（`DOCK_GAP_PX` 位移丢失，会每帧内缩 16px）→ 修复后 PASS 0C/0I/3M |
| W10-1 | api.rs 799 → 266（api_settings 292 / api_events 253 / api_memory 22） | `078af44` + doc `6d6cccc` | PASS 0C/0I/1M（minimax-m3；implementer step-explore） |

## 进行中卡点（阻塞项）

1. **真机实测 10 项 —— 唯一人工阻塞，表仍全空**：`.superpowers/sdd/manual-test-checklist-2026-08-27.md`，要求构建 ≥ `2cfd737`（当前 HEAD 远新于此，旧实例不算数）。覆盖：折叠态 / 抽屉三窗跟随 / 防跳底 / Z-order / provider 编辑全流 / MCP+LSP 子进程无残留 / 退出无残留 / 会话持久化 / 确认门三档 / 记忆面板与文件树真机验证。结果表 10 行未填。
2. **观察项 O-1（不阻塞，下波排查）**：`test_delete_provider_default_provider_rejected` 约 25% 概率 flaky，根因疑似 `GlobalConfigManager.initialize` 跨测试并发窗口；`TEST_GLOBAL_CONFIG_MUTEX` 仅覆盖 `*_with_keyring` 路径，未覆盖 `kernel_facade().upsert_model_config(...)` 自身。W10-3 全量那一跑是绿的。

## 队列（无 blocking 边，可并行）

- **防腐生长实验**：选实验组文件（按设计 §4 选拔标准：单一职责 / 数据声明为主 / 有测试 / 近期有人管），登记 ceiling 并注明 growth-permitted。对照组 = 现有 6 个 god-file，ceiling 钉死：css.rs 790 / pages_onboarding 866 / cli theme.rs 989 / memory_db 894 / selectors 827 / lsp/manager 836。
- **下个桌面波候选**：贴线文件 pages_onboarding(866) / lsp manager(836) / memory_db(894)；CLI 侧 theme.rs(989) 与 selectors(827) 属另一工作区（不相交文件集，可与桌面波并行，注意 cargo 锁互踩）。
- **小单**：CLI popup 映射去重（key_popups.rs）；css.rs:57 scrim 陈旧注释（W11-1 遗留 Minor）；selectors B 层页面级合并（等 C6 薄抽象层搭车，附合并难点 top3：视图调用对象差异 / Scheme C 不对称 / apply_model_selection 强写——均需产品决策）。
- **W11-2 偏离待处理**：`chat/{mcp,commands,run}.rs` 15 处 bridge 未迁（出 cluster 范围）；sentinel 未 Option 化（语义面，留决策）。
- **W11-3 遗留观察（需用户决策）**：chat 编辑时新输入的 key 不回写 keyring（重启即失）——startup 新建路径有回写，更新路径两侧均无；若要"编辑即写穿"，5 行 + 1 测试可达。
- **W8 遗留架构欠账**：popup dispatch 下沉、apply_exit_reason 8 参数、provider_display_name 竞速解析。
- **rot-probe P2**：auth_oauth(12)/lifecycle(11)/navigation(7) 等 30 处 `let _ =` 静默错误处理。
- **产品面未开工**：PCS 插件系统（论题 0.3 末）、身份"位格/准则"真实数据源、成长/演化 UI（TH-5）、半被动 cron UI（TH-6）、验收环第 6 步。
- **老欠账**：T2-1 CI 补齐（前置：i18n-contract 24 个预存失败）；F3 几何跟随线程（等 dioxus 0.8 stable 事件钩子）。

## 环境与工具教训（08-29 新增）

- **hygiene 闸会扫出历史预存问题**：W10-3 因 progress.md 进变更集，扫出一条 Wave-2 旧台账行含本机绝对路径（预存）→ 就地脱敏（路径泛化，信息保留）。教训：动 progress.md 前先预期 hygiene 会翻旧账。
- **`pnpm run desktop:dev` 本机仍跑不起来**（PATH 上 GNU cargo 遮住 rustup shim）：正确拉起 = `C:\Users\UmR\.cargo\bin\rustup.exe run stable-x86_64-pc-windows-msvc cargo run -p northhing`。全量测试同前缀。
- **纯位移拆分单的 judge 逐常量核对不是形式**：W10-2 真抓到 `DOCK_GAP_PX` 漏移（会改变窗口行为）。
- **w9-6 截图是 SVG mockup 而非真机**（本机无 GUI 运行时）：`w9-6-shot-1.svg` + NOTE.md 说明重拍步骤。凡需真截图的验收，先确认壳能否启动。

## subagent 运维变更（08-29 实证）

- **`minimax-m3` 已实证可做实现单**：W11-2 / W11-3 均一轮 PASS/CLEAN（原 judge 位跨档使用成功）。judge 位仍主推 `judge-m3`。
- **`step-explore` 双位可用**：W11-1 实现单（出活质量高）+ W11-2/W11-3 reviewer 位。注意其曾有一次静默失败后续派脱轨的记录（见上一 handoff 过程事件），返回先查 git log。
- **`gemini-37-flash` / `-agy` 双渠道**仍为 coder 主推；08-29 这波主力是 minimax-m3 + step-explore。
- **judge checklist 已校验**：5 项法盲审两轮，0 漏判 0 误报，E 项（证据抽查）加固。派 judge 时继续附 `.opencode/templates/judge-brief-block.md`。

## 下 session 第一件事（按序）

1. **真机实测 10 项**（唯一人工项）：先重拉最新构建（HEAD ≥ `cfd5ece`），再按清单逐项跑并填结果表；失败即新 finding 进队列。
2. 实测之外，队列里唯一需要用户拍板的是 **防腐生长实验实验组选拔** 和 **chat 编辑 key 是否回写 keyring**；其余均可编排者闭环。

## 远程同步

本 handoff 提交时，本地 main 曾领先 origin/main 391 个 commit（origin 停在 `ae44334`，8 月初）；本次随推送一并同步至 GitHub（`UmR-2026/NortHing`，main）。

## Suggested skills（下 session）

- `subagent-driven-development`（开新波前）
- `anti-rot-system`（触碰 6 个观测 god-file 或跑 rot 闸时必用）
- `requesting-code-review` / judge brief 模板（派审查前）
- `verification-before-completion`（声称完成前，尤其桌面侧 `cargo check -p northhing`）
- `handoff`（本波收口时续写，勿再漏）
