# v0.1.0 Review Checklist

**Review Date:** ___________  
**Reviewer:** ___________  
**Branch:** `main`  
**Commit:** `157d593`  
**Tag:** `v0.1.0`

---

## 1. 环境准备

- [ ] 检出正确的 commit：`git checkout v0.1.0`
- [ ] Rust 工具链版本：`rustc --version` ≥ 1.85
- [ ] 确认在 `main` 分支：`git branch --show-current`
- [ ] Clippy 已安装：`rustup component add clippy`（如未安装）
- [ ] 注意：Windows 上 desktop/webdriver 测试可能因 DLL 缺失崩溃，需排除

---

## 2. 构建验证

| 检查项 | 命令 | 期望结果 | 实际结果 | 通过? |
|--------|------|----------|----------|-------|
| Workspace lib 编译 | `cargo check --workspace --lib` | 0 errors | | ☐ |
| CLI 编译 | `cargo build -p northhing-cli` | 成功 | | ☐ |
| 核心 crate 编译 | `cargo check -p northhing-core` | 成功 | | ☐ |
| Runtime ports 编译 | `cargo check -p northhing-runtime-ports` | 成功 | | ☐ |
| GUI 编译 (Windows) | `cargo build -p northhing-desktop --target x86_64-pc-windows-msvc` | 成功（编译通过，测试因 DLL 缺失需排除） | | ☐ |

---

## 3. 测试验证

| 检查项 | 命令 | 期望结果 | 实际结果 | 通过? |
|--------|------|----------|----------|-------|
| Runtime ports 测试 | `cargo test -p northhing-runtime-ports` | 46+ passed | | ☐ |
| Runtime services 测试 | `cargo test -p northhing-runtime-services` | 7+ passed | | ☐ |
| Core session manager 测试 | `cargo test -p northhing-core session_manager` | 35+ passed | | ☐ |
| AI adapters 测试 | `cargo test -p northhing-ai-adapters` | 131+ passed | | ☐ |
| Workspace lib 测试 | `cargo test --workspace --lib --exclude northhing --exclude northhing-webdriver` | 1456+ passed, 0 failed, 2 ignored | | ☐ |

---

## 4. Clippy 验证

| 检查项 | 命令 | 期望结果 | 实际结果 | 通过? |
|--------|------|----------|----------|-------|
| 无 errors | `cargo clippy --workspace --lib` | 0 errors | | ☐ |
| Warnings 数量 | `cargo clippy --workspace --lib --exclude northhing --exclude northhing-webdriver 2>&1 | grep -c "warning:"` | ≤ 15 | | ☐ |
| 无 dead_code | `cargo clippy --workspace --lib --exclude northhing --exclude northhing-webdriver 2>&1 | grep -c "dead_code"` | 0 | | ☐ |
| 无 unused | `cargo clippy --workspace --lib --exclude northhing --exclude northhing-webdriver 2>&1 | grep -c "unused"` | 0 | | ☐ |

---

## 5. 代码审查（重点文件）

### R3: SessionStoragePathResolution Enum

| 文件 | 检查项 | 通过? |
|------|--------|-------|
| `src/crates/contracts/runtime-ports/src/lib.rs` | Enum 定义清晰，variant 命名合理 | ☐ |
| | 自定义 `Serialize` 实现正确，JSON 输出与原来一致 | ☐ |
| | 自定义 `Deserialize` 实现正确，能解析旧 JSON | ☐ |
| | `effective_storage_path()` 方法返回正确的路径 | ☐ |
| | `storage_kind()` 方法返回正确的 kind | ☐ |
| | `remote_connection_id()` 和 `remote_ssh_host()` 正确 | ☐ |
| `src/crates/assembly/core/src/agentic/session/session_store_port.rs` | 构造函数使用正确 | ☐ |
| `src/crates/assembly/core/src/agentic/session/session_manager.rs` | `.effective_storage_path()` 调用正确 | ☐ |
| `src/crates/execution/runtime-services/src/test_support.rs` | `local()` 构造函数使用正确 | ☐ |
| `src/crates/contracts/runtime-ports/tests/session_store_contracts.rs` | 测试覆盖 Local/Remote/UnresolvedRemote | ☐ |
| | Serde 序列化测试通过 | ☐ |

### B2: Tracing 迁移

| 检查项 | 通过? |
|--------|-------|
| 无 `use log::` 残留（除文档外） | ☐ |
| `tracing::debug!`/`info!`/`warn!`/`error!` 使用正确 | ☐ |
| Cargo.toml 中 `tracing` 依赖已添加 | ☐ |
| 日志输出格式与原来一致 | ☐ |

### C2: 死代码清理

| 检查项 | 通过? |
|--------|-------|
| 未使用 import 已移除 | ☐ |
| 未使用变量已加 `_` 前缀 | ☐ |
| unnecessary `mut` 已移除 | ☐ |
| 无功能代码被误删 | ☐ |

---

## 6. 发布验证

| 检查项 | 期望结果 | 实际结果 | 通过? |
|--------|----------|----------|-------|
| `Cargo.toml` 版本号 | `0.1.0` | | ☐ |
| `CHANGELOG.md` 存在 | 是 | | ☐ |
| `CHANGELOG.md` 内容准确 | 包含所有 Phase A-D 变更 | | ☐ |
| Git tag `v0.1.0` 存在 | `git tag -l | grep v0.1.0` | | ☐ |
| Tag 指向正确 commit | `git rev-parse v0.1.0` = `157d593` | | ☐ |

---

## 7. 文档审查

| 检查项 | 通过? |
|--------|-------|
| `CHANGELOG.md` 格式符合 Keep a Changelog | ☐ |
| `docs/reviews/2026-06-24-mvp-pre-review-guide.md` 已更新为最终状态 | ☐ |
| 提交信息清晰描述了变更 | ☐ |

---

## 8. Review 结论

### 阻塞问题

| # | 问题描述 | 严重性 | 状态 |
|---|----------|--------|------|
| 1 | | | |
| 2 | | | |

### 非阻塞建议

| # | 建议 | 优先级 |
|---|------|--------|
| 1 | | |
| 2 | | |

### 最终决策

- [ ] **通过** — 可以发布 v0.1.0
- [ ] **有条件通过** — 需要修复阻塞问题后发布
- [ ] **不通过** — 需要重大修改

**Reviewer 签名：** ___________  
**Date：** ___________

---

## 9. 发布后检查清单（Review 通过后执行）

- [ ] 创建 GitHub Release（基于 tag `v0.1.0`）
- [ ] 更新 `docs/PROJECT_STATE.md`
- [ ] 更新 `CODE_REVIEW.md` P0/P1 标记为 FIXED
- [ ] 通知团队 v0.1.0 已发布
- [ ] 合并到 `release` 分支（如存在）
- [ ] 更新网站/文档版本号

---

## 附录：快速验证脚本

```bash
#!/bin/bash
set -e

echo "=== v0.1.0 Review Verification ==="
echo ""

echo "1. Checking version..."
grep "^version" Cargo.toml

echo ""
echo "2. Checking tag..."
git tag -l | grep "v0.1.0"

echo ""
echo "3. Building workspace..."
cargo check --workspace --lib

echo ""
echo "4. Running tests..."
cargo test --workspace --lib

echo ""
echo "5. Running clippy..."
cargo clippy --workspace --lib

echo ""
echo "=== All checks passed! ==="
```
