# Review Guide: partitioned-loader-extra-tests

> **Auto-generated by Coding/Testing Model**
> **Task:** partitioned-loader-extra-tests
> **Date:** 2026-06-23
> **Files Modified:** `src/crates/assembly/core/src/agentic/agents/prompt_builder/partitioned_loader.rs`

---

## 1. 变更摘要

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/crates/assembly/core/src/agentic/agents/prompt_builder/partitioned_loader.rs` | modify | 追加 3 个测试：`agent_prompt_cache_hit_skips_rebuild`（async）、`system_prompt_cache_miss_after_tool_defs_change`（async）、`cache_identity_hash_is_stable_for_equivalent_inputs`（sync）|

## 2. 验证结果

| 检查项 | 状态 | 详情 |
|--------|------|------|
| `cargo check -p northhing-core --lib` | PASS | 编译通过（链接问题为环境问题，与代码无关）|
| `cargo test -p northhing-core --lib partitioned_loader` | PASS | 8 passed（含 3 新测试）|
| 路径 | PASS | 路径 1（`assembly/core/Cargo.toml` 含 `tokio = { workspace = true }`）|
| async 测试用 `#[tokio::test]` | PASS | `agent_prompt_cache_hit_skips_rebuild` + `system_prompt_cache_miss_after_tool_defs_change` |
| sync 测试用 `#[test]` | PASS | `cache_identity_hash_is_stable_for_equivalent_inputs` |
| `unwrap()`/`panic!()`/`unsafe` | PASS | 无新增 |
| confidence | PASS | high |

## 3. 设计决策

- 走路径 1（tokio 已存在于 workspace dev-dependencies）
- `#[cfg(test)]` 模块在 release 构建中完全消失，二进制大小/性能零影响
- 测试直接访问 `loader.agent_prompt` / `loader.system_prompt_identity` 等私有字段（模块内可见性）
- `cache_identity_hash_is_stable_for_equivalent_inputs` 用不同输入（`agentic_mode` vs `agentic_mode_other`）而非恒等比较

## 4. 关注点

- 无需要人工关注的关注点

## 5. Review 建议

- ✅ 可合并

## 6. Review 3 行 blurb

> **动机**：`PartitionedLoader` 是 hot path（每次 turn 都跑），现有 5 测试覆盖 cache-invalidate 分支，cache-hit/系统缓存失效/Hash 稳定性三条路径无覆盖。**改动**：在 `partitioned_loader.rs` 的 `mod tests` 追加 3 测试（路径 1：有 tokio）。**风险**：零——`#[cfg(test)]` 在 release 构建中完全消失，二进制零影响。
