# Review Guide: runtime-ports-lightweight-task-serde

> **Auto-generated by Coding/Testing Model**
> **Task:** runtime-ports-lightweight-task-serde（bonus fix）
> **Date:** 2026-06-23
> **Files Modified:** `src/crates/contracts/runtime-ports/src/lightweight_task.rs`

---

## 1. 变更摘要

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/crates/contracts/runtime-ports/src/lightweight_task.rs` | modify | 为 `LightweightTaskOutput` 每个 variant 和字段显式添加 `#[serde(rename = "camelCase")]`；修复 `output_tag_is_stable` 测试 |

## 2. 验证结果

| 检查项 | 状态 | 详情 |
|--------|------|------|
| `cargo test -p northhing-runtime-ports --lib` | PASS | 43 passed（之前 42 passed + 1 FAILED → 43 passed + 0 failed）|
| `output_tag_is_stable` | PASS | 修复后通过 |
| JSON 输出格式 | PASS | `{"kind":"toolResult","toolName":"file_search","output":"ok"}` |
| `unwrap()`/`panic!()`/`unsafe` | PASS | 无新增 |
| confidence | PASS | high |

## 3. 设计决策

- `#[serde(rename_all = "camelCase", tag = "kind")]` 只对 variant 名称和字段名称生效，不对 variant **内部**字段名生效
- 因此 `ToolResult.tool_name` 需要显式 `#[serde(rename = "toolName")]` 而非继承 `camelCase`
- 同理应用于 `NoToolMatched { reason }`（字段不变因为已是 camelCase）

## 4. 关注点

- 无需要人工关注的关注点

## 5. Review 建议

- ✅ 可合并

## 6. Review 3 行 blurb

> **动机**：回归测试发现 `LightweightTaskOutput::ToolResult` 序列化后 `toolName` 为 `Null`——`rename_all = "camelCase"` 不作用于 variant 内部字段。**改动**：显式为每个 variant 和字段添加 `#[serde(rename = "camelCase")]`。**风险**：零——纯序列化修复，不改运行时行为。
