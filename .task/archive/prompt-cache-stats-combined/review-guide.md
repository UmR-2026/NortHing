# Review Guide: prompt-cache-stats-combined

> **Auto-generated by Coding/Testing Model**
> **Task:** prompt-cache-stats-combined
> **Date:** 2026-06-23
> **Files Modified:** `src/crates/execution/agent-runtime/src/prompt_cache.rs`

---

## 1. 变更摘要

| 文件 | 类型 | 说明 |
|------|------|------|
| `src/crates/execution/agent-runtime/src/prompt_cache.rs` | modify | `PromptCacheStats` 新增 `combined_total()` + `combined_hit_rate()` 方法；追加 2 测试 |

## 2. 验证结果

| 检查项 | 状态 | 详情 |
|--------|------|------|
| `cargo check -p northhing-agent-runtime --lib` | PASS | 编译通过 |
| `cargo test -p northhing-agent-runtime --lib prompt_cache::tests::combined` | PASS | 2 passed |
| 零除保护 | PASS | 返回 `0.0` 而非 `NaN` |
| 复用现有方法 | PASS | `combined_total = system_prompt_total + user_context_total` |
| `unwrap()`/`panic!()`/`unsafe` | PASS | 无新增 |
| confidence | PASS | high |

## 3. 设计决策

- `combined_total()` 复用已有的 `system_prompt_total()` + `user_context_total()`，不重复计算
- `combined_hit_rate()` 的零除保护与已有 `system_prompt_hit_rate()` 行为一致（返回 `0.0`）
- 测试 `combined_hit_rate_averages_two_caches` 用非对称输入（system: 2/3, user: 1/1 → 3/4 = 0.75），避免恒等式假阳性

## 4. 关注点

- 无需要人工关注的关注点

## 5. Review 建议

- ✅ 可合并

## 6. Review 3 行 blurb

> **动机**：运营仪表盘要"一个总命中率"而非两个分命中率。**改动**：`PromptCacheStats` 新增 `combined_total()` + `combined_hit_rate()` 方法，追加 2 测试。**风险**：零——纯计算函数，无 IO、无锁、无 panic 路径。
